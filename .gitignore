# Build output (always safe to delete)
build/
out/

# CMake / editor scratch
CMakeUserPresets.json
.cache/
compile_commands.json

# Visual Studio / MSVC
*.user
*.pdb
*.ilk
*.idb
.vs/

# Third-party art kept purely as visual reference. Not ours, never shipped, and
# deliberately outside assets/ so the build cannot copy it into the game.
reference/

# Magnified texture sheets written by tools/preview-textures.ps1
*-preview.png

# Screenshots written by tools/capture-window.ps1, plus redirected game output
*-shot.png
capture.png
run.log
run.err
