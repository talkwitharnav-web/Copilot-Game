# LESSONS.md

<!-- INDEX -->

**Index** - 21 sections. Line numbers, so a reader can jump straight there.
**Generated by `tools/index-docs.ps1`; do not hand-edit. Re-run it after editing this file.**

- **1** &middot; LESSONS.md
  - **45** &middot; Where to look
  - **55** &middot; Creature models and box nets
  - **105** &middot; Animation and rigging
  - **129** &middot; Vulkan and the renderer
  - **230** &middot; Textures, atlases and sprites
  - **290** &middot; World, chunks, meshing and light
  - **381** &middot; Physics, collision and movement
  - **441** &middot; Creatures: AI, spawning, behaviour
  - **493** &middot; Interface, HUD and inventory
  - **554** &middot; Saving, settings and persistence
  - **572** &middot; Build, tooling and PowerShell
  - **643** &middot; Process and working method
  - **713** &middot; Survival, audio and the M21-M22 round
  - **725** &middot; Villages, villagers and the golem (M29d)
  - **740** &middot; Block models and the playtest round (M29e)
  - **795** &middot; Redstone blocks, and four ways to be wrong about them (M29h)
  - **813** &middot; Potions, signs and the last three hundred entries (M29i–M29k)
  - **823** &middot; Mining, drops and loot: five shapes a clean build cannot see
  - **836** &middot; Sixteen agents in parallel: seams, comments and asserts (2026-08-18)
  - **880** &middot; Twenty-seven agents, and nothing could be compiled (2026-08-19)

<!-- /INDEX -->

Every mistake this project has already paid for once, and what it cost.

**Split out of `CLAUDE.md` on 2026-08-06.** That file is attached to every session and this
one is not, which is the entire point: the lessons are worth keeping and are not worth paying
for on a turn that never goes near their subject. `CLAUDE.md` still owns the rules and the
architecture; `DECISIONS.md` owns the reasoning behind settled choices; this owns the scar
tissue.

**Read the part that matches what you are about to change.** The single most common shape of
bug in this codebase is a value derived somewhere other than the one table that owns it.

---

## Where to look

**The index at the top of this file is the map** — eighteen subject sections, each with the
line it starts on. Jump to the one that matches what you are about to change; there are more
than six hundred lessons here and reading them all is not the intended use.

A hand-kept table of per-section counts used to sit here as well. It was a second copy of
something the index already knows, it went out of date every time a lesson was added, and it
is exactly the shape of bug this file is full of warnings about. Deleted on 2026-08-10.

## Creature models and box nets

- ⛔⛔ **A pipeline can agree with itself about something false, and then no single file looks wrong.** Tinted glass drew as a uniform ghost with no border and no highlight streak. Three separate layers had each decided that glass transparency "rides on the vertex": staging **flattened** the art to fully opaque, the mesher handed the whole face one *averaged* alpha, and the shader threw `texel.a` away on the blended path. Every one of them carried a comment justifying itself, and one of them said outright **"do not fix this by exempting them"**. The block's entire appearance was in the alpha channel - a frame at 200, a streak at 155, a panel at 110 - and all three levels were gone before the renderer ever ran. **When something looks uniformly wrong rather than wrong in places, suspect that the variation was destroyed upstream, and go and measure the asset.** One `GetPixel(...).A` loop over the staged file and the reference settled it in seconds: 256 opaque against 256 partial.
- ⛔ **A cutout alpha test and see-through art cannot share a texture.** The reason the flattening existed at all was that the shader discards any texel under half alpha, and glass sits either side of that line - so un-flattening alone would have punched the panel out and left a frame. The fix is a flag on the *pass* (`kDrawFlagBlended`), and it is only safe because a blended fragment does not write depth, which is the thing the test protects. **Check what a guard is actually guarding before you exempt anything from it.**
- ⛔ **"A block is drawn in four places" caught a fifth time, and the drop was again the one that would have shipped broken.** Changing the glass art fixed the world, left the inventory icon right by luck (the HUD shader blends and has no alpha test), and would have put a **wireframe cube** on the floor, because dropped items are built into the opaque mesh. `ItemEntities::buildMesh` now takes a second mesh for the blended ones. **After changing how a block looks, walk the four paths out loud: mesher, icon, drop, thrown.**

- ⛔⛔ **A timer that is set but read behind a flag it does not have produces nothing at all, and it will sit there for milestones looking like an animation.** An archer's shot set `swingTimer` on every arrow, and both swing curves are gated on `species.swingsArms`, which archers deliberately do not set. So the line ran, the timer counted down, and **not one vertex moved** — for as long as archers had existed. **When something "already has an animation", find the code that reads the timer, not the code that sets it.** And the fix was not to give archers the flag: that flag means "mimes a sword blow", which is the thing that once made every skeleton swing a phantom sword and the user was furious about. A rig is geometry; an attack is behaviour; a *third* kind of attack needs its own pose, not somebody else's flag.
- ⛔ **Do not go looking for a reference number that does not exist, and do not invent one to fill the hole.** A mob's bow draw time is not in Bedrock's data: no mob has a `charge_*` field, and a skeleton's drawn bow is a **render-controller state gated on `query.has_target`**, not a behaviour timer — it raises the bow when it sees you and the interval is a reload happening behind it. Two agents nearly built a wind-up that the reference has no trace of. **When a number cannot be found, say it is ours in the comment rather than dressing a guess up as the reference's.**
- ⛔ **The same component's fields can be in different units from its neighbours, and "seconds" is not the safe assumption either way.** Everything in `minecraft:behavior.ranged_attack` is in **seconds**, while `mob_arrow`'s power beside it is **blocks per tick**. Our archer's interval was in seconds and correct as a unit and still **three times too fast** as a value — 1.0 against the reference's 3.0. A right unit is not a right number.
- ⛔⛔ **Running two agents at once was tried on 2026-08-11 and is now banned.** Both windows were handed the same brief, so both claimed and both wrote `Creature.cpp` — and the thing that caught it was **comparing the file's `LastWriteTime` against the clock**, not any of the machinery built to prevent exactly that. A message log, a status board of file claims and a script that announced unread messages from the build and every script: all of it deleted, on the user's instruction — _"remove the multi-agent thing it's not working out man."_ Two things survive it and are worth keeping. **`grep_search` returned a stale index** and showed none of the other window's work, which is why the timestamp was the only honest signal — **use `Select-String` when the answer matters**. And nothing was lost only because the edit tool patches the file on disk rather than writing back a stale copy, which is luck rather than design. **Research subagents are a different thing and are unaffected**: they read rather than write, and they are still wanted.
- **Where a hand is must have exactly one owner, and this is the one place that rule nearly broke.** The mesh hangs a bow on the hand and the behaviour looses an arrow from it. Derived separately they drift, and the arrow starts coming out of a shoulder — with nothing about the shot looking wrong enough to notice. `bipedHandPoint` reproduces `legBox`'s own pivot rule and **both** read it. The guard is a `static_assert` that every species with `shootsArrows` is the skeleton rig, because the launch point is derived from that rig's limb width.

- **`Get-LocalGroupMember` can silently return nothing for a non-admin caller**, which makes an "is this user an admin?" check produce a false negative for the wrong reason. `net localgroup Administrators` works from a standard account and gives the real answer.
- **The reference-art boundary is mechanical, not remembered, and the incident behind it is worth knowing.** The rule: **reference art may be measured from, must never live under `assets/`, and must never ship in a release** — and every staging tool *refuses* to write under `assets/` so it cannot be forgotten. It exists because a Minecraft asset mirror was once dropped into `assets/textures/blocks/`, and because the build copies `assets/` wholesale, that silently put 925 of Mojang's PNGs into the output folder. Reference material lives in `reference/`, gitignored and outside `assets/`.
- **A face mapping that is wrong stays invisible until an asset disagrees with itself.** Creature boxes read rectangles out of an unwrapped skin net, and the quarter-turn mapping for a lying body had world top and bottom swapped. On a sheep that is undetectable — its belly and back are both plain fleece — and it surfaced only on a cow, which has a pink underside, painted across its spine. The same shape of bug put a face on all six sides of every head for a whole milestone. **When a mapping is wrong, look for the first asset where the two sides disagree; until then it looks perfect.**
- **Passing every metric is not the same as looking right, and this was measured rather than supposed.** A deliberately flattened pig scored **correlation 0.76 and mask agreement 79.1** against the reference - *beating* the properly modelled version's 0.66 and 72.7. Four faults that were obvious on sight - a slab body, a zip up the spine, a halo on the face, panel borders on every rect - passed every check that existed at the time, and the user found each in seconds. **The table catches gross deviation; the eye is the gate.** `tools/compare-skin.ps1` now prints that warning above every run.
- **Shading must follow the form, never the rectangle.** Darkening the rim of each net rect looked like a cheap way to suggest roundness. On the model those rims meet, so every face gains a dark frame and the animal reads as a grid of tiles - the user called it a zipper. The reference vignettes across the *whole body*, indifferent to where one net rect ends and the next begins.
- **Put invented detail on a face the player cannot see, or do not invent it.** A seam painted down the pig's back rect was in view from every angle and read as a zip. The reference carries the same seam on its *belly* - the net's front rect, which the quarter turn puts underneath. Before adding detail, check which world face the rect actually becomes.
- **Identify an unknown texture region by measuring its colour, not by reasoning about what it ought to be.** Three unexplained nets in the cow sheet were settled in one command by averaging their pixels: pink was the udder, grey the horns, and a brown one was neither. The guess had been that brown net, which put a slab of hide through the animal's back.
- **A refactor from index to offset is exactly where silent breakage hides.** Skin slots were addressed as "skin 1" and became a texel row offset, because a cow's net is 64 rows where a sheep's is 32. Both are floats, so every remaining `1` compiled cleanly and meant *one texel down* rather than *thirty-two*. Nothing warned. **After changing what a parameter means, grep for the old literals rather than trusting the build.**
- **The biggest box is not necessarily the head.** The goat's 11×14×11 net sits where a head belongs and is the largest piece on the sheet; it is the woolly neck. What settled it was two measurements, not reasoning: its front rect is featureless white, and the eyes are on the *side* rects of a different, much narrower net — at `(37,58)` and `(55,58)`, which mirror exactly about that box's 10-texel depth. **Find the eyes before deciding what anything else is.**
- **A correct net does not make a correct animal; posture does.** Every one of the rabbit's box nets was measured right and it still rendered as a squat brick with its head buried in its chest, because every box was level. The user sent the reference render back with "shaped wrong" and they were right. Nothing about the dimensions changed — three tilts fixed it: the body pitched so the haunches ride high and the head sits low and forward, the head tipped slightly up, the ears raked back. The same failure named the horse bug: with a level head, the pale muzzle sits *beside* the eye and reads as a cheek patch instead of a nose. **When a model looks wrong but every measurement checks out, the answer is the pose, not the numbers** — and an axis-aligned box system needs one optional rotation to have any pose at all.
- **Two species sharing a model is right; sharing a size is not.** Horse, mule and donkey are one net layout and one box list, which is correct and is how the original does it. They are *not* one size — the original scales the whole model to 0.92 and 0.87. Rendering all three identically is a bug the user spotted immediately ("i know that the donkeys tend to be smaller than horses"). The scale multiplies geometry and placement only and must never touch the UV rectangles.
- **A subject with 25 colours does not want flat fields *or* dithering, and getting that wrong looks like coloured paper.** The first draft of the nine skins authored one tone per face and measured 9–13 colours against references at 19–36. Every anatomical decision was right and it still looked like card. Almost all of a reference's colour count sits **one step either side of each face's base tone**, sprinkled — not spent on contrast. Three tools fixed it: weighted per-texel grain, a coverage *rate* so the grain perturbs a vignette instead of erasing it, and swatch-mixing to get the intermediate tones a five-tone coat cannot supply. See `TEXTURING.md` §4.
- **Standing a box net on end rotates its texture too, and for the rabbit that landed right rather than wrong.** Pitching the torso 85° nose-up puts the net's belly rect facing forward and its rump rect facing down, which is exactly how a rabbit sitting on its haunches is arranged — so check where each rect ends up before assuming a large rotation needs a re-authored net. The knock-on is that an upright animal is far taller than a level one built from the same boxes, so `modelScale` has to drop hard: the rabbit went 0.88 → 0.48 to stay rabbit-sized, and got the slimmer silhouette the user had asked for as a side effect.
- **Two nets of identical footprint are ambiguous by arithmetic and unambiguous by colour.** The frog's back and belly are both 7×9 and differ only in height, so reading them as head-and-body is arithmetically legal — and produces an animal twice as long as it is wide, which every measurement would still pass. Averaging the two side bands settled it in one command: patterned orange over flat pale tan is a back over a belly, so they **stack**. This is the third time colour has resolved what geometry could not (the cow's udder, the goat's ruff, now this), which is enough to call it the rule: **when two readings are equally consistent, sample the pixels rather than reasoning about anatomy.**
- **The whole "prove the model, then author the skin" sequence now has a written order, and following it took the wolf and frog to a correct render on the second attempt each.** `TEXTURING.md` "Reading a net you have never seen before" records it: alpha runs before islands, missing rects as placement clues, colour where arithmetic ties, a shared-extent scan before rendering, then two renders and the eye. The order matters because **every step's failure mode is invisible to the next one** — a wrong dimension, a wrong interpretation of correct dimensions, and a z-fighting shared extent all produce something that looks like a plausible animal in a still frame.
- **A missing top or bottom rect outranks any colour reading, and the frog proves it.** Its two body nets are both 7×9 and differ only in height, so §12's "sample the colour" was used to call (3,1) the back and (0,13) the belly. That was **backwards**, and it shipped a frog with a hole through its spine for two milestones. The decisive fact was in the alpha the whole time: **(3,1) has no top rect and (0,13) has no bottom rect, so they interlock and (0,13) must be the upper box.** Colour could not settle it because both *side* bands are patterned and neither says which way is up. An absent face is a statement about what covers it — read the absences first, and use colour only where every rect is present.
- **Read the whole model from the alpha before deciding what any one net is.** The polar bear's second-largest net is a 12×12×10 slab sitting exactly where a head would go — it is the **shoulder hump**, and the real head is a far smaller 7×7×7. Same trap as the goat's ruff, and the same tell: the piece that looks like a head leaves the actual head nowhere to sit. Its front legs are also 8 deep against the hind pair's 6, so building all four from one net would quietly lose the heavy front end that makes a bear a bear.
- **Size and z-fighting are different problems, and three rounds were spent trading one for the other.** The villager's crossed forearms meet the upper arms where the reference lines up *three* pairs of faces — undersides, fronts and backs. Minecraft never draws the inward side of a surface so it does not care; every creature quad of ours is double-sided, so all three fight. Each attempted fix resized the part — bigger bulged, equal flickered, smaller looked recessed — and **resizing cannot cure a coincident plane.** The answer is to match the cross-sections exactly and **offset the bridging box about a sixteenth of a texel along its own height and depth axes**, which separates the planes at a size difference of zero. `TEXTURING.md` §14.5b has the rule. **`growSide` was added for the same join**: `grow` swells every axis, so using it to reach toward a neighbour thickens the part as much as it lengthens it.
- **But "shares a rig" has to be proven over the *whole* sheet, and a truncated dump is worse than no dump.** The villager and zombie villager are identical row for row up to row 33 — head, nose, body, legs, every net — so a listing cut off at row 30 said "same model" with total confidence. They differ in exactly one dimension, visible in exactly four rows: the villager's arm side rows stop at 33 (a 4×8×4 upper arm plus a separate crossbar for the folded pose) and the zombie villager's run to 37 (a 4×12×4 zombie-length arm, no crossbar). The zombie villager shipped with arms a third too short, and **the user caught it by eye while apologising for not knowing the game** — which is the whole argument for handing them the build. Two rules: never `Select-Object -First` a run dump you are deriving from, and when two sheets look identical, the interesting difference is wherever one of them *stops*.
- **A whole-sheet alpha diff produces false negatives on rig sharing, so it is a hint and never a verdict.** The Blackbone (wither skeleton), stray and bogged all report as "differing" from the skeleton because their interior pixels differ — ribcage holes, a skull's eye sockets, the bogged's mushrooms. Their **net boundaries are identical**, which is the only thing that decides the model. Compare run *widths at the net edges*, not opaque pixel counts.
- **A face mapping can be wrong for thirty species and invisible in every one of them.** `uprightBox` mapped its **top and bottom rects backwards front-to-back** from the day it was written. A box net folds so the top rect's *last* row meets the front face, and the code had the first row there. Nothing caught it because every model up to that point had top and bottom rects that are near enough symmetric — a sheep's crown, a pig's back, a villager's scalp. It surfaced the moment a model arrived with a **stripe painted down the front of its back**, which rendered at the rear; the user spotted it in one screenshot. **When a shared mapping has never been exercised asymmetrically, it has never been tested** — and the fix is to the one function, not to the model that exposed it.
- **Do not build a mob's model and its skin in the same pass, and do not assume a species is wanted just because it was asked for.** Ten mobs were added in one session and one of them — the hoglin — was removed entirely a day later, model, nets, docs and all. That cost nothing, because every one of them was a net table plus a species row plus a rig reused from something else. **The cheapness is the point:** a species that is one table row is a species you can delete. The moment a mob needs bespoke movement code, that stops being true, so keep new creatures on existing rigs unless the reference genuinely demands otherwise.

- **"Which mobs have arms" is not the same question as "which mobs punch", and shipping the first cost a correction.** The attack swing was rolled out by *rig*, so every skeleton on the biped rig mimed a sword blow. They are **archers** — the reference gives them `ranged_attack` at priority 0 and only drops one to melee when it has no bow — and the user's objection was immediate: _"remove the skeleton's arms moving, they use bow and arrow not melee attack, think about the game properly."_ They were right, and the fix is a species field rather than a rig flag, because the Blackbone shares that exact rig and *does* swing: it carries a stone sword. **A rig is geometry and an attack is behaviour; the moment an animation is chosen by the first, it will be wrong for whichever species shares a body and not a job.**
- **A refactor whose acceptance test is "nothing changed" wants a mechanism that *cannot* change the untouched cases, not merely care.** M20c had to move retaliation, the grudge, `huntsBelowLight` and herd alerting out of branches and into table rows without altering any of them, and rendering a head turn meant editing `buildMesh` — a thousand lines of art the user had already signed off. The trick that made it safe was **swapping two local vectors rather than touching any box builder**: `forward` and `side` became mutable, `headParts(true/false)` points them at the head's yaw, and every existing `place`/`uprightBox`/`lyingBox` call picks the change up for free. A species that never calls it emits **byte-identical geometry**, so the roster could be converted one animal at a time with no possibility of regressing the rest. Reach for "the untouched path is provably unchanged" before reaching for care.
- **A creature's facial features are placed against a *measured* landmark, not by eye, and the llama proves why.** Its muzzle sat centred at 2.02 against a neck box whose top is 2.0125 — so it covered the face entirely, and the user's report was that "the nose is wayyy above its eyes". The fix came from two numbers off the texture rather than from nudging: the eyes are painted on the neck net's **front rect at rows 1-2 below its top**, and the reference muzzle spans **rows 2-6**, so the muzzle's top edge meets the underside of the eyes and everything follows. **When a face looks wrong, find where the eyes are painted on the net and place everything else relative to that** - it is one command and it turns a guess into arithmetic.
- **An overlay is not a skin, and the machinery for one already existed.** The charged Bramble's energy shell needed no shader work, no new texture layer and no tint: creature skins are *already* alpha-tested cutouts, so a mostly-transparent overlay net drawn as a second, slightly larger shell just works. The sheep's fleece has done exactly this since M19. **Before adding a rendering path, check whether an existing asset arrangement already produces the effect.**

- **A refactor that must not change 36 things is safe only if the untouched path is *provably* unchanged, and "at zero it reduces to the old call" is the cheapest form of that proof.** Converting every limb from a translated box to a joint rotation touched a thousand lines of signed-off model work. What made it survivable was that `legBox` is **additive**: `uprightBox` kept its exact signature and maths, and `legBox` at an angle of zero emits precisely the box `uprightBox` would have. So an unconverted species is byte-identical by construction rather than by care, and the roster converts one animal at a time. Same property that let `headParts()` roll out at M20c. Reach for it before reaching for diligence.
- **Two boxes can be the right sizes, the right UVs and in each other's places, and every metric still passes.** The camel's `7x8x19` is its *neck* - a long low bar running forward from the chest - and its `7x14x7` is the *head* standing on that bar's far end. They shipped swapped, which put the tall box back at the shoulder where it read as a slab bolted onto the animal; the user's words were "its neck is an extra block which i don't think should be there". Nothing in `TEXTURING.md` §13 catches this, because the net table records sizes and origins and both were correct. **`Mojang/bedrock-samples` settles it in one fetch** - `resource_pack/models/entity/<mob>.geo.json` gives the cube origins, so *where* each box goes is arithmetic rather than judgement. Fetch it before deriving placement by eye, not after a bug report.
- **The internet is a resource; working numbers out from first principles when someone has already published them is wasted time.** Direct user instruction, 2026-08-04: _"instead of working out numbers, remember the internet is a resource? search it up and fix it."_ I had invented plausible ore rarities and drop counts by reasoning about what felt balanced. The wiki has them exactly — copper drops 2–5, redstone 4–5, lapis 4–9, everything else 1 — along with the tier each ore demands. **The same instruction caught a second error in the same session:** reach had been 12 m since M7, which is Bedrock's *touch creative* figure. The mouse values are 5 m for blocks and 3 m for entities. Nobody would ever have found that by deriving; it took one page fetch. **Fetch before deriving, and when a plausible number has no source, say so rather than letting it settle.**
- **Two boxes that merely *touch* z-fight exactly as overlapping ones do, and §14.5's scan does not catch it.** That scan looks for shared extents among *overlapping* boxes. A turtle's head resting on the top plane of its shell, or a dolphin's head meeting its body at identical 8×7 cross-sections, overlaps in nothing at all and still puts two double-sided quads on one plane. Same cure as §14.5b: overlap them slightly with different `grow` values rather than letting the faces meet. **The general form of this rule is now settled — a coincident plane is a coincident plane however the boxes arrived at it.**
- **The "reduces to the old behaviour at its default" property has now made three risky rollouts safe, and it should be reached for before diligence.** `legBox` at angle zero emits exactly `uprightBox`'s box; `beginHead` at rest reduces to the old expression; and `walkTo` with no route steers with the identical fan it replaced. In each case the untouched cases are unchanged **by construction** rather than by care, which is what allows a signed-off roster of thirty-six models or a four-milestone-old movement system to be converted one piece at a time.
- **A whole-sheet alpha diff is a hint about rig sharing; the geometry file is the verdict.** `tools/compare-nets.ps1` reports the skeleton horse as differing from the horse on **51 of 64 rows** and they share the rig exactly — the differences are skeletal holes in the artwork, not net boundaries. `Mojang/bedrock-samples`' `<mob>.entity.json` names the geometry outright and settles it in one fetch: the zombified piglin and the piglin brute are both `geometry.piglin`, the trader llama is `geometry.llama.v1.8` plus a decor overlay. **Fetch the entity file before deriving anything from pixels.**
- **A mob's identifying feature is not always in its skin.** The mooshroom's alpha matches the cow's on every row, and `geometry.mooshroom.v2` has no mushroom cubes at all — they are separate **block** models placed by a render controller. So a mushroom cow built from the skin alone is a red cow, and shipping one would be exactly the "compiled and looked plausible but was subtly wrong" failure this file is full of. Check the geometry for what the *name* promises before assuming a skin swap covers it.
- **A pair of limbs shares one net, so drawing both sides identically puts the artwork outward on one and inward on the other.** Our `+side` face reads the rect at `u` and the `-side` face the one at `u + netD + netW`, which means a limb placed at a negative side offset shows the *far* flap outward. This was wrong for every quadruped from the beginning and stayed invisible for thirty-six species, because leg textures are almost always near-symmetric — until the skeleton horse, which paints bone on the first flap and leaves the second bare, so one flank wore its bones on the inside. The reference's answer is a `mirror` flag and so is ours. **The remaining models are still unmirrored: the same latent bug is waiting for the next asymmetric skin.**
- **A coincident plane can hide for the entire roster and then be exposed by transparency rather than by geometry.** Every quadruped here puts a leg's top face on exactly the body's underside — the horse's are both `0.6875` to the last digit — and it never showed, because both surfaces are opaque and much the same colour so whichever quad won looked identical. The **skeleton** horse's underside is barely a third painted, so the fight became visible *through the gaps*. **Two boxes that merely touch z-fight exactly as overlapping ones do, and neither §14.5's shared-extent scan nor a still screenshot of an opaque animal will find it.** The cure is the usual one: overlap by a few thousandths, never touch. Worth knowing that a `grow` cancels out of `legBox`'s `hang`, so it moves the extents without moving the pivot.

## Animation and rigging

- **The convenient upload helper was the bottleneck, and it was written that way on purpose at M9.** `uploadBufferData` allocates a staging buffer, submits, and calls `vkQueueWaitIdle` — per buffer. That was the right call when one mesh existed; by M12 it meant 726 full GPU stalls at startup and two per block break, and it had quietly become the largest cost in the frame. **A helper documented as "fine at load time and nowhere else" will end up everywhere unless something forces the issue.** Batching into one arena, one submit and one fence took it from 186 ms to 33 ms. Check whether the slow-but-simple path from an early milestone is still confined to where it was meant to be.
- **The same bounce came back for dropped items, from a different mistake with the same shape.** Drops were rested a small `kRestHeight` *above* the surface to make them hover. That leaves a gap they immediately fall through, and landing pushes them back up — a loop again. **Anything that rests on a surface must rest exactly on it**; visual hover belongs in the animation, not the physics. The related trap is the ground probe: `floor(bottom)` points at the *air* cell whenever the object sits exactly on a boundary, so it has to probe slightly below.
- **A suppression flag that is only set on the frame an action fires does not suppress anything afterwards.** `struckCreature` was true only on the frame a swing landed, but the swing then locked out for 0.45 s — during which the dig branch saw `false` and happily mined the block behind the animal. The question "is a creature in the way?" has to be asked **every frame, independently of whether the swing is ready**, because it is a fact about the world and not an outcome of the action. Splitting the ray query out of `strike` made the two questions separate.
- **One gait for every species makes a camel mince and a chicken plod.** The leg cycle was a single hardcoded `gait += distance * 6` with one amplitude. How fast the legs cycle per metre travelled is a property of the animal, not of the engine, and it belongs in the species row beside its speed. A rabbit needed more than a number: it *hops*, so it raises the whole model on an arc instead of swinging legs — which means the lift has to reach every box, not just the ones that look like legs.
- **The zombie's arm swing and the humanoid's run in opposite directions, and it is not a sign slip.** `animation.zombie.attack_bare_hand` is `sin(tπ)·1.2 − sin((1−(1−t)²)π)·0.4` **added** to an arms-out pitch, so arms already held in front chop *down*; `animation.humanoid.attack.rotations` is `sin((1−(1−t)³)π)` **subtracted** from a hanging pitch, so a weapon arm swings *up*. Fetching `resource_pack/animations/zombie.animation.json` settled it in one call, and the same file confirmed the idle sway already in the code — its `2.865` and `103.13` are exactly our 0.05 rad on a 3.5 s period. **The shipped animation also uses a flat −90° base rather than the −120°/−80° aggressive-versus-calm split I recalled**, so the arms are *not* raised on sighting you. That difference is the whole argument for fetching rather than recalling.
- **A curve that is zero at both ends needs no state flag.** Both swing curves return exactly zero at phase 0 and phase 1, so "is this creature swinging" is already answered by a timer that has run out — no branch to get wrong, and no pose to reset.
- **Rotating a limb about its own centre swings the root into the body.** The chicken's wings are hinged at the shoulder, not through the middle, which is one extra line: place the box's centre by rotating its offset *from the pivot* rather than passing the resting centre. Centre-rotation is fine for something small and symmetric and obviously wrong for anything that hangs off a body.

- **"The top of the limb does not move" is wrong, and it is the trap that opens a wedge at the hip.** Only the pivot *point* is fixed; the top **face** tilts, and its corners swing by half the in-plane extent times `sin(angle)`. The reference's answer is an **overhang** - a biped arm pivots 2 texels below its top so that much box stays buried in the torso through the whole swing (`ANIMATION.md` §1.2 names it explicitly). Derive it from the box (`min(netW, netD)/2` reproduces both the arm and the chicken's wing); never author it. **The hip is where a rotation's sweep concentrates**, so §14.5's shared-extent check belongs there first, and at the swing extremes rather than at rest.
- **The spider's walk is worth copying exactly rather than approximating with one sine.** Ours gave alternate pairs opposite phases off a single shared sine, so the legs rowed in two groups. `animation.spider.walk` uses **a quarter turn of phase per pair**, a fore-and-aft sweep at **twice** the rate of the lift, and **absolute values on both**, so a leg only ever moves to one side of its rest pose. That last part is what keeps the travel small. And the thing that stops adjacent tips colliding is not the sweep but the **lift**: two neighbours pass within half a degree of each other in yaw, and are several texels apart vertically exactly when they do. **Java takes the cosine signed and Bedrock does not** - a real edition difference, in a formula that otherwise matches term for term.
- **A limb is not always a leg, and "pivot at the top" is only one of two locations.** The spider's legs run *sideways* and hinge at their **inner end, mid-height**, so their swing is a **yaw** rather than a tilt - which is the head-turn mechanism, not the leg one. Treating them as an exception to skip was wrong; §1.2's pivot table lists them. The silverfish is the genuine exception, because it has no limb at all and its wriggle is a sideways *distance*.
- **Changing what a column *means* is the same hazard as widening what a value can be, and the compiler cannot see either.** `gaitSwing` went from metres of foot travel to radians of joint rotation. Two species were using it as a distance for something that is not a leg - the silverfish's wriggle and the spider's scuttle - and both would have compiled and rendered nonsense. Grep for every reader before repurposing a field, exactly as when widening a block's shape.
- **The reference's constants are calibrated to the reference's speeds.** Minecraft saturates its limb amplitude at 5 m/s because its mobs move at 4 m/s; ours walk at 1.6. Copying the 5 verbatim would have left the whole roster barely lifting a foot. Saturating against each species' **own `runSpeed`** lands the walk/run ratio at a consistent ~0.45 across all 36 rows for free, because those two numbers were tuned together. **Ask what a reference constant is a fraction *of* before porting it.**
- **Per-species rates are a decision, not an accident, and the reference having one number is not an argument.** §2.2 calibrates Minecraft at a single 2.665 rad/m; the user's standing call is that per-species rate stays, because one gait for everything makes a camel mince and a chicken plod. Amplitude got the same treatment - the roster's hand-tuned spread was carried across rather than flattened to the reference's single value. **Say which knobs you are touching before touching them.**
- **A rotation about the wrong origin looks like the part is *sliding*, not turning, and it took a playtest to name it.** The head turn shipped at M20c as a whole-frame axis swap, which rotates about the **creature's own origin** - so every head placed forward of centre *orbited*: it swung out sideways and lunged forward as it looked, and dragged the neck along with it. The user's words were "sticking the head out and then looking - it's creepy". The maths was not wrong so much as pivotless, and it was invisible for as long as nobody watched a creature turn its head while standing still. **Any "rotate this group" needs its pivot named at the same moment the rotation is written**, or the origin silently becomes the pivot. The fix also delivered head *pitch* for free, because a pivot was the only thing pitch had been waiting for.
- **Splitting one boolean-flag function into two named ones turns a silent omission into a compile error.** `headParts(bool)` became `beginHead(neckForward, neckUp)` and `endHead()`. There is no default pivot, so a head group that forgets one does not build - which matters at 23 call sites where the wrong answer is a head hovering in the wrong place rather than anything that throws. Prefer this to a defaulted parameter whenever the default would be *wrong* rather than merely unset.
- **Where the reference parents a part is a decision we are allowed to overrule, and here we did.** Mojang keep the camel's neck inside the head bone, so a camel there swings its whole neck to look. Over a 19-texel neck at our 52° clamp that is a lunge, and the eyes are on the head box anyway - so ours leaves the neck with the body. Direct user instruction: _"only the head should be turning on the camel."_ Written at the call site so a later session does not "correct" it back to the reference.

- **A hand-tuned offset outlives the mechanism it was tuned for, and the compiler cannot see it.** The zombie and husk arms sat at a rest height of 1.42 - a number that made sense when a limb was a *translated* box, and became wrong the moment `legBox` started **deriving the joint from the rest centre**. It put the shoulder at 1.68 against a head spanning 1.5 to 2.0, so the arms hinged from the ears. Every other limb on the roster converted safely because `legBox` at angle zero reproduces `uprightBox` exactly - but these two are the only limbs posed at 90°, where the pivot is the whole of what the pose depends on. **After changing what a value is measured from, the rows to re-check are the ones that were compensating for the old measurement**, and they are exactly the rows that look most deliberately tuned.
- **A symmetric sine is a walk cycle, and using one for anything that is not walking will be spotted immediately.** The squid's first tentacle animation was `sin(t)`, and the user's word for it was that the squid was *walking*. The reference's curve is `sin(f²π)` over the first half of the cycle: it flares **slowly** and snaps shut **fast**, and the asymmetry is the entire read. Crucially the **thrust is applied during the snap only**, from the same number that drives the pose, so the push and the picture cannot drift apart — and a consequence falls out for free that would have been hard to author, namely that a squid must turn about before it can go the other way, because it does not steer while it is pushing.
- **A box whose rect is entirely transparent is not free — it is noise.** The skeleton horse paints nothing at all in the tail rect and only two one-pixel columns in the mane, so those boxes contributed stray slivers rather than a tail. Measure coverage per rect when a skin is mostly holes; "it renders nothing" and "it renders almost nothing" look very different on screen.

## Vulkan and the renderer

- ⛔⛔ **The resource Vulkan is short of is the *number* of allocations, not the bytes.** One `vkAllocateMemory` per buffer, two buffers per chunk mesh and up to two meshes per chunk reached about **three thousand** live allocations at render distance 12 and went past the spec floor of **4096** at render distance 16. The failure mode is `VK_ERROR_TOO_MANY_OBJECTS` from a call nobody checks, i.e. the world silently stops appearing, on a device that is behaving perfectly correctly. A first-fit sub-allocator over 32 MiB blocks took it to **26**. Three things about writing one here are worth keeping: **sub-allocate buffers only and never images**, which sidesteps `bufferImageGranularity` entirely; **map a host-visible block once for its whole life**, because Vulkan forbids mapping one `VkDeviceMemory` twice and a buffer's destructor must therefore never unmap; and **release the pool from `~VulkanContext` before `vkDestroyDevice`**, because nothing else is alive at the right moment.
- **A counter that is only warned about cannot be watched.** The allocation count had a warning at three quarters of the device limit and no way to see the current value, so "how close are we?" was unanswerable without a debugger. It is now in `RenderStats`, on the F5 overlay and in the per-second log line. **If a number can end a run, print it every second.**

- ⛔⛔ **A normal recovered from the triangle always faces the camera, so a two-sided surface lit from it follows you around.** Leaves, glass and every other cutout are drawn with both windings from one set of vertices, so the mesher gave them an "unaligned" normal code and the shader derived one with `dFdx`/`dFdy`. That derivation is exact - and for a quad whose *far* side is the one being drawn it returns the near side's normal, which is to say the camera's. Every leaf on every tree was therefore lit as though turned to face you, and the sun's highlight slid round the trunk as you walked. The user reported it as leaves glowing where the sun could not reach. **Emit the real face normal and flip it toward the eye in the shader**: for a two-sided surface that is what you wanted, and for ordinary geometry the far side is culled so it changes nothing.
- ⛔ **Ambient light and direct light need different gates, and using one for both means only the outer shell of anything is ever lit.** The sun's contribution was gated on baked sky light, which falls one level per block through a canopy - so the inside of a tree was a dark mass however many holes the light could actually get through. The shadow map *can* see a hole in a leaf and baked sky light cannot. Ambient now scales straight off sky light (so a cave is dark) while the directional half is gated by the shadow map, with sky light only asked whether the cell is sealed inside rock.
- ⛔ **`gl_FragDepth` clamped to exactly 1.0 loses the `LESS` test against a cleared depth buffer.** A cloud deck beyond the far plane wrote 1.0, the sky's cleared depth was 1.0, and the entire horizon silently vanished. `min(depth, 0.999999)` still loses to any terrain in front of it, which is the only ordering something above the world can need.
- **A one-tap directional derivative measures nothing when the tap is much shorter than the feature.** The cheap substitute for a light march - `(density(p) - density(p + k*L)) / k` - is a real technique and it returned zero everywhere for a cloud field whose features were a hundred blocks across and a tap three blocks long. Either scale the tap to the feature or pay for three real taps. **Check a sampling distance against the wavelength of what it is sampling**, which is the same rule as a probe window against a noise field.
- **Dither is right for a smooth field and wrong for a hard-edged one.** Offsetting each pixel's ray start is what hides banding in a volumetric march at a low step count. Applied to a binary density it frays every silhouette, which for blocky clouds is the one thing they exist to have.
- **`flat` is a reserved word in GLSL** — it is an interpolation qualifier. Naming a local `flat` fails to compile with a message that points at the line after it. So are `sample`, `smooth`, `noperspective`, `filter` and `buffer`.
- ⛔⛔ **A standard approximation written from memory came out inverted, and the shape of the code hid it.** The air-mass term in the new sky reported *less* air toward the horizon than straight up — the exact opposite of the truth — and it divided by zero below the horizon, which painted a black band precisely where the loaded chunks stop, in the one place the fog exists to hide. It went unnoticed because the model is consumed as a **ratio**, and a ratio still looks plausible when both halves are wrong. **Print the curve at a few known points before trusting a formula you did not derive on the spot**: air mass is 1 at the zenith and about 40 at the horizon, and three printed rows would have caught it instantly. Ten seconds of arithmetic against six turns of it being on screen.
- **Nothing below the horizon is sky, so do not ask a sky model about it.** Every closed form for atmospheric depth is undefined or negative down there. Answer with the horizon in the same direction, which is exactly the fog colour and exactly what the world fades into.
- ⛔⛔ **A per-block flag cannot describe a plant that is taller than one block.** The wind's sway was a single bit meaning "this vertex moves", set on the top half of each *quad* — so every block of a sugar cane bent its own top against its own bottom and the stalk came apart at every boundary. The player reported it as each block blowing its own way. The fix is to store **a height above the plant's own root**, so the value is continuous across a block edge. **When something is made of several blocks, ask whether each block can answer the question alone** — here it could not, and the flag had looked perfectly correct for the eighteen months everything that swayed was one block tall.
- ⛔ **The same bug had a second, independent cause in the shader**: the sway phase included `position.y`, so even a continuous height field would have twisted a tall stalk along its length. The comment above it already warned that the phase must turn slowly across space to stop a plant shearing — and then included the one axis a plant is long in. **Two causes with one symptom is normal; fixing the first and shipping is what makes it look unfixed.**
- ⛔⛔ **Depth values at the far end of the buffer are crowded, so ordering two distant things by a hand-picked constant is a race, not an ordering.** Clouds clamped their depth to `0.999999` and the sun sat at 0.92 of the far plane, which works out to about `0.999992` — so **every cloud further away than the sun lost the depth test and the sun punched through**, which near the horizon is most of them. A comment two lines up said clouds were drawn after the bodies "so a cloud passing in front of one occludes it", which was true of the draw order and false of the result. Fix: the renderer computes the bodies' real depth from the distance the game draws them at and publishes it, so the clamp cannot drift from the thing it is ordering against.
- **Scaling the resolution only helps a frame that is pixel-bound, and ours is not.** Render scale went in expecting a large win and measured 2.42 ms to 2.11 ms from full resolution to half — because the shadow pass is a fixed size and does not scale at all, and the geometry pass is bound by 2.6 M triangles. **Prove the mechanism separately from the benefit**: a probe confirmed a 1280x720 window drawing a 640x360 world, so the small number is the truth about the frame rather than a broken setting. Had I only looked at the timing I would have gone hunting for a bug that was not there.
- ⛔⛔ **A tilt added for lighting silently broke an orientation requirement, and the two look completely unrelated.** The sun's arc was pushed 15.6° out of the east-overhead-west plane so it would miss the zenith and keep lighting vertical faces at noon. **Four separate attempts to stop the sun appearing to rotate then failed**, because a square referenced to the world rolls by however far the arc misses the camera's own vertical plane - measured 36° off upright by mid-morning, which is a diamond. No choice of frame can fix that while the arc is tilted, and nothing in the function that builds the quad hints at why. **When a requirement about how something looks keeps failing, question what it is attached to, not only the code that draws it**; the fix was one constant in a different function. The geometry was then settled in a throwaway script that replayed the shipped expressions over every case the player had named, in seconds and without running the game - the same trick as compositing the font atlas offline.

- ⛔⛔ **A line moved into a new shader can be wrong because the old shader *later overwrote it*.** The G-buffer pass kept `albedo = texel.rgb * fragColor.rgb` when it was split out of the forward shader. For world geometry those three channels are **sky light, block light and face shade**, not a tint - so red was scaled by sky light and **green by block light, which is zero away from a torch**. Green was crushed, red and blue survived, and the entire world went violet, worst at night. The forward shader had computed the identical expression for milestones and then thrown it away inside its `if (worldLit)` branch, so it had never mattered there. **When you lift a line out of a working shader, read the rest of the old one before trusting it.**
- ⛔ **Front-face culling is the textbook cure for shadow acne and it is wrong for voxel geometry.** It works by storing the *far* surface of a closed object, which pushes the error inside where nothing can see it. A voxel mesh is a **hollow shell** - the mesher never emits the face between two solid blocks - so a hill has no far side, and front-culling leaves flat ground casting no shadow whatsoever. Cull nothing, and pay for it with a slope-scaled depth bias plus a normal offset. The tell would have been catastrophic and obvious; the reasoning that leads there is entirely plausible, which is why it is written down.
- **A shadow cascade must be fitted to a sphere, not a box, and then snapped to whole texels.** A box fitted to a slice of the view frustum changes size as the camera turns, so the map's scale changes with it and every shadow edge in the world crawls while you look around. A bounding sphere is rotation-invariant. Snapping the light-space centre to texel multiples is the other half: without it the map slides a fraction of a texel every time the player moves, and the edges shimmer instead.
- **A descriptor that is declared must be in the right layout even when the shader never reaches it.** With shadows off, or at night, the shadow pass returns early - and the map then sat in `UNDEFINED` while the lighting pass's descriptor claimed `SHADER_READ_ONLY`. The shader's loop never executes at a cascade count of zero, but validation checks *static* use, not what runs. The skip path still emits the barrier.
- **Four shaders had hand-copied the same uniform block at three different lengths.** That is legal - a shorter declaration is still a prefix of the same buffer - which is exactly why it went unnoticed, and it is one careless insertion away from every shader after it reading the wrong bytes. One `frame.glsl`, included everywhere. **Anything declared identically in more than two shaders wants an include.**
- **A blended fragment still writes depth, so "nearest" is not the same as "on top".** [see the interface section - it has now cost four separate bugs]

- **A limit the specification only *guarantees* is not a limit the hardware *has*, and exceeding it excludes real machines silently until someone counts.** `MeshPushConstants` grew to 144 bytes - a `mat4` plus five `vec4` - past the 128 Vulkan guarantees. That was a known, commented decision with a startup check behind it, so it looked handled. Measuring it against the Vulkan hardware database said otherwise: **13.6% of Windows devices report exactly 128**, and the group is not the one you would guess - **AMD's own Windows driver reported 128 for years** before raising it to 256, so a modern Radeon on an older driver clears our Vulkan 1.3 gate and still cannot run. Two lessons. **Check a limit you exceed against real-world distribution, not against the spec minimum**, because "most hardware has more" is a claim with a number attached and the number may be 86%. And **test the limit during device *selection*, not after it** - ours was checked in `Renderer` once a device had already been chosen, so on a two-GPU machine it would pick the discrete card, fail, and never try the integrated one that would have worked.
- **Backface culling errors are completely silent, and a hand derivation of the winding was wrong.** With `VK_CULL_MODE_BACK_BIT`, getting `frontFace` backwards keeps the far half of every closed object and discards the near half — boxes render as hollow shells you are looking *into*. Validation layers say nothing, the frame rate is unchanged, and every individual triangle is drawn correctly. Only a human looking at a closed shape catches it. On 2026-07-30 the value was derived by hand — twice, through the Vulkan signed-area formula — reasoning that flipping Y in the projection reverses screen winding, concluding `VK_FRONT_FACE_CLOCKWISE`. **That was wrong; the correct value with this codebase's CCW-from-outside geometry and Y-flipped projection is `VK_FRONT_FACE_COUNTER_CLOCKWISE`,** established by looking at the screen. The derivation's flaw was never found. **Do not re-derive this and "fix" it — verify against a closed box viewed from outside.**
- **Sharing vertices between cube faces destroys the edges that make it read as a cube.** The first cube used 8 shared corners; where three faces meet they were forced to share one colour, so the GPU blended smoothly across every edge and the result looked like a soft blob. Geometrically perfect, visually unreadable. The fix is **4 vertices per face, 24 per box** — which is what voxel meshing needs regardless, since normals and texture coordinates also differ per face at a shared corner. A vertex is position *plus everything that varies*; only the position is actually shared.
- **A Windows console window freezes the program the moment you click inside it.** Cost real confusion on 2026-07-30: the Vulkan SDK installer was reported as "a blank TUI" doing nothing. It was not broken — the window title had become `Select C:\...\vulkansdk-...exe`, and that `Select ` prefix is Windows' QuickEdit mode, entered by a single click in the console. In that mode the process blocks the next time it writes to stdout. **Diagnosis:** check `(Get-Process <name>).MainWindowTitle` for a `Select ` prefix and compare `.CPU` seconds against elapsed wall time — a process using 0.08 CPU-seconds over 90 seconds is parked, not working. **Fix:** press `Esc` or `Enter` in that window. Applies to any long-running console tool, not just installers.
- **`std::this_thread::sleep_for` is far too coarse for frame pacing on Windows.** The default system timer resolution is about 15.6 ms, so asking to sleep 8.3 ms for a 120 fps cap actually sleeps ~15.6 ms and lands near 64 fps. The naive implementation would have looked like a mysterious "my cap doesn't work" bug. `FrameLimiter` instead uses a Win32 **high-resolution waitable timer** (`CreateWaitableTimerExW` with `CREATE_WAITABLE_TIMER_HIGH_RESOLUTION`) and spins the final ~500 µs. `timeBeginPeriod(1)` was rejected because it changes timer resolution process-wide as a side effect. Measured result: a steady 120–121 fps against a 120 target. Reuse this pattern for any future precise wait; do not reach for `sleep_for`.
- **PowerShell prints a native program's stderr as a red `NativeCommandError`, which is not a failure.** `cl.exe`, `code --install-extension`, and `vulkaninfo` all write informational banners and warnings to stderr and still succeed. Check the exit code and the actual text before reporting a problem.
- **Opaque artwork cannot be made translucent by turning on blending.** The hotbar slots stayed solid black through a working alpha pipeline, because the sprite's interior pixels are opaque black in the source image — blending faithfully blended *black at full alpha*. The fix was not a shader or blend-state change but a geometry one: draw the frame as four edge strips and simply never draw the interior. **Check what the source pixels actually contain before debugging the blend path.**
- **A feature can be "finished" and never actually run, and only rendering it exposes that.** M11 was signed off with `RenderStats` reporting GPU time and draw calls. Both were dead: `m_frameDrawCalls` was incremented but never copied into `m_stats` or reset, and **no `vkCmdWriteTimestamp` was ever recorded at all**, so `m_timestampsPending` stayed false and the read path returned early forever. Everything compiled, validation was silent, and the console log never printed either figure. The bug surfaced the instant the numbers appeared on screen as `gpu 0.00 ms` and `draws 0`. **A statistic that is only ever stored is untested; display it or assert on it.**
- **Never benchmark threading on a Debug build.** M12 first measured as a *regression*: 813 ms single-threaded before, 1325 ms with eleven workers after. The same code in Release went 179 ms → 55 ms. MSVC's debug CRT serialises the heap, so eleven threads allocating mesh vectors spend their time queueing on one lock, and the debug numbers were not merely noisy but **non-monotonic** — four workers beat eleven. Two rounds of "optimisation" were spent chasing that phantom before building Release. Debug is for validation layers; Release is for numbers.
- **Blending is a draw-order problem, not a shader problem.** Water looked correct in isolation and wrong against terrain, because a translucent face blends with whatever is already in the framebuffer — and the seabed behind it had not been drawn yet. No amount of tuning alpha fixes that. Chunks now carry two meshes and every opaque one is drawn before any translucent one. **If transparency looks wrong, check what order it is drawn in before touching the blend state.**
- **A placeholder can quietly break a feature three milestones later, in a system it has no obvious connection to.** M14c multiplied every lit surface by the sun's ambient-plus-directional term. That is correct for sunlight and wrong for everything else, so a glowstone placed in a cave was multiplied by the daytime ambient of 0.22 and, on faces pointing away from the sun, lit nothing at all. The light system was working perfectly; the *shader* was overwriting its result. **Sky light and block light must stay separate all the way into the fragment shader** — they are packed into the vertex colour's red and green channels, which cost nothing because the shading was greyscale and blue was spare. When a lighting bug appears, check what the shader does to the value before assuming the propagation is wrong.
- **`discard` is not free of device features.** Compiled against Vulkan 1.3 it becomes `OpDemoteToHelperInvocation`, which validation rejects unless `shaderDemoteToHelperInvocation` is enabled. Release builds ran fine because validation is off there — which is exactly why the debug build has to be run before a milestone is called done, not just built.
- **Two boxes of the same width will z-fight wherever they overlap, and every creature quad is double-sided so nothing hides it.** The camel's eyes are painted across the two front corners of one box, and the box in front of it happens to be the same 7 texels across — so their side faces landed on exactly one plane, right over the eyes, and the winner flipped per pixel and per frame. The user described it as triangles forming over the eyes, which is the tell: z-fighting exposes a quad's diagonal, because the two triangles interpolate depth slightly differently. **Grow the box carrying the artwork rather than insetting the one in front** — insetting cascades, and shrinking the camel's face box moved its top and bottom to within a few thousandths of the ears and the muzzle, merely relocating the problem. The rule for this model system: overlapping boxes must differ in extent on every shared axis, and the one with the detail on it wins.
- **Convert the reference's per-tick probabilities, or the feature scales with frame rate.** Bedrock's `look_at_player` rolls 0.02 **per tick**, and a tick is a fixed twentieth of a second. Our tick is a frame, so used verbatim a chicken at 120 fps would glance six times as often as one at 20 — a bug that only appears on someone else's machine. The conversion is `0.02 / 0.05` as a per-second rate, the same family as the velocity ×20 and acceleration ×400 in `RESEARCH.md` §1.2.

- **Per-frame sampling of a per-tick decision needs a memory, not just a conversion.** The reference tests a creeper's line of sight twenty times a second; we test it every frame, so at 120 fps a fence post clipped for one frame is six times likelier to be caught, and every catch reverses the fuse. A single tick of grace (`kSightGrace`) restores the reference's sampling rate. Same family as converting `look_at_player`'s per-tick probability, and worth checking for wherever a boolean drives a countdown.

- **A tint is not fog, and the difference is the whole look.** Ours was a full-screen quad at 42% alpha: it announced the state and said nothing about distance, so an underwater scene had no depth at all. Real distance fog needed one varying (`gl_Position.w` is already the view depth under a perspective projection, so it is free), one push constant, and a `mix` - far less than the flat quad implied it would cost.
- **`MeshPushConstants` was 128 bytes for a while, which is Vulkan's guaranteed minimum, and that was too close.** A `mat4` plus four `vec4`s exactly filled it, so the next thing wanting a push constant had nowhere to go. **AMD really does expose only 128**, so it was never theoretical, and the Steam Deck is on the list of things this project wants to keep possible. M23a settled it by moving everything constant across a frame into a uniform buffer; the block is **80 bytes** now with a `static_assert` on it. **The rule that came out of it: a per-draw block holds things that differ per draw, and nothing else.**
- **A colour that is "the value from the reference, times something" is not that colour, and the user will notice before you do.** The underwater fog was quoted as `#1165b0` and then multiplied by the daylight term on the way to the shader - so it was only ever the stated colour at noon and drifted to a murky navy the rest of the day. The reference's fog data carries a flat constant with no light modulation at all; the multiply was invented, unfaithful *and* the thing that looked wrong. **When a value is taken from a source, ship it unmodified unless the source modifies it.**
- **A colour in shipped data is an *input* to the renderer, not a pixel, and the fog colour was wrong for a third time because of it.** With the daylight multiply gone, `#1165b0` straight out of `ocean_fog_setting.json` still read as "a weird blue" - because Minecraft blends that value with the water's own colour before anything reaches the screen, so the raw number never appears anywhere. **Measuring the user's own screenshots settled it in one command and the failure was a single channel**: fully-fogged ocean is `#146FFF` = rgb(20, 111, 255) - one exact colour across 24,644 sampled pixels - and a deeper scene `#1762CD` = rgb(23, 98, 205). Red and green matched the JSON almost exactly; **only blue was off, 176 against 205-255**, which is exactly what "not blue enough, looks grey" means. Ours is now the measured middle, `#1465E7`. **A constant lifted from data is not verified until something rendered from it has been compared against a picture** - and `SYSTEM_MEMORY.md` now carries a do-not-revert box, because "the JSON says 0x1165b0" is a very convincing-looking correction to make.

- ⛔ **A pass cannot read the image it is drawing into, and the cheapest way round it is a copy that carries two things at once.** Water needs the lit world both as a *colour* to reflect and as a *distance* to measure thickness against. The obvious build is a colour copy plus a depth copy, which is two images, two sets of layout transitions and a depth format to keep in step. Writing the eye distance into the scene colour's **alpha** in the lighting pass collapses all of it to one `vkCmdCopyImage`: nothing downstream reads alpha, and the forward pass overwrites it in the same frame. **Before adding a second resource, check whether a channel of the first one is already dead.**

- **A reflection can only show what was already drawn, so its limits are geometric and must be faded, not fixed.** Two of them, both real: anything off the edge of the screen was never rendered, so a screen-space reflection has to fade toward the border or it stops dead in a line; and anything drawn *after* the copy - here the sun, the moon and the clouds - simply is not in it. The fallback for both is the same sky gradient the sky pass writes, which is exactly why that function has one owner (`sky.glsl`) and both passes call it. **When two passes have to agree on a colour, the agreement has to be a shared function, not a shared constant.**

- **Fresnel is not a look to be tuned, and "the reflection is too weak" usually means you are standing at the wrong angle.** Water returns 2% looking straight down and nearly all of it a few degrees off the horizon; the fifth power is what produces that. The first test shot was aimed steeply downward, showed almost no sky, and looked like a failure - the same build seen along the surface was a mirror. **Judge an angle-dependent effect from the angle it is meant for before changing its maths.**

- **One flat opacity is why water read as tinted glass, not why the reflection was missing.** A lake bed ten metres down was exactly as legible as one an inch down. Beer's law through the thickness the view ray actually crosses - available for free once alpha carries a distance - is what separates a clear edge from a solid middle, and it did more for "looks like water" than the reflection did. **It must be skipped while submerged**, or the surface seen from below takes the sky's distance as an infinite thickness, goes opaque, and seals the player in.

- **Snapping shading to the texture's own grid is a one-line way to keep an effect in style.** One block face is one texture, so `floor(worldXZ * textureSize(blockTextures, 0).x)` is exactly the grid the world is drawn on - no new uniform, and it follows the art if the resolution ever changes. Computing the *view direction* from the snapped point as well as the normal is what makes the whole reflection step per texel rather than merely its shape.

- **A shadow map can only remove the sun, and that alone is not a shadow.** Cutting the directional half left shadows reading as pale patches because the surface was still receiving the entire sky. Taking a share of the **ambient** with it is where the depth comes from and is also the honest answer - a point the sun cannot see is cut off from much of the sky too. Gate it on sky light as well, or a cave gets darkened twice.

- ⛔ **Spare bits are not spare until the shader masks when it unpacks.** Ambient occlusion sat in bits 3-10 of the vertex surface word for four milestones and was read with a bare `>> 3`. The comment said "the rest spare"; it was not. The first flag added above it would have folded straight into the occlusion and lit every affected surface at eight times white - silently, because occlusion is a multiply and nothing validates it. **Any packed field that is not the top field must be masked, and a comment promising spare room is a claim to check rather than trust.**

- **Two shader stages that must agree about a shape need one file, and that file must not depend on anything else.** The vertex stage displaces the water surface and the fragment stage shades its slope; from different constants, the light runs down the wrong side of every swell - which nobody can name and everybody sees. `waves.glsl` returns the height and its analytic gradient together and includes no uniforms, so the vertex stage can take it without dragging in samplers it has no use for.

- **Displacing geometry is safe exactly when the displacement is a pure function of world XZ and only the right vertices get it.** Two vertices at the same XZ then move identically whatever face or chunk they came from, so nothing can open a seam. The mesher marks a fluid's top corners with a flag; a waterfall's foot, the seabed and everything below the waterline stay put. **Deriving "is this a top vertex" from the geometry instead does not work** - a source block's top sits exactly on the block boundary, so there is no fractional height to test for.

- ⛔ **A number ported from the reference is only as good as the unit it was measured in, and "per chunk" is a unit.** Lightning is one in a hundred thousand per loaded chunk per tick. Ported literally that would have been wrong twice over: our chunks are 32 blocks where the reference's columns are 16, and our render distance reaches three times further than the simulation radius it actually uses. Either mistake alone multiplies the strike rate tenfold. **Port the resulting rate, which is the thing that was observed, not the probability, which is an implementation detail of a different world size.**

- **Pixel width is a unit too.** Our rain draws at 16 texels to the block; the reference's draws at 64. Taking its drop *count* with our drop *width* filled the screen with white slabs, because every drop was four times fatter. The first tuning pass looked right on one quad and catastrophic on the six hundred that stack up along any horizontal view.

- **When a second uniform would say the same thing as one you already have, derive it.** Storm cloud darkness comes from the deck's own coverage, because an overcast sky is exactly what a dark deck means. A separate `stormAmount` could only ever drift out of step with the coverage the game is already animating.

- ⛔ **On a cutout path you cannot fade anything out.** A fragment is either fully present or thrown away, so an alpha ramp does nothing at all until it crosses the discard threshold and then blinks the whole thing out in one frame. Particles now **shrink** instead, which reads as the same effect and costs nothing. The tell is that the first version looked fine in a still and popped in motion.

- **A new effect that reuses an existing pipeline is worth a lot more than a tidier one that needs its own.** Particles are camera-facing quads carrying the block texture with light in the same three vertex channels world geometry uses, so they went down the existing lit path with no shader case anywhere and no pipeline, descriptor set or vertex format to maintain. Building them facing the camera on the CPU costs two cross products for the whole set - cheaper than a per-vertex billboard, and the camera is known at that moment anyway.

- ⛔ **Displacing vertices and greedily merging faces are incompatible, and the artefact is a straight line.** The mesher merges evenly-lit coplanar faces into one big quad; the wave then moves only that quad's four corners, so a sixteen-block water face becomes a flat tilted plane rather than a swell - and where it meets a differently-sized neighbour their shared edge disagrees about its own height. The player saw it immediately as "a cut in the water", and named the cause: "segmenting of water blocks". **Anything the vertex stage moves has to be emitted at the resolution the movement varies at.** Unit quads for water cost 0.4% more triangles and no measurable GPU time - the merge is worth far less than it looks on a surface this small a fraction of the world.

- ⛔ **A per-vertex animation phase that turns faster than about a radian per block tears the model apart.** Two corners of the same blade land on opposite parts of the cycle and move in opposite directions. The phase exists to stop a field moving in unison, which only needs it to differ over *several* blocks - a fifth of a radian per block does that and keeps each plant coherent.

- **Two systems driven by the same vector still need their signs checked against each other.** Rain and grass both read one wind vector, and the rain's shear sign was inverted - so the streaks leaned into the wind while the grass bent away from it, and the player reported it as the wind coming from two directions at once. Sharing the input guarantees they agree about *magnitude*, never about *direction*.

- ⛔ **Anything that displaces a surface has to leave it exactly flush where it meets something else, and both directions of error look like a hole.** The water wave lifted the waterline above the bank, which exposed the top of a water side face culled precisely because that bank was supposed to hide it - you saw straight through the seam. Offsetting the wave so it never dipped *below* the bank only moved the same fault to the other side, and the player reported it twice. The fix is to pin every surface vertex that touches anything other than water: **where a moving surface meets a static one, the vertices they share must not move at all.**

- ⛔ **Art converted from an additive blend needs an alpha floor, not a snap.** The sun and moon are drawn additively against black in the reference, so alpha is derived from brightness - but their halo never actually reaches black. Inside the cropped tile it bottomed out at **14**, and the snap-to-zero threshold was 10, so every single texel stayed slightly opaque and the whole tile showed as a faint square of dark blue behind the moon. A floor at 40 with the survivors rescaled to reach full opacity fixes it. **Measure the corner alpha of any sprite that is supposed to have transparent corners** - it takes one command and the artefact is invisible until it is on screen against the right background.

- ⛔ **"It looks like it is rotating" is a screen-space complaint, and four attempts went into it without the player being satisfied.** World up needed a threshold near the zenith and snapped mid-arc; the orbit axis turned steadily; world up without the threshold still read as tilted, because a flat panel away from the centre of the view is foreshortened along one diagonal and that is indistinguishable from a roll. Only a camera-spanned quad is upright on screen everywhere - and the player had already rejected that, because in *world* terms it is the panel swivelling. **The two requirements are in genuine tension and cannot both hold.** The lesson is not about billboards: **when a visual complaint has been misread twice, stop implementing and ask, with the trade-off written out.** Three rounds were spent guessing before that occurred to me, and by then the goodwill was gone.

- **A sky body drawn nearer than the world is drawn reads as an object in the landscape.** At 320 blocks with the fog ending at 384, the sun passed behind hills a few hundred metres off and faded into the murk as it set - reported as "sinking through the floor close to me". It now sits just inside the far plane, sized as a *fraction* of that distance so it cannot change size when the render distance does, and is exempt from distance fog - which would otherwise dissolve the one thing that is supposed to be infinitely far away. It keeps the fog that starts at the eye, so it still vanishes underwater.

- ⛔ **A procedural event hashed on position alone repeats in the same place for ever.** Rain rings were placed by hashing the tile, which gave each tile one fixed spot and one fixed moment - so the same four places on a lake were struck over and over, and the player saw it in seconds. **The cycle number has to be part of the hash**, and then so should whether the event happens at all, so density can answer to something.
 The foliage sway flag was set when `worldY - floor(worldY) > 0.5`, and a plant blade's corners sit at whole world heights - so grass stood perfectly still while leaves moved, and the player reported exactly that. **A test about "the top of this thing" has to be measured against the thing's own extent**, not against a coordinate that happens to be near it.

- ⛔ **A direction baked in local space rotates with whatever holds it.** Rain quads are each turned to face the camera's own column, so a slant applied in quad-local space leaned a different way for every quad - and looking straight up at the result read as the rain spiralling around the player. Anything that is a *world* direction - wind, gravity, a flow - has to be resolved into each piece of geometry's own frame, not applied as a constant to all of them. Here the mesh carries the wind's component along each quad, which costs one dot product at build time.

- **Anti-aliasing belongs after the tone curve, not before it.** A bright pixel beside a dark one is a 40:1 ratio in HDR and about 2:1 once graded, so blending beforehand is dominated by whichever neighbour is brightest and barely moves the edge at all. The fix is structural: put the whole grade in a function the anti-aliaser can run over its neighbours.

- **A steady animation reads as a bug; an intermittent one reads as weather.** The first foliage sway ran at a constant low amplitude and was reported as "always swaying". Multiplying two slow sine waves gives a product that spends real time at zero, so the world stands still and then a gust crosses it. **Anything meant to feel natural needs to stop sometimes.**

- **A counter that "must read zero" is worthless until you print what it counted.** The worldgen probe reported 1472 cells of floating terrain for an unknown number of milestones. One temporary line breaking it down by block id showed all 1472 were lily pads, which grow on water and therefore sit above the terrain top by definition. The world was correct and the exemption list was not. **When an invariant fails by a small margin, print the offenders before touching the thing being measured.**

## Textures, atlases and sprites
- ⛔ **Reference art drawn for an *additive* blend is opaque with a black surround, and used as-is it renders as a black tile with a picture in it.** The sun and all eight moon phases ship that way. The honest translation into an alpha-blended pipeline is to **derive alpha from brightness**, which is what an additive blend does - and it gives the moon's crescents their shape for free. Then two things bite in sequence: the staging tool's "this looks like translucent block art, flatten its alpha" rule matches a sky body exactly, because a halo is partly clear *everywhere*; and the body itself is only the **middle eight columns** of a 32x32 sheet, so a naive downscale makes it a quarter of the size it should be. Crop the centre, exclude it from the flatten pass, and size the quad for the proportion.
- **A placeholder in `assets/` exists so the load does not fail, and nothing more.** Eight moon phases got eight identical pale discs. Shaping eight crescents by hand would be work thrown away twice over - development runs on the staged reference art, and the original art belongs to the artist.

- ⛔ **Going proportional broke every string in the game, and the cause was the depth test again.** Each glyph was drawn at full cell width (8 texels) while the pen advanced by the glyph's own width (6 for most letters), on the reasoning that a narrow letter's transparent columns would harmlessly overlap its neighbour. They do not: **the font layer has no alpha discard**, so those transparent texels still write depth, and the next glyph's overlapping left columns were rejected. Every character after the first lost its left two columns - `h` rendered as `n`, `e` as a stub - and the user's report was simply "text is broken". The fix is to **crop each quad to its advance** so the quads sit edge to edge, which is exactly what the old monospace atlas got for free and why nothing had ever exercised the case. Third appearance of "a blended fragment still writes depth" in two days; it is worth asking of any HUD geometry that overlaps.
- **A font can be proved without running the game.** A throwaway script composited the same strings out of the staged atlas using the *same* advance-and-crop rule and saved a PNG, which showed "Construction" and "Cobblestone Stairs" rendering cleanly before the build was even launched. Cheaper than a screenshot harness, and it tests the arithmetic rather than the window.

- **A pixel font's advances belong to its artwork, so measure them from the atlas that loaded.** Swapping our monospace Consolas atlas for Mojang's `ascii.png` made the font proportional, and the obvious way to carry that is a written-down width table — which is the second copy this project keeps paying for, and goes stale the moment the atlas is swapped again. The rule is one line: **a glyph's advance is its rightmost opaque column plus one texel**, read at startup through the renderer's alpha. It reproduces the reference's published widths exactly (`i` 2, `l` 3, `I` 4, `a` 6, `@` 7), which is also the check that the *layout* is right — get the grid wrong and 'a' does not measure 6. The space is the one advance that must be a number, because a blank cell has no column to measure.
- **Lay our own placeholder art out exactly like the reference's.** `ascii.png` is a 16×16 grid of 8×8 cells indexed by codepoint; our generated font was a 16×6 grid of 8×14 cells indexed from 32. Regenerating ours in *their* layout is what makes the reference a drop-in swap and lets one set of constants serve both — the same property `hud-reference.png` already has, and the reason a wrong sheet size is logged as an error rather than silently skewing everything.

- **A model's lid is painted somewhere other than its walls.** `ModelBox` carried one rectangle for all six faces, so a lantern's glass panels appeared on its lid and its cap's 4x4 plate was squashed into a 4x2 sliver. The reference's own `template_lantern` gives `up` and `down` a different `uv` from the four sides, and transcribing that is the whole fix. **Transcribe a model's faces, not its element.**
- **Derive a "flashes white" texture as pure white, not as a mix toward it.** Lit TNT was blended 65% of the way to white, which reads as dull grey with the lettering still legible - the user asked for "pure white to the point the texture isn't visible". The reference has no such texture at all; it tints the whole entity in a shader. The honest derivation is therefore to keep the alpha and set RGB to 255, not to pick a blend that looks bright in a paint program.

- ⛔ **A block that is a *model* cannot take its UVs from where its boxes sit.** Every cut shape here is carved out of a cube, so box position *is* texture position — right for a stair, wrong for a lantern, whose 6×7 body is painted in the corner of a 16×48 sheet while its box sits in the middle of the cell. The lantern rendered as a small box of flame. `ModelBox` carries its own rect; the two facts are genuinely separate and have to be said separately.

- **Mojang's `normal_left.png` is the viewer's *right* half of a double chest, and `normal_right.png` is the viewer's *left*.** The names describe the chest's own handedness, not the camera's, and taking them at face value puts the latch on the outside corners with the lid split down the middle — a plausible-looking result that is exactly wrong. It was settled by **measuring** instead: per-column luminance of the front rects shows `normal` carrying a border at *both* ends (columns 36 and 59) while each half carries one at a single end, and the buried face is the transparent one, which confirms the handedness a second independent way. Two crops additionally need mirroring because a top face's `u` runs opposite to a front face's in the mesher's face table. **When a filename asserts an orientation, verify it against pixels before building on it.**
- **Smooth noise is the wrong tool for pixel art, and "quantize it afterwards" does not rescue it.** The first two attempts at block textures used smoothstep-interpolated value noise mapped through a palette. The result was soft melted blobs that the user called "odd and uncanny" — correctly, because interpolation *is* blurring, and blurring is the one thing pixel art never does. Real block textures are per-pixel choices from a **narrow** palette, weighted so most pixels sit on middle tones, with sparse accent pixels (the grey pebbles in soil) and short horizontal runs. Contrast instinct is also wrong: reference stone spans about five near-identical greys, roughly a third of the range that felt right. **When making art, get reference in front of you before generating anything** — two rewrites were spent guessing at what "looks like a block texture" means.
- **Measure the reference's statistics instead of describing it in words.** "Bark is vertical stripes" produced whole 16-pixel columns painted one tone, which the user correctly called "all lines and stuff" — it reads as planks. Counting run lengths settled it in one command: the reference's vertical runs are overwhelmingly **1-3 pixels** and 90% of horizontal runs are a single pixel, so bark is a field of short dashes where neighbouring pixels almost never match. Three further numbers each caught a separate mistake: column means showed one column at luminance 59 against another at 122 (the "big fat strip of dark"), which came from applying a per-column tone lean to *every* run rather than letting each column draw from a narrow window; consecutive runs picking the same tone silently merged into chunky 5-9px slabs; and tiling the texture out 3×2 showed that a strict `x % 2` light/dark alternation makes the 16-pixel repeat obvious, where the reference runs two dark or two light columns together often enough to hide it. **Tile a texture before judging it** — a seam or a repeat is invisible in a single tile.
- **The same "tight value range" rule applies to detail, and I broke it twice in one texture set.** Log end-grain rings were given a 35-luminance jump where the reference uses **7**, and the bark border was two pixels where the reference is one. Rings are thin 1px lines on a light field, not thick alternating bands.
- **The agent is the artist, and the exception has grown — but only in *who authors*, never in *where reference art may live*.** Stated plainly by the user, 2026-07-30: _"buddy there is no artist, you ARE the artist. this is a solo dev project."_ That still holds for block textures, HUD art, the font and item icons — do not defer those, do not ship deliberately rough placeholders and call them someone else's problem, and do not treat "I can't see the screen" as an excuse; magnify the output and look at it (`tools/preview-textures.ps1` exists for exactly this).

  **It no longer holds for creature skins.** On 2026-08-03 the user asked an artist friend to take them on. Two phases sit ahead of her and both are slow: **researching how UV texturing works** — how a flat image wraps onto a 3D model — and then **drawing the art**, which is the longer half. Weeks, not days. **Do not re-author the creature skins to "help", and do not treat the wait as a problem to route around.** The generator that produced our rejected attempt still exists as a fallback and as living documentation of the nets; leave it alone unless asked. What *is* useful is keeping `TEXTURING.md` §13 and §14 accurate, because those are what she works from.

  **Almost every texture in the running build is currently Mojang's, and that is deliberate, sanctioned and temporary.** Direct user decision, 2026-08-02: _"yeah just replace all 9 textures with mojang's, i'll have my friend rework them."_ Four staged sets sit **beside each `game.exe`, never in `assets/`** — creature skins, spawn eggs, catalogue tabs and every block and item texture. Nothing is being smuggled anywhere. A future session finding this **should not panic, should not rip it out, and should not lecture the user about it**; the only hard requirement is that none of it survives into a release build. `assets/textures/hud.png` is the one exception that does live under `assets/`, also at the user's request.
- **A raised bevel is asymmetric, so a sprite's centre is not its interior's centre.** The hotbar cell has a three-pixel bevel on the top and left but two on the bottom and right — that asymmetry *is* the raised look. Centring icons on the tile put them visibly high and left. Anything placed inside a bevelled frame must be positioned from the interior rectangle, not the outer one.
- **`add_custom_command(TARGET ... POST_BUILD)` does not run when nothing was rebuilt, so editing only an asset silently shipped the old one.** The CMake file carried a comment claiming the opposite — that copying after every build meant "editing a texture and rebuilding is enough to see the change" — and that comment was wrong from the day it was written. Ninja reports `no work to do`, the copy never fires, and the game loads the previous texture while you stare at it wondering why your edit did nothing. It is now a real target with `ALL` plus `copy_directory_if_different`. **A build step that only runs as a side effect of another step is not a build step**, and this class of bug wastes time specifically when iterating on art, which is when you are least inclined to suspect the build.
- **An alpha-tested pixel must be written fully opaque, and the failure looks like a lighting bug.** Cutout leaves came out pale grey at distance while near ones were fine. Mip levels average alpha, so distant foliage arrives at around 0.6 — above the `< 0.5` discard threshold, so it survives, and then blends with the sky because blending is enabled for every draw. **The diagnosis came from sampling the pixels rather than theorising:** the pale colour was almost exactly halfway between leaf green and sky blue, which named the mechanism immediately. Texture alpha is consumed by the discard test and must not reach the output; world transparency belongs in the vertex alpha, where water already keeps it.
- **Matching a reference statistic is not the same as looking right, and chasing the number shipped something visibly worse.** M19a rebuilt every block texture against measured reference statistics — palette size, luminance spread, run lengths — and most of that was a genuine improvement. Then it went further and copied the *mechanic* of biome-tinted foliage: author grass and leaves greyscale, multiply by a per-column colour. Every measurement agreed with the reference. The result was a world of pale sage-grey plants that the user called ugly on sight, and they were right. **The mechanism is arithmetic anyone could have predicted: a mid-grey texture times a mid-green tint lands far darker and duller than either, so authoring the texture bright enough to survive the multiply is a tuning problem that was never done.** Two rules follow. **Look at the result before calling it done** — a statistic moving toward the reference is evidence, not proof, and the screenshot took ten seconds to check and was never taken. And **the reference documents what Minecraft does, not what this game should do**; copying a mechanic needs its own justification, because the tint only pays for itself once there are enough biomes to notice, and it cost a vertex attribute, per-chunk climate storage and a merge-comparison field to deliver washed-out grass. All of it was reverted; the texture statistics work was kept.
- **PowerShell binds `,` tighter than arithmetic, and the failure lands at runtime.** `@(@($i, 0), @($i, $size - 1), ...)` parses as `(... , @($i, $size)) - 1`, which throws `[System.Object[]] does not contain a method named 'op_Subtraction'` once per call. Parenthesise the arithmetic, or better, avoid nested coordinate arrays entirely and write the four cases out. **The error was invisible because the run was filtered** with `Select-String "error|Exception"` and the message contains neither word — it says "Method invocation failed". The textures were written *without their border* and the script reported success. **When a generator's output looks unchanged, run it unfiltered before assuming the code is wrong.**
- **"You are the artist" is about not deferring art, not about when to author it — and I got that wrong on the UI.** Slice 3 of the inventory screen exists to de-risk the *layout*, and I drew original tab sprites for it. The user's reaction was immediate: _"who asked you to make textures? i thought instructions in mds were clear."_ They were. The standing instruction is theirs, from 2026-08-02 and stated generally: _"the reason i ask for mojang's textures is so that we know it works, and then after we use mojang's textures in the reference folder, we can develop our own."_ Twenty-one UI crops had been extracted into `reference/ui-icons/` for exactly this, and the brief said **"Measure from them."** I took the *numbers* from them and then drew my own pixels — which leaves two unknowns behind one symptom, because a tab that looks wrong could be bad geometry or bad art. **Stage the reference, prove the layout, author afterwards.** `tools/make-reference-hud.ps1` now does for the HUD what the creature atlas does for skins, with the same `assets/` guard.
- **A proof atlas must never resize what it composites, and the size check is the proof.** `make-reference-hud.ps1` throws if a tab crop is not exactly 22×25, because that dimension *is* the evidence that `crafting-ui.avif` is vanilla's layout at a clean 3×. Rescaling to fit would hide a drifted extraction behind a plausible-looking result — the same class of silent failure as the sheet-height bug, where a wrong size did not fail but slid every UV.
- **When our own art comes out wrong, read the reference again and rewrite it — do not tweak.** Direct user instruction, 2026-08-02: _"if the first iteration of our own texture doesn't work or looks ugly or whatever, instead of trying to tweak, just make it from scratch after looking at the reference texture AGAIN. if our texture is almost there and just needs a smidge of work then tweaking is fine."_ The reason it matters is that bad art is almost always the wrong **technique**, not the wrong **parameter**, and no amount of parameter nudging crosses that gap — the cow's patches needed grown blobs rather than per-cell odds, and the pig needed flat per-face planes rather than per-pixel palette picks. `tools/compare-skin.ps1` says which case you are in: **correlation near zero means rewrite**, because there is no structure to preserve. The pig went from -0.03 to 0.82 by being rewritten and 0.82 to 0.83 by being tuned.
- **Markings made from independent per-cell odds read as static, not as an animal.** A cow's patches were generated by rolling a 3×3 grid cell by cell; the user's verdict was "this looks like a cyborg", and that is exactly what independent rolls produce. Real markings are *connected blobs* three to eight texels across with ragged edges, made by placing a few centres and growing outward — nine stamped discs beat two hundred cell rolls. The same texture also had a bone-white muzzle where the reference lifts it only from luminance 56 to 82; **an accent is usually far subtler than instinct suggests**, and overdoing it turns a face into a mask.
- **A measurement can be wrong in a way that looks like the art being wrong, and it will not tell you which.** `tools/compare-skin.ps1` reported the cow's patches at 11.6× the reference's — damning — while the texture was actually fine. The fault was a **median threshold**, which only separates light from dark when the two are balanced; our markings cover about a third, so the median landed inside the hide and carved it up by its own faint variation into phantom patches. Otsu's method scored the identical texture at 0.9×. **Before acting on a metric, check its assumptions hold for the thing being measured** — and make it refuse to answer where they do not, which is why the tool now skips patch analysis on near-uniform or densely dithered references rather than emitting a confident wrong number.
- **A blunt regex over a script hits the definition as well as the call.** `-replace "New-CharcoalTexture", "New-CharcoalTexture`nNew-TorchTexture"` turned `function New-CharcoalTexture {` into a syntax error. Anchoring with `(?m)^…$` then silently matched nothing, because the files are CRLF. Edit script call-sites by hand, or match on something that only appears once.
- **PowerShell's `-File` flattens an array parameter into one string.** `powershell -File tools\preview-textures.ps1 -Files a.png,b.png` arrives as a single filename containing commas and throws "Missing file" listing all of them at once, which reads like a path problem rather than a quoting one. Dot-source or call the script directly (`& .\tools\preview-textures.ps1 -Files a,b`) when a parameter is `[string[]]`.
- **A current texture does not prove an old model map is still current.** The 26.2 chicken replaced its boxed legs with a one-texel strip and alpha-shaped foot, and the goat and rabbit repacked most appendages entirely. Legacy dimensions looked plausible and failed the alpha-footprint check. The reliable sequence is: look at the actual image, measure candidate nets against its alpha, check a compatible model source where one exists, then use clean original packing for any irregular islands rather than forcing an obsolete six-face net. `tools/analyze-alpha.ps1` and `tools/preview-grid.ps1` keep those checks visual and bounded.
- **Prove the model against the reference pixels first, then author the skin.** User instruction, 2026-08-02: _"the reason i ask for mojang's textures is so that we know it works, and then after we use mojang's textures in the reference folder, we can develop our own."_ A wrong net and a wrong skin produce the same symptom — pixels on the wrong face — so debugging both at once is guessing. `tools/make-reference-creature-atlas.ps1` packs the reference textures at our row offsets and writes `creatures-reference.png` **beside the exe**, never under `assets/`, and the game prefers it when present. That separation turned nine species from "looks wrong somehow" into a list of exact box dimensions in one pass. **Delete the atlas before any release** — it is the same category of exception as the HUD sheet, but with an expiry date.
- **Alpha runs per row beat a character grid, and a band narrower than the formula predicts is information about where the box is buried.** The §2 grid stops being readable past 64 columns, and the camel is 128. Printing each row's opaque runs makes the net arithmetic fall out directly: band rows are `2w` wide at `u+d`, full rows `2(w+d)` at `u`, and the two row counts are `d` and `h`. **Where a band is narrower than predicted, a rect is transparent — and *which* one tells you where the box sits.** The goat's ruff has an empty bottom rect, so it must be sunk into the body far enough that the hole never shows. The wolf's body shows 6 columns where 12 are predicted, and crucially at `u+d+w` rather than `u+d`, so it is the *top* rect that is missing — which after the torso's quarter turn points forward, which is where the mane must sit. That placed a second box before a line of code was written. **Never read an unexpected width as a measurement error; work out which rect is absent and ask what covers it.**
- **PowerShell variable names are case insensitive, so a local silently overwrote a parameter.** A mixing helper declared `[string]$B` and assigned the blue channel to `$b`. The int became the string `"186"`, `{2:X2}` printed it verbatim, and the six-character hex became seven — which read back as a completely different colour. Fifteen percent of the goat turned bright yellow and *every metric still looked reasonable*; only the palette dump showed it. Prefix locals in any function with single-letter parameters, and validate generated hex where it is consumed.
- **A two-stage generator with the sheet size written down twice will silently undo the first stage — and then poison everything downstream of it.** Growing `creatures.png` from 896 to 1088 rows worked in `make-creature-skins.ps1` and was thrown away by `make-roster-skins.ps1`, which carried its own `$sheetHeight = 896` and passed it to `New-Object Bitmap $loaded, $w, $h` — **the *scaling* constructor**. The sheet was resampled back down and saved, with no error and both scripts reporting success. **The second-order failure was worse and took longer to find.** The corrupted sheet was then composited into `creatures-reference.png`, which is what the game actually loads; fixing the sheet afterwards did *not* fix the atlas, so the sheep stayed mangled through a rebuild, a re-run and a verification pass that only ever checked the sheet. **Fixing a generator does not fix what was generated from it while it was broken — list the derived artifacts and rebuild every one.** `run.ps1` now rebuilds the atlas whenever the sheet is newer, `kCreatureSheetWidth/Height` in `Creature.hpp` is the single owner of the size, and `Main.cpp` reads the PNG header and logs an error on mismatch, because a wrong sheet height does not fail — it slides every UV and mistextures the entire roster in silence.
- **Two species can share a rig outright, and the alpha channel says so for free.** `ocelot.png` and `cat_tabby.png` have byte-identical opaque runs on every row, which is proof they are one model before any measuring is done — so the ocelot cost one lambda and a row offset. Diff the alpha of a new skin against every skin already modelled before deriving anything.
- **Skipping "decorative" alpha detail can be the difference between an animal and a box.** A spiky mane was written off as flat sprite noise on the sheet and left out; it was a clean box whose spikes are cut by alpha, and without it the animal was bald and the dark stripe on its back read as a texture error rather than as the root of a mane. **Parse an unexplained region as a net before calling it decoration** — the arithmetic takes one minute, and a scatter of loose bristles is exactly what alpha cutout looks like on a sheet.
- **A comment naming its own blocker outlives the blocker.** `blockTextureLayer`'s crafting-table branch said the reference's two-and-two side arrangement "needs a face direction the mesher does not supply". `FaceDirection` had supplied it since the furnace work, and the table had gone on returning its front on all four sides — the same shape as the `FurnaceSide` layer that existed and was never returned. **When you add the thing a comment says is missing, grep for the comment.**
- **Growing the creature sheet is safe only if every derived copy is rebuilt in order, and there are four of them.** `assets/textures/creatures.png` comes from `make-creature-skins.ps1` (which owns `$sheetHeight`), `creatures-reference.png` is composited *from that sheet* by the atlas tool, and the build then syncs both next to each of the two executables. Regenerate the sheet, **then** the atlas, then build - and check all four with a one-line size dump, because a stale one does not fail, it silently slides every UV. `kCreatureSheetWidth/Height` is the single owner and `Main.cpp` checks the PNG header against it.
- **A texture layer run that starts at a hardcoded index is one appended texture away from silent breakage.** `TextureLayer::SpawnEggFirst = 38` is correct only while the block texture list has exactly 38 entries, and adding a block would slide all thirty-six egg sprites by one with nothing to notice. The list and the constant are now compared at startup and the mismatch is logged by name. **Any "this enum value equals that container's size" relationship needs a runtime check, because the compiler cannot see it.**
- **Warn about faults, never about a state the user chose.** Three startup lines announced that placeholder reference art was in use — accurate, and pure noise, because the arrangement is deliberate, documented in three places and visible on screen. The user's verdict: _"remove the warnings, they're excess and just stupid... don't do unecessary things like that."_ What stayed is the diagnostics that catch a genuine fault: a sheet whose dimensions disagree with the code, a missing sprite, a layer constant that has drifted. **The test is whether the line tells the reader something they could not have known and would want to act on.**
- **A guard written as "is this the common case?" hides whatever the uncommon case does.** `ItemEntities::buildMesh` opened with `if (!isBlockItem(drop.item)) continue;` — so a dropped pickaxe fell, bobbed, could be picked up, and was **invisible**, and had been since tools existed. That was fixed at M20d by routing non-blocks down the *plant* path, which then rendered a sword as **two crossed swords** at 90° to each other. Both bugs are the same shape: a branch that answers "what is the common thing" and quietly mishandles everything else. A sprite with thickness — two quads a few centimetres apart along the spin axis — is the answer the crossed pair was standing in for, and the comment justifying the cross (*"a single quad turns edge-on and disappears"*) describes exactly the problem thickness solves better.
- **`TextureLayer::SpawnEggFirst` has now moved three times in two sessions, 38 → 54 → 67, and nothing about that is safe.** It is a hardcoded index that must equal the length of a list in a different file; adding one block texture slides all thirty-six egg sprites and, since M20e, the eleven resource sprites behind them. It fails silently and mistextures the lot. The startup check comparing it against the loaded list is what makes this survivable, and it is the general answer: **any "this constant equals that container's size" relationship needs a runtime check, because the compiler cannot see it.**
- **A thickness given as an absolute distance stops meaning anything when you cannot see the object it belongs to.** A dropped sprite was extruded by 0.045 m, which is nearly half the 0.20 m sprite's own width - two quads that far apart read as two parallel swords, and the earlier "crossed swords" fix had only changed the angle between them. The reference extrudes an item by **one texel of its own grid**, and deriving it that way (`kHalfSize / 16`) makes it correct at any size the drop is ever rendered. **When a visual constant is a distance, ask what fraction of the thing it is.**
- **A simplification that names its own blocker will sit there until someone reads the comment as a bug report.** `blockTextureLayer` carried "the mesher cannot say which way a side face points, so the mouth appears on all four" for the furnace *and* the crafting table. It was accurate, and it meant a furnace was a cube with four faces on it. The blocker turned out to be one column: the mesher's face table already listed all six directions and simply threw the horizontal ones away as `BlockFace::Side`. **A `TextureLayer` that exists and is never returned is the tell** - `FurnaceSide` had been loaded into the texture array the whole time.
- **When one id becomes eight, every predicate that named the id has to become a predicate about the family.** Giving the furnace a facing turned two ids into eight, and the dangerous one was not the texture lookup - it was `blockLightEmission`, which tested `id == FurnaceLit` and would have left three of the four directions glowing for nobody. `isFurnace` was already a helper and every user of it came along for free; the ones that had spelled out `case BlockId::Furnace: case BlockId::FurnaceLit:` (blast resistance) were the ones that silently stopped matching. **Grep for the enumerator, not just the helper.**
- **A constant equal to another constant's value, written twice, is the bug shape even when both are right.** `TextureLayer::SpawnEggFirst = 67` and `kResourceSpritesFirst = 67 + kSpawnEggLayers` both carried the literal, so moving one would silently mistexture eleven items while the startup check on the *other* stayed happy. Derived now. **When adding a run of texture layers, append it after every existing run** - the water animation's thirty-two frames went on the very end precisely so no constant had to move.
- **Fog has to fade alpha as well as colour, and the clear colour has to move with it.** A distant translucent surface otherwise stays see-through in the middle of solid fog, and the sky shows through wherever no geometry was drawn - from sixty metres underwater, which is absurd. Both are one line each and both are invisible until you look in the right direction.
- **Our reference textures are Java's and our model geometry is Bedrock's, and for most mobs that difference is invisible.** The two sheets agree for nearly every species, so transcribing a `geo.json`'s UVs works — right up until it does not. The dolphin's tail sits at `(0,33)` in Bedrock and at `(0,19)` on the Java sheet, so its whole back half sampled empty pixels and simply **did not exist**, with no error and nothing in the log. The turtle's head net starts at `u=3` where its geometry says `2`. **After transcribing any UV, dump the alpha of that rect and check coverage** — 100% means the origin is right, partial means it has slid, and empty means the part is about to vanish. `tools/analyze-alpha.ps1` answers it in one command, and it is far cheaper than a render.
- **A `geo.json`'s UVs are only valid for the sheet that geometry ships with, and reaching for them before reading our own texture cost two wrong models in one pass.** The magma cube's Bedrock geometry describes a 64×32 sheet; our reference art is Java's 64×64, and the two layouts have nothing in common — Java runs four slabs down a left column at `u=0` and four down a right column at `u=32`, nine rows apart. Transcribing Bedrock's list produced rects that measured **50% covered**, and the reflex fix — pointing every slab at the one row that measured 100% — was worse, because it **deleted the eyes**, which are painted on the front faces of two specific slabs. `tools/alpha-runs.ps1` on our own texture gave the true net in one command and every rect then measured 100%. **Read the net from the texture you are actually going to sample; a geometry file tells you the box sizes, not where they live.**
- **A symmetric texture cannot disprove a mirror, and the double chest wore the proof of it down the middle of its lid.** The paired lids were built with `-Mirror` on the theory that a box's UP rect unwraps with u running the other way from its front's. A single chest can never contradict that, because its lid is symmetric in u — so the claim sat in a comment, unfalsified, until two halves had to agree with each other. Per-column luminance settles it in one command: the dark border sits at column 0 of `normal_right`'s UP rect and column 14 of `normal_left`'s, **exactly where each front's does**. Mirrored, both borders land against the seam, which is the line the user saw. **When a transform is justified by an argument rather than a measurement, find the case where the argument is falsifiable** — here, the asymmetric pair — rather than trusting the case that cannot fail.
- **The renderer has one blended pass and water owns it, so any reference art with a uniform partial alpha is a bug in waiting.** Ice, honey and slime are `translucent` in the reference and their textures are flat alpha 180-191 across all 256 texels. They arrive in *our* opaque pass, where blending is always on and nothing sorts, so they came out washed out and see-through against whatever happened to be drawn first. Flattening the alpha at staging time is the honest fix until M23 can sort translucent geometry. **The test is the shape of the alpha, not a list**: no fully clear texel rules out glass, leaves and every plant; most texels partly clear rules out ordinary art. That distinguishes the four blocks that need it from the six hundred that do not, and it will catch the next one automatically.
- **A reference texture is only correct for the model that samples it.** `bamboo_stalk.png` is a fully opaque 16×16 because the reference's model draws a 2/16-wide post from uv 13-16. We draw cross blades spanning the whole cell, so the same file came out as a solid square of bamboo colour — no transparency, no error, and it had shipped that way since bamboo was added. **Anything drawn as a `Cross` needs art whose silhouette is the object**, so a stalk has to be cropped onto a transparent field at staging time. The automated form of this check is "a cutout block whose texture has no clear texels", which is what found it.

## World, chunks, meshing and light

- ⛔⛔ **Two constants with the same name are not necessarily one constant, and "unifying" them changes the world.** `kSteepDrop` sat in `TerrainGenerator.cpp` at 2 and in `Structures.cpp` at 4 — which reads exactly like the duplication this project keeps paying for, was flagged as such by an audit, and was unified. It was **reverted the same day.** They are two different questions: the generator asks for a **signed fall across a two-column span** (does the ground drop away *this* way, which is what makes a face bare rock), and the tree gate asks for an **absolute one-column step against all four neighbours** (is this ground level enough for a sapling, which has no direction to care about). Sharing the generator's test at 4 makes far less bare stone on every peak in the world — a silent terrain change, delivered inside a bug-fix session, in exchange for nothing. They are now `kSteepDropOverTwoColumns` and `kTreeSteepNeighbourStep`, and each name says what it measures. **Before merging two constants, write down in words what each one measures; if the sentences differ, the fix is two names rather than one value.** The counter-case is real and was fixed the same day: the altitude lapse rate genuinely *was* one rule in two copies, and the copies disagreed about which four biomes freeze.

- ⛔⛔ **The second copy of a rule does not inherit the first one's bug fixes, and nothing will tell you.** Lava's spread is a deliberate parallel of water's — different step, different delay, different contact behaviour — and its pooling test read `isSolid(under) || isLava(under)`. Water's had been corrected at M20h to *exclude* a cell resting on more of itself, with the reasoning written out at the function: **"a cell resting on more water is partway down a column, not the bottom of one."** Lava never got that clause, so every cell of a fall counted as the bottom of one and fed sideways at every height it passed — a bucket emptied off a tower grew a wall down the drop. The user reported it as **"lava stacks, it's a liquid it shouldn't do that."** **When two systems are written as deliberate parallels, a fix to one is a bug report against the other** — go and read the sibling, and say in the comment that they are siblings so the next person does.

- ⛔⛔ **A work queue with no priority is a random queue, and the symptom is a feature that "kind of" works.** The mesh queue is a stack; entries arrive in whatever order the chunk map iterates and light propagation dirties them. So when the distance tier queued a ring of chunks for re-meshing, they were rebuilt in effectively random order — **measured, the closest chunk still drawn at the wrong tier reached zero, the one under the player's feet**, which on screen is plants appearing a few paces ahead of you while flying. One `nth_element` per dispatch, so the nearest few are at the back, fixes it; a full sort of several thousand entries every frame would not be affordable and is not needed, because only the handful about to be dispatched have to be the right ones.
- ⛔ **Recording an intent is not the same as recording a fact, and a queue that can drop entries needs both.** The tier a chunk *should* be at has to be stored for the boundary's hysteresis to work at all — what a chunk should be depends on what it already is. But a queued re-mesh is dropped when a neighbour is not loaded or the player has moved on, and with only the intent recorded such a chunk then agrees with itself and keeps the wrong mesh for ever. Two fields, `detailWanted` and `detailBuilt`, and the recovery is one clause in the pass that already walks every chunk.
- **A padded copy is a row of memory, not a cube of cells.** `gatherVolume` walked 39,304 cells through three bounds-checked accessors, recomputing a chunk pointer and a local coordinate for each. X is the fastest-moving axis in both layouts, so a row is one `memcpy` at each end plus one in the middle; the waterlogged bits are consecutive, so a whole row is usually dismissed with a single test. It also **returned the volume by value into a second one that had just been zeroed** — four passes over 118 KB where two will do.

- ⛐⛐ **Two thirds of everything a voxel mesher emits can be geometry nobody can ever see, and no amount of reasoning will tell you that.** A throwaway probe tallying triangles by category *and by height band* reported opaque terrain 75.8%, plants 13.7%, cutouts 7.4%, fluid 2.7% — and **65% of the total in the bottom 32 blocks of a 96-block world**, on the walls of caves sealed inside rock. The optimisation everyone would have written first (drop distant plants) was worth 4.3%; dropping distant faces that receive no light at all was worth four times that. **Twenty lines of atomic counters in the emit path, read once, deleted. Write the probe before writing the optimisation.**
- ⭐ **"Receives no light of any kind" is a safe proxy for "cannot be seen", because light is a flood fill.** For a face inside sealed rock to be visible from outside there would have to be an opening, and an opening is exactly what would have raised its light above zero. The one thing to check before trusting it: **sky light is a *level*, not a time of day** — an outdoor face reads 15 at midnight as well as at noon, so nothing on the surface is ever caught by the rule.
- **Water is the one thing in the world that never greedily merges, and the reason is in the vertex, not the texture.** A wave displaces a quad's own corners, so a merged sixteen-block face becomes one flat tilted plane instead of a swell and disagrees with its neighbours about the height of their shared edge. Every ocean surface cell is therefore its own quad. Holding the surface still is what lets it merge — and only the *geometric* displacement has to go, because the fragment stage shades the same wave field from world position, so it still ripples.
- **A canopy is solid geometry on purpose, and that is the expensive choice.** The mesher deliberately keeps the faces a leaf block shares with its own kind, so looking through a hole in the near leaves shows more leaves rather than sky. Correct up close and invisible at distance — dropping those interior faces turns a blob into a hollow shell with an identical silhouette.
- ⛔ **A rendering tier that is also read by a worker thread is not a rendering tier, it is a race.** Meshing is a pure function of its input volume, which is the whole reason it can run off the main thread; a job that asked where the player was would give two chunks meshed in the same frame different answers about where the boundary is. **Decide the tier on the main thread and carry it with the volume.** And put hysteresis on it: the boundary is measured between chunk coordinates, so without a chunk of slack a player pacing across a single chunk edge flips a whole ring in and out on alternate steps.
- **The proof that a detail tier is safe is a subset test, not a screenshot.** Mesh the same volume at both tiers and check that every vertex the far tier emits also exists in the near tier's set — position and texture layer. **Far-only vertices must be zero**, which is exactly the statement "this can only ever remove geometry, so the world's silhouette cannot change with distance". Cheap, exhaustive, and it needs no window.

- ⛔⛔ **How big a feature looks depends on how far away the layer is, and a layer directly overhead is the case where that bites.** The first cloud deck sat at y 74 in a 96-tall world with the player at 40 - 34 blocks up - and its features were 156 blocks across. The entire visible sky therefore spanned less than *half of one feature*, so the whole hemisphere came back as a single value and it rendered as a uniform white veil. It looked like a broken shader and was a scale error. The reference's deck is about 128 blocks above the player, which is what makes a cloud subtend a useful angle. **A cloud deck is a field, not geometry, so it is not bound by the build ceiling** - ours sits at 140 in a world 96 tall, and that one line fixed it.
- **A threshold on a sum of noise octaves needs the distribution, not the range.** Four octaves at halving amplitudes sum to a value centred near 0.47 with a spread of about 0.13 - nowhere near uniform over 0 to 1. A coverage "fraction" used directly as the threshold landed far out in the tail, so instead of some columns being solid cloud and the rest clear, *every* column came back faintly cloudy. Normalise the field first, then threshold. **Fourth time this project has been caught by treating a concentrated field as a uniform one** - see the cave entrance threshold, the climate gains and the flower index.

- ⛔ **A derivation applied to one axis and not to its twin.** The shape pass read `flipV` off the face tables and then wrote U as the raw cell coordinate, so the **+X and -Z faces of every shaped block in the game were mirrored**. It had been that way since shapes existed and nothing showed it, because no shaped block carried an asymmetric texture until lit TNT did - the user reported it as "the n is reversed". Ordinary TNT is a full cube and goes through the merged pass, which was always right, so the same block was correct in one state and backwards in the other. There is now a `constexpr` proof, `shapeUvsMatchFaceTables()`, that both flips reproduce the tables exactly on a unit box. **When you derive one of a pair off a table, derive the other off the same table and assert it.**
- ⛔ **Two arms meeting at a cell wall each drew their own end cap.** Adjacent glass panes are geometrically continuous, and each still emitted the quad standing at the shared boundary: two coplanar faces fighting over one depth, which reads as a hard border line down the middle of what should be one sheet of glass. `occludesFace` cannot catch it, because a pane does not occlude anything. The cull is one test - the face is flush, the axis is horizontal, and the neighbour has the same shape - and it is safe precisely because two connecting shapes of a kind always grow matching arms toward each other.
- ⛔ **A burial test that probes only the middle of a face deletes the parts of it you can see.** The mesher culled a box face when its *centre* landed inside another box of the same block. Right for a stair's step sitting on its own lower half; wrong for a lantern, whose cap covers the middle of the body's lid and leaves a one-texel ring showing all the way round. The ring vanished and you could see straight through the top of every lantern. Five probes now - the centre and the four corners pulled a hundredth inward - and buried means *all* of them are inside.

- ⛔⛔ **A probe window smaller than the field's wavelength measures one blob, not a distribution.** The worldgen census sampled 2048 blocks square; humidity and weirdness have wavelengths near a thousand, so it held about four noise cells. It reported 176 jungle columns with **every single one** on the same side of the weirdness crossing — which looks exactly like a broken axis, and I nearly "fixed" a mechanism that was working. Widened to 16,384 blocks the split is 1184 of 2064. **Before believing a share, check the window against the wavelength.**
- ⛔⛔ **A chunk is a hundred kilobytes and MSVC reserves the whole frame up front.** Three of them beside a per-biome block table overran the thread's stack, and the process died with `0xC00000FD` **before flushing a single log line** — so it read as "the probe does nothing" rather than as a crash. Every buffer that size in a diagnostic now lives on the heap. The tell is an exit code of `-1073741571` with an empty log.
- **Order of placement is a rule, not an ordering.** Cocoa generated **zero** pods because the trunk vines went on first and a cell holding a vine refuses one. Nothing reported it; the tree looked fine. Where two decorators want the same cells, the one that is rarer has to go first.

- **A family of blocks that differ only in *material* should carry a parent, not a copy of every answer.** There was one stair type, one slab and one fence in the game because adding a second meant a row in the texture table, the name switch, the hardness switch, the tool switch, the tier switch, the blast switch and the category test — seven edits per material, times fifty materials. A `ShapedFamily` row holding **the block it was cut from** collapsed all seven into `shapedParent`, and six hundred and forty blocks arrived with no art and no table rows. **Before writing the fortieth near-identical row, ask what the rows actually differ by.**
- ⛔ **A list of shapes written out twice will be edited once.** The mesher skipped six shapes in the greedy pass and accepted the same six in the shape pass, in two separate expressions three hundred lines apart. Adding a seventh to one and not the other draws the block twice or not at all, and the compiler is perfectly happy. One `usesShapePass(BlockShape)` owns the question now, and it is written as "anything that is neither empty nor a full cube" so it can never fall behind the enum.
- ⛔ **Adding a family test above a switch turns that switch's own cases into dead code**, and this is the *third* time. `blockName` had `case StoneSlab: return "Stone Slab"` and `case PlanksFence: return "Fence"`; the moment `isSlab` and `isFence` answered for them above, both labels became unreachable and the compiler said nothing. Same for `PlanksFence` in `blockHardness` and `harvestTool`. **When you widen a family predicate, grep for every id it now covers and delete what it swallowed.**
- **A run-bounded range test is a bug waiting for the run to grow.** `blastResistance` tested `block >= Netherrack && block <= FloweringAzalea` for its second table run — but five stripped logs had been appended after `FloweringAzalea`, so all five fell through to the stone default of 6.0 instead of wood's 2.0. It is now `kFirstExtraBlock2 .. kLastExtraBlock2`, which is what those constants are for. **Name the run's bound, never the block that currently happens to be last in it.**
- **A census that covers five biomes cannot disprove a plant that needs the sixth, and it reads exactly like a bug.** Ferns, cactus, bamboo, berry bushes, mushrooms and lily pads all came back **0** from the worldgen probe after being wired in. Five of the six were working perfectly. The probe's block census covers roughly 352×352 blocks around the origin — river, jagged peaks, meadow, windswept hills and plains — while its *biome share* list is sampled far more coarsely across a much wider area, so it cheerfully reports Taiga 4.9% and Desert 0.85% for biomes the census will never touch. Moving `spawn_x`/`spawn_z` does not help either: **the probe samples the origin regardless of the spawn setting.** A temporary wide scan across ±70 chunks settled it in one run. **Read what a diagnostic's sample actually covers before believing its zeros** — and when two figures in the same report disagree about which biomes exist, that is the tell.
- **A block that exists but is placed nowhere is completely invisible and nothing warns you.** Thirteen plants and both mushrooms shipped across two milestones without generating a single time, because the ground-cover pass held one hardcoded eight-entry flower array that nobody widened. Textures loaded, the catalogue listed them, both presets built clean, every probe assertion passed. **Existing and appearing are two different jobs**, and only the second one has no compiler behind it.
- **Where a placement lands is a rule, and having it in only one of the two places that need it is why you could not light a fire on grass.** The reference's `canBeReplaced` says a plant is built *into* rather than *on*; ours had the idea written down only as `yieldsToFalling`, for sand. So the placement path always used the cell one step along the face — a block placed while aiming at a flower appeared above it with the flower still standing — and flint and steel refused outright, because it demanded the target cell be `Air` and grassy ground almost always has a plant in it. **Two bugs, one missing rule.** The predicate is now `isReplaceable` and both paths read it.
- **A survival rule split across two functions will drift, and the half that is wrong is the one that runs least.** Fire's tick accepted "fuel beside it *or* a floor under it", exactly like the reference; the *placement* check demanded solid ground only. So fire could burn in places it could not be lit, which is invisible until someone tries to light one against a wall of logs. `World::fireCanSurvive` is now the single owner and both call it.
- **`BlockShape::Empty` means "draw a fluid volume", not "draw nothing", so giving fire that shape rendered a solid cube of flame.** Air and water are both `Empty` and water is very much drawn; the shape that actually wanted was `Cross`, which is what plants use — and it brought three more correct behaviours with it for nothing: no collision, extinguished by a flow (`isWashedAway`), and dying when the block under it goes (`needsSupportBelow`). **When a new block needs several behaviours at once, look for an existing shape that already implies them** rather than adding a flag per behaviour. The one thing it does *not* imply is cutout rendering, which had to be said outright.
- **Deciding where a flow *spreads* and deciding what an existing cell *is* are two questions, and folding them together makes water flicker for ever.** The reference keeps them apart: `getNewLiquid` recomputes a level from the horizontal neighbours with **no slope test in it at all**, and the slope weights are consulted only when spreading into an empty cell. Ours ran the slope test on every evaluation, so a settled cell could be starved by a weight that changed *because of its own outflow* — water reaches a ledge, the column below it fills, that direction stops scoring as a drop, a rival direction wins, the cell empties, the column drains, the weight flips back. Air, water, air, water, once per tick, for ever. **The tell is that it needs a drop within four blocks to happen at all**, which is why it read as "water goes mad near an edge" and why flat ground never showed it. Gate the slope test on `!isWater(current)` and the loop cannot form, because the level away from a source strictly increases and a mutual pair therefore drains.
- **A flicker is a measurement, not a judgement call.** The above was found by building eight scenarios on floating platforms, sampling every cell in the region at 20 Hz, letting everything settle for twenty seconds, **clearing the counters**, and then reporting any cell that changed four or more times in the next twenty. Before: 100 flips on six cells. After: zero, across sixteen scenarios including every water/lava arrangement. The settle-then-clear step is what makes the number mean something — a spreading flow legitimately changes dozens of cells, and counting from t=0 buries the signal. The matching negative control is to delete the source afterwards and require every rig back to **zero water**, or "nothing ever oscillates" is trivially satisfied by water that never recedes.
- **Widening a fluid predicate is not the same as widening a fluid.** `isWater` had 53 call sites across 13 files, and blanket-replacing them with `isFluid` would have been wrong in most: swimming, drowning, fish spawning and buckets-of-fish all mean *water* specifically. The rule that worked was to introduce `isFluid` for the questions where the two are genuinely interchangeable — can something move through, see through or be swept away by this cell — and leave every water-specific question alone. **Grep the call sites and decide one at a time; the count is the warning, not the work.**
- **A probe counter that names blocks by id goes stale the moment the family grows, and it reports the failure as a *regression in the world*.** `FLOATING-TERRAIN` read **8031** after a batch of new blocks landed — alarming, and entirely the probe's fault: its "is this a tree?" test listed oak log and leaves by id, so every spruce and birch trunk added milestones earlier counted as floating land. The world had been fine the whole time. Replacing the id list with the family predicates `isLogBlock`, `isLeafBlock` and `isCrossBlock` returned it to 0. **A diagnostic is code and rots like code** — when you widen a family, grep the probes too, and keep a positive control beside every assertion (the "tree cells above surface 27566" figure is what proves the counter can still see up there at all).
- **A guarantee that converts a bug into a different bug is only half a fix, and the second half is the condition it was standing in for.** Single-valued terrain made floating islands impossible — and turned every would-be island into a sheer-sided pillar attached to the ground, which the user found by watching a sheep walk off one into the water. The guarantee was doing its job; what was still wrong was the thing that produced the islands, namely `factor` running down to 1.15 across the whole low-erosion third of the erosion axis where the reference holds 5.1–6.3. **When a hard rule turns symptom A into symptom B, the rule is fine and the input is not.**
- **Two knobs that describe the same idea must share the same gate.** Jaggedness was correctly confined to a narrow window of high peaks-and-valleys on unworn ground; ruggedness (`factor`) was keyed on erosion alone. So a third of the world got peak-level ruggedness with none of the craggedness that is supposed to come with it. They are one decision — the reference gates both on the same spline branch — and splitting them is how the pillars got everywhere instead of onto summits.
- **A material can be *unreachable* rather than merely unused, and that is a stronger design.** The reference never names `river` in its surface rules at all: a river column falls through to grass, and gravel is reachable only after both of its water tests have failed — which cannot happen with no water overhead. **Sand never appears on a riverbed there under any circumstance.** So a river that fails to reach the sea is invisible, and the reference ships plenty of them through its mountains. Ours painted the biome's own top block regardless of the waterline and drew long ribbons of gravel and sand across dry grassland. Prefer "this cannot be selected here" to "this is not selected here".
- **An interpolated field's contour lines cluster on its cell boundaries, which reads as a pattern rather than as landscape.** The density lattice gave gentle slopes a step every four blocks — the user's words were that the terrain "looks random but at the same time, not random". The lattice is right for the reference, whose terrain is genuinely 3D and which cannot afford a density evaluation per block. It was wrong for us the moment terrain became single-valued: it was buying nothing but its own artefact. Deleting it made the height exact per block **and eight times faster**. **When a borrowed optimisation stops paying for the thing it was borrowed for, it is just its own side effects.**
- **Three bugs reported in one sentence by a player, and all three were a mechanism the reference has that we had dropped.** "Floating chunks of land", "random stone rings at surface", "peaks that are only snow". Not one was a tuning problem. The floating land was the 3D noise's vertical slope beating the terrain ramp — 0.10 per block against 0.02–0.08 — where the reference's noise carries amplitude proportional to wavelength and comes out at 0.010. The rings were `top = Stone` on a biome occupying a narrow band of a smooth field, where the reference uses a *patch* rule that falls through to grass. The white peaks were a missing `steep` condition. **When a generated world looks wrong in several unrelated ways at once, the fault is usually one layer that was ported as numbers when it was really a set of rules.**
- **A narrow interval of a smooth 2D field is an annulus, so a biome with one narrow axis is a ring.** The reference's own erosion band 5 is `[0.45, 0.55]` and it does not read as rings there for two reasons worth knowing: its windswept biomes are also boxed on temperature, humidity, continentalness and peaks-and-valleys, which cuts the annulus to about 3.7% of its own area — arcs, not a closed ring — and the terrain `factor` has spline knots at *exactly* those band edges, so the eye reads a change of landform rather than a painted stripe. Blurring the boundary does not work and neither does per-cell fuzz; it is ±2 blocks against a 200-block contour.
- **A guarantee can be cheaper than the tuning it replaces — but it does not excuse you from the tuning.** Chasing "no floating islands" through noise amplitudes and spline factors is a balancing act against a moving target; declaring terrain single-valued makes it impossible instead, and *pays for itself* twice more (the surface-rule depth counter becomes exact without walking above the chunk, and the ore air-exposure test becomes a height comparison). What it does **not** do is stop the islands being produced — it reshapes each one into a sheer pillar attached to the ground. That was written down at the time as "which looks better than what it replaced", and it was wrong: the pillars came back as the *next* bug report. See the first entry in this section.
- **A probe that reports a huge number may be measuring the wrong thing, and the fix is to name what you excluded.** The floating-terrain count read 175,742 after the fix that was supposed to make it impossible. It was **water** — every cell between the seabed and sea level is solid, not air, and above the terrain top by definition. The tell was that the number matched the ocean volume almost exactly. A count is only as good as its exclusion list, and the exclusion list belongs next to the count.
- **Print the probe's own sanity check beside its answer.** `FLOATING-TERRAIN 0 (tree cells above surface 22097, so the probe can see up there)` is one line and it retires the whole "is this zero because it is fixed or because the probe is broken" question, which has cost this project a round before.
- **A startup probe found four bugs that "both presets build clean with zero validation errors" could never have found, and one of them read as zero.** The worldgen rebuild passed the acceptance bar on its first build. The probe then measured **0.000% hollow space in deep rock** — caves were being generated and then *flooded*, because the water fill asked "is this cell empty now" instead of "did the uncarved terrain leave this empty", so every cave below sea level filled. One word. It also found humidity clamped at an extreme almost all the time (one shared field gain for five fields with different octave counts), iron generating more often than coal, and coal's band centred **above** the mean surface so most of it was in open air rather than in rock. **Anything with a distribution has a distribution you can print.**
- **A probe that answers "zero" is not automatically broken — check which zero it is.** The 0% hollow reading looked like the classic broken-probe result, and the reflex was to doubt the probe. It was right: the caves existed and were full of water. The tell was that total air was 66% of all blocks, so the probe could plainly see air; it was the *classification* that was wrong, not the measurement. Confirm a probe can produce a positive result **and** check what a negative one is actually saying.
- **A rule can be present, correct and never fire.** `steep` was implemented faithfully at the reference's 4-block threshold and produced bare rock on **4%** of jagged-peak columns, so the peaks were still white blobs. The rule was fine; the *terrain* was too smooth for it, because jaggedness ran at a 44-block wavelength where the reference samples its jagged noise far finer than anything else in its router. Moving it to 16 blocks took the same rule from 4% to 31%. **When a copied condition almost never triggers, measure the thing it is testing before touching the condition.**
- **Interpolating a field is not free of its own resolution, and the reference's answer did not transfer even once.** Terrain density on a 4 × 8 × 4 lattice is exactly what Minecraft does and it looked like a huge win — 1,500 samples a chunk against 32,768. Putting **caves** in the same lattice was obviously wrong from the start (a tunnel three blocks across rounds into a four-block blob, and the reference gets away with it only because its caves are large relative to its cell). Putting **terrain** in it turned out to be wrong too, one bug report later, for the reason in the contour-clustering entry above. Caves were carved at full resolution from the beginning; the terrain lattice was deleted afterwards. **When you find yourself writing "named divergence" next to an imported optimisation, check whether the import was worth making at all.**
- **Two paths that must agree about the ground have to share the arithmetic, not just the intent.** `surfaceHeightAt` and chunk generation both have to say where the surface is, and if they differed by a rounding step every tree would hang in the air or sink. `surfaceHeightAt` is literally a one-line call to the same `columnHeight` the generator uses, so they agree by construction rather than by care — the same property that made `legBox`, `beginHead` and `walkTo` safe to roll out. This mattered *more* under the old lattice, where agreement meant matching corner samples and a `trilerp`; collapsing to one function is why the property survived the rewrite for free.
- **A spline that clamps at its ends puts a flat shelf exactly where the interesting terrain lives.** Cubic Hermite between control points is obvious; what is not is that the extremes of the parameter space are *where mountains and deep ocean are*, so clamping outside the last point makes both of them plateaus the size of a biome. Extrapolate linearly with the end derivative.
- **Scaling a term by the thing that varies it can delete the term.** The river channel was first cut inside the erosion relief factor, like everything else that shapes the land. A plain has relief 0.2, so a six-and-a-half-block channel became one and a third and never reached the waterline — rivers existed everywhere except on flat ground, which is where rivers are. Some terms are absolute on purpose. The same mistake in reverse put a third of the snowy slopes underwater: the peaks-and-valleys trough was deep enough that every upland valley dipped below sea level.
- **A shared tuning constant across fields with different octave counts is a bug waiting to be measured.** One gain of 2.6 widened all five climate fields. Continentalness has nine octaves and landed at a mean of 0.36; humidity has two, landed at 0.65, and therefore spent almost all its time clamped — so the three middle humidity bands were nearly empty and forest, taiga and dense forest together came to under 7% of the world while badlands alone took 6.5%. Per-field gains, each measured.
- **Threading revealed that "re-mesh this chunk" and "this chunk changed" are different statements.** They were the same function (`markDirty`) while everything was synchronous, and that was harmless because a mesh was rebuilt immediately. Once meshing became asynchronous the distinction became load-bearing: editing a block on a chunk *face* has to invalidate the neighbour's in-flight job too, or the neighbour installs a mesh built from the old border and nothing ever corrects it. But using invalidation for the merely-unmeshed sweep would discard good in-flight work every time the player crossed a chunk boundary. Hence `invalidateMesh` (bumps the revision) versus `queueMesh` (does not). **This bug was found by reading the code, not by playing — it produces a seam that looks like ordinary chunk popping.**
- **The render distance limit was never a performance limit.** Before touching any code, making the radius configurable and simply raising it showed 383 fps at distance 16 where the game had been shipping distance 5. The real ceiling was **memory** (1.66 GB at distance 24), not frame rate. Had greedy meshing gone in first on the assumption that triangles were the problem, the right fix would still have landed but for the wrong reason, and LOD would have looked necessary when it is not. **Measure which resource is actually exhausted before choosing an optimisation.**
- **Greedy meshing did not need a vertex format change, because the sampler already repeated.** A merged quad simply takes texture coordinates of 0→N and tiles. The merged corners are built by *scaling the existing unit-face corners*, never reconstructed, so the backface winding that was verified on screen at M3 cannot be accidentally reversed. It costs about twice the meshing CPU for 6.4× less geometry.
- **Seeding a flood fill with everything already lit is the difference between 0.7 s and 8.5 s.** The first sky-light pass queued every lit cell as a propagation start point: tens of millions of entries, each costing six neighbour lookups through a hash map, all to discover their neighbours were equally bright. Only cells that can actually spread somewhere are worth queueing — for sky light, those with a horizontal neighbour whose light reaches less far down. **A flood fill's cost is set by how many seeds it starts with, not by how much it changes.**
- **Ambient occlusion partly undoes greedy meshing, and that is arithmetic rather than a bug.** A merged quad interpolates its four corners across the whole span, so it only reproduces the faces it replaced when every one of them was uniformly lit. Merging therefore has to be restricted to flat-lit faces, and geometry rose 2.37×. This was predicted before starting and measured afterwards; do not "fix" it by merging unevenly lit faces, which produces lighting that visibly slides across large quads.
- **Do not conclude a visual feature is broken from one object — confirm the thing being tested is actually in frame.** This has happened twice, in one session. Ambient occlusion looked entirely absent around a block resting on grass, which prompted a hunt through the corner-stepping maths; that block was *floating*, so nothing was adjacent to occlude, and the terrain terraces in the same screenshot had contact shadows the whole time. Then water looked missing at a spot that was above sea level. Both times the code was right. **A one-line log of the value at that position settles it faster than reading code**, and for a subtle effect, **exaggerate it to an absurd value first** — one rebuild turns "is this working?" into an unambiguous yes or no.
- **A face normal can be recovered in the fragment shader instead of stored per vertex.** `normalize(cross(dFdx(worldPos), dFdy(worldPos)))` is exact for flat geometry, which every voxel face is, and it avoided adding 12 bytes to every vertex in the world. It works only because chunk meshes are drawn with an identity model matrix, so the vertex position *is* the world position — if chunks ever gain a per-chunk transform, this breaks silently and everything goes flat-lit.
- **Cave geometry cost tracks surface area, not hollow volume.** Halving the tunnel width took the underground from 21% hollow to 12% and reduced triangles by 3%, because narrow winding tunnels have far more surface per unit volume than open caverns. Lowering the noise *frequency* instead — fewer, bigger caves — cut geometry 25% while making the underground *more* open. **When voxel geometry is too expensive, ask what the surface area is doing, not what the volume is doing.**
- **The user's framing of biomes was better than mine.** I filed them as content needing a design conversation; they pointed out that a biome is first a *developer* abstraction — the table that answers "which block goes here and why" — and the player-facing variety is a consequence. That reframing is what split M15b (the machinery, pure engineering) from the roster (the content). **When a feature looks like content, check whether there is a structural half that can ship first.**
- **Two noise fields, not one, is what makes biome selection work.** A single "climate" value can only order regions along a line, so desert, plains and tundra end up as bands. Temperature and humidity as independent fields give a 2D space where each biome is a point and territory falls out of proximity — and the same weights then give height blending for free, which is what stops neighbouring regions meeting at a cliff.
- **Generation purity forced the *better* structure design, not a worse one.** A tree crosses chunk boundaries, and the obvious implementation writes into neighbouring chunks — which breaks the "pure function of `(seed, chunkCoord)`" rule that all the threading rests on. The alternative sounds wasteful and is not: **each chunk rebuilds every structure that could reach it and throws away the blocks landing outside itself.** The same tree gets built up to four times over, independently, and every copy agrees because it is derived from the seed alone. No cross-chunk writes, no ordering dependency, no deferred-block queue to persist, and it costs a few hash evaluations. **Do not "optimise" this into writing across chunk boundaries** — it would reintroduce a generation-order dependency that only misbehaves under specific thread timing.
- **Put the cheap rejection first, and the ordering is not obvious until you count.** Tree placement originally sampled biome and surface height for every candidate cell, then rolled a hash to find that ~84% of them were empty anyway — three noise evaluations to answer a question one hash already settles. Reordering cut generation from 1912 ms to 1275 ms with no change in output. The guard constant is *derived* from the biome table (`maxTreeDensity()`) rather than written down, because a hand-maintained "max" silently breaks the moment someone adds a leafier biome.
- **The triangle count is a free correctness check on anything that touches meshing.** The first cut of that fix pre-multiplied face shade and ambient occlusion into the two light values. That looked tidy and was wrong: where no light reaches, both terms become exactly zero, so every unlit face compares equal, greedy meshing merges the lot, and the underground loses its ambient occlusion entirely. Nothing errored — but geometry fell from 2,829,100 to 1,487,958 triangles, which is a 47% drop that no lighting change should ever cause. Shading now rides in the spare blue channel instead. **Watch the counter after any meshing change; a large move in either direction is a bug until explained.**
- **Two queues sharing one budget is a starvation bug waiting for the right conditions.** Sky light propagated before block light, and both stopped when the frame budget ran out. Sky light is refilled every frame by chunk streaming, so while moving, block light could simply never run — a placed torch that lights nothing, intermittently, depending on whether you were walking. Block light now goes first: its queue is short, so it costs sky light almost nothing, and it can no longer be crowded out. **Ordering two consumers of one budget is a correctness decision, not a performance one.**
- **Shade came from a narrower predicate, not from a new number.** Making leaves attenuate light looked like it needed a per-block absorption value. It did not: sky light already falls for free only until something stops it, so splitting `isSkyTransparent` out of `isLightTransparent` was enough — leaves let light through but break the free fall, and everything below dims normally. **Check whether an existing rule can be narrowed before adding a quantity to tune.**
- **Foliage is the one place where hiding shared faces is wrong.** Culling the faces between two leaf blocks is the standard optimisation and it makes a canopy a hollow shell — every hole shows sky rather than more leaves, which the user spotted immediately. Leaves also need to be **double-sided**, or looking through a hole shows straight past the block instead of the inside of its far face. Both together cost +6.9% geometry, which is worth it. The trap: with shared faces kept, *both* neighbours emit the boundary, leaving coplanar quads at identical depth. Emitting it only from the positive-facing side saved 4% and removes the depth conflict, and costs nothing because the quad is double-sided anyway.
- **Pixel counts prove a structure the way triangle counts prove a mesh.** The corrected table was verified by counting bands rather than by looking: 20 rim pixels (four 5-pixel corner diagonals), 24 border, 64 grid (a 10×10 box minus nine 2×2 cells), 108 body, 28 dark + 12 plank in the cut corners. Every one matched the reference exactly, which settles "is the geometry right?" before any judgement about colour. `tools/dump-pixels.ps1` exists for this — it was built on the third occasion that eyeballing a magnified sheet missed something, after "all lines and stuff" and the circular corners.
- **Derive an outline from where the neighbouring rows stop, not by drawing it.** Both the furnace flame and the charcoal lump are described as a first and last lit column per row, with the rim computed from whether the row above and below cover each pixel. That produces a clean one-pixel rim on every side including the diagonals, and it cannot be drawn thicker on one side by accident — which is what a hand-authored character map kept doing.
- **A shared helper's hardcoded constant is tuned for whoever wrote it first.** `Set-Dithered` nudged each channel by a fixed +/-20 regardless of its `spread` parameter, which suited the Bramble's dense noise and turned a nearly flat pig into vertical colour static. Anything a caller can dial should scale everything, not some of it.
- **Sky light is seeded once per column and must be un-seeded when the column unloads.** `m_litColumns` recorded which columns had been lit and never forgot, so lowering render distance and raising it again brought chunks back that skipped light propagation entirely and rendered black. Any "have I done this yet?" set that guards expensive work needs a matching erase wherever the work is thrown away.
- **Anything that falls under gravity must first ask whether the ground is loaded.** An absent chunk reads as *air*, so creatures standing over unloaded terrain fell out of the world and reappeared buried when it returned. `World::columnResident` exists for this. Dropped items have the same exposure and have not been fixed.
- **A block without a border has no silhouette, and the user caught it before I did.** Every reference block face carries a 1px darker frame: `furnace_top` and `furnace_side` have byte-identical top and bottom border rows, and the body never goes below luminance 92 while the border sits at 59-79. Without it the block dissolves into the terrain behind it. The count is the check — a 16×16 perimeter is exactly `16 × 4 − 4 = 60` pixels.
- **Two blocks that share a position can disagree, and the saved one wins.** A furnace's lit state is in the block id and its contents are in `furnaces.dat`; bumping that file's version orphaned a lit furnace, which stayed burning with nothing behind it. Whenever state is split between a block and a side table, decide what happens when only one of them survives — here, opening it creates a fresh entry and the next tick puts it out, which is good enough and is written down rather than left to be rediscovered.
- **A block that needs to remember things is a block entity, and it does not belong in `World`.** The furnace is the first of these. Its *lit* state stayed in the block id, where water levels and stair facings already live, because the lit face and the light it casts are properties of the block itself. Its *contents* could not: 233 spare ids cannot hold an inventory. They live in a position-keyed map outside `World` entirely, because chunks are loaded and saved on worker threads and there is no reason to thread block-entity data through that — the whole class of ordering bugs that would buy is not worth it for something a player places a handful of. The cost, written down rather than discovered later: they save when the world saves, not when a chunk unloads.
- **A part that looks like a head is not necessarily a head, and the fix is to ask what a quarter turn does to it.** The wolf's 8×6×7 mane stood upright is a broad slab that swallows the head; laid down the same quarter turn as the torso it becomes the tall narrow shoulder ruff that is the difference between a wolf and a dog. The reference rotates it, and the tell was that the head had nowhere to sit. **When a box makes its neighbour look wrong, try rotating the box rather than moving the neighbour.**
- **An interior face cannot be lit from its own cell, and the bug was three milestones old before anyone saw it.** The partial-shape mesher lit every face from `lightOf({x,y,z})` — the block's *own* cell, which is solid, so light never propagates into it and it reads 0. For a face flush with the cell wall that is harmless because the sampling steps outward first; for an **interior** face — a stair's step, a slab's top, a fence rail — it is pure black. It had affected slabs since M17b and fences since M17d, and nobody reported it because a dark stair edge reads as shading. Interior faces now light from the cell they face, falling back to the brightest of six neighbours if that is opaque too. **The check that it changed nothing else: the triangle count must not move**, because this touches vertex colours and no face counts at all.
- **An animated texture needs no re-meshing if the layer is swapped at draw time.** Water is meshed once with `TextureLayer::Water`, and a spare push-constant slot names the layer to redirect and the frame to use, so the fragment shader substitutes it. Rebuilding chunks to animate a surface would have been the obvious approach and would have cost a full re-mesh several times a second.
- **The triangle counter is only an invariant when the world *and* the camera are identical, and neither is guaranteed across runs.** A 65k drop after a meshing change looked damning; two runs of the *same* binary then gave 282k and 157k, because both were sampled with `pending 372` chunks still streaming. The real explanation was better than a regression: the run in between had saved four modified chunks, which was the new fluid rules deleting the fan of water the old ones had left in the saved world. **Check `pending` is zero and the world is unchanged before comparing the number to anything.** And check the player has not *moved* - an idle soak drifted 80 m and rose 36 m, because the saved position was standing in flowing water and the current carried it into a wall it then auto-climbed. Both behaviours are faithful; the measurement was not.
- **Creature physics had no frame-time clamp where the player's has had one all along.** The resolver snaps out of at most one block, so a chunk-loading hitch was enough to drop a body through the floor. Ported the player's `min(dt, 0.05)`.
- **A ratio whose denominator is also still growing reads high from the first frame.** Meshing progress was meshed-over-*loaded* chunks, so two chunks in with one meshed reported "half drawn" - and it could never reach 1 either, because the outer ring is loaded deliberately and never meshed. Counting over the **visible box**, a denominator fixed by the render distance, makes it start at zero, end at one, and mean something in between. **A progress fraction needs a denominator that is known up front.**
- **The measurable check for "did the loading screen finish too early" is the `pending` counter on the first log line.** It read `pending 372` and `pending 498` before the fix and `pending 0` after, which is far better evidence than watching chunks appear. The counters exist for this; read them before reaching for a stopwatch.
- **A strict gate needs a bail-out that cannot mask the thing it is gating.** Waiting for every visible chunk to be meshed can in principle hang forever. The safe release is not a timeout but a *contradiction*: if every queue is empty while the checkpoints still disagree, nothing will ever move them, so waiting is pointless. An ordinary slow load always has work outstanding and can never trip it.

## Physics, collision and movement

- ⛔⛔ **The fall accumulator is cleared in some places and not its siblings — the rule that did not travel, third instance.** `fallDistance` is zeroed by water, by a ladder and by a vine, and was zeroed by **neither slime nor Slow Falling**. Slime returns most of the speed you arrived with, so the accumulator survived every bounce and each descent between them piled on top of it: a **40-block drop onto slime was lethal**, which is the exact opposite of what the block is for, and it had been that way since slime was added. Slow Falling has to clear it on **every descending tick** rather than veto the damage at the landing, or a potion that lapses six blocks up charges you for the whole sixty — the effect looked correct in a full-length test and killed you in a short one. And a bed's halving has to be charged **at the impact**, not applied to the accumulator, or it halves once per bounce and a tall drop costs a quarter of what it should. All three now go through one `chargeFall` in `Player.cpp`, which is the only place that turns a fall into damage. `CLAUDE.md` #14 is the general shape: a guard that is correct, commented and present in only one of the places that need it.

- ⛔ **A fixed simulation step needs render interpolation, or it looks broken at any frame rate above it.** Projectiles tick at the reference's twenty a second, which is load-bearing — every published constant is per tick and four `static_assert`s depend on it. But `buildMesh` drew them *at* the tick, so a 120 fps screen showed each position six frames running and the user reported the arc as "very very rough... the amount of frames it shows while moving aren't many". The fix is entirely in the renderer: keep each shot's previous tick position and heading, and blend toward the current pair by `accumulator / tickLength`. One tick of latency, invisible, simulation untouched. **The instinct to raise the tick rate instead would have quietly falsified every number in the table.**
- **Measure smoothness rather than eyeballing it.** A temporary probe logged the drawn x of the first vertex each frame: before, the same value six frames running; after, a new value every frame in proportion to the frame time. That is a two-line check for a class of bug that is otherwise pure judgement.

- ⛔ **A per-tick reference velocity is not a sustained velocity.** Blast knockback took the reference's one block per tick at point blank, multiplied by twenty to reach blocks per second, and assigned it. That is arithmetically the same *initial* speed and nothing like the same shove: the reference decays it by 9% every tick, and our player keeps whatever horizontal speed it left the ground with until it lands. The user's word was "crazy". **When porting an impulse, port what it does, not what it starts at** - this is the unit trap in `CLAUDE.md` wearing its third hat.
- ⛔ **A shape that depends on its neighbours cannot be answered from an id.** `collisionBoxes(pane)` assumed every arm, because that is all it can do with an id alone - so a lone pane of glass, drawn as a two-texel post, was collided with and aimed at as a full cross. The user reported it the same way they had reported the ladder: an invisible shell round a thin thing. `connectionBits` is now the one owner of the reaching rule and `worldCollisionBoxes`/`worldSelectionBoxes` ask it, so what is drawn, what you bump into and what the crosshair picks out are one answer. **Whenever collision and the mesher disagree about a shape, the mesher is the one that can see the neighbours.**

- ⛔⛔ **A system whose numbers are published per *tick* must run on a fixed tick, not on `deltaSeconds`.** Every projectile figure the reference gives is per tick at 20 Hz. `velocity *= 0.99` once a frame at 120 fps is **six times** the intended drag, and no amount of `pow(inertia, dt * 20)` fixes the interleaving with gravity and the position update. `Projectiles::update` runs a fixed accumulator, and the payoff is the cheapest verification available: at a fixed step the closed form `v(t) = k^t (v₀ + g/(1−k)) − g/(1−k)` holds *exactly*, and a probe measured the drift at 1e-6 over forty ticks. **A ported number in the wrong unit compiles, validates, and is silently wrong** — the `panicSpeedScale` lesson, one layer deeper.
- ⭐ **A published side-effect can prove an update order.** Drag-before-gravity gives a terminal speed of `g/(1−k)`; gravity-before-drag gives `k·g/(1−k)`. For an arrow those are 5.00 and 4.95 blocks a tick, and the reference publishes **5.00 for arrows and 4.95 for thrown potions** — two families, two orders, both documented. A `static_assert` on that one number now pins the order. **When two plausible implementations differ, look for a number that differs with them.**
- ⛔ **Sweep the segment; never test the endpoint cell.** An arrow moves three blocks a tick. A per-axis resolve at where it ended up — which is exactly what the dropped-item entity does, correctly, at a fraction of a block a frame — flies straight through walls. And the fix is *not* substepping: `if (speed > 1) subdivide` is a second collision rule that will eventually disagree with the first. Sweep once, over the whole segment, and tunnelling is impossible by construction.
- **Blocks before entities, and clamp the segment between them.** Asking what is in the way and then what blocked it lets a projectile hit something standing behind a wall. The reference's order is: sweep blocks, move the far end of the segment to the hit point, *then* look for entities inside what is left.
- **A projectile's damage is a function of the speed it is doing now, not the speed it left at.** The reference stores no damage on an arrow at all — `impact_damage.damage` is zero and a `power_multiplier` does the whole job — which is why a long shot lands softer and one through water barely lands. Storing the number at launch is the obvious implementation and the wrong one.
- **A block with no collision box needs its selection box back, or it cannot be broken.** A ladder collides with nothing on purpose; `selectionBoxes` falls through to `collisionBoxes` for everything that is not a plant, so a ladder aimed at reported the wall behind it. The same one-line branch also has to be mirrored in the mesher, which cannot draw a shape with no boxes.
- **A predicate that must fire for a block with no collision has to test whole cells.** `Player::onLadder` asks whether any cell the body occupies *is* a ladder, deliberately not whether the body overlaps its geometry — there is no geometry. Testing the shape would have been the natural thing to write and would never have returned true.
- **Knockback assigned to something that owns its own velocity does nothing useful and looks absurd.** `strike` sets `target.velocity` outright. For a walker that is a shove gravity then resolves; for a flier, whose locomotion branch rewrites all three axes every frame from its heading, the assignment is either fought immediately or — on the vertical — reads as the animal being thrown across the sky. Anything that owns its own velocity has to be **exempted** from impulses, not given a smaller one.
- **A fluid needs a fixed point or it drains itself.** Every flowing cell asks "what supplies me?", and without something that answers "nothing does, and I am still here", the recursion bottoms out at empty. Level 0 water is that fixed point. The corollary is that generated oceans must be *sources*, not merely full cells, or the first hole a player digs quietly empties the sea.
- **A half-height block breaks collision in a way that loops rather than fails.** Adding slabs, the overlap test was updated to respect the shape's height but the *resolution* was not — it still snapped the feet to `floor(y) + 1`, the block boundary. So the player landed at 41.0 above a slab whose surface is 40.5, the ground probe found nothing beneath them, and they fell again: a permanent bounce, which is exactly how the user described it. **Overlap and resolution are two separate places that both encode the shape**, and fixing one without the other produces a cycle rather than a clean failure. Only downward motion needed it — a slab's sides and underside are still on integer planes.
- **"Not solid" quietly stopped meaning "air or water", and it deleted the new block type.** The fluid update skipped solid cells and treated everything else as a fluid candidate, so a cell with no water supply was rewritten to air. That was harmless while every block was a full cube or water. Tall grass is neither, so **every placed plant vanished the same frame** — `setBlock` schedules a fluid update around each edit, while generation does not, which is why generated grass survived and placed grass did not. The fix was to test for air-or-water explicitly. **When a predicate is used as a proxy for a closed set of cases, adding a case to that set breaks every user of the proxy silently** — grep for `isSolid`, `isOpaque` and friends after widening what a block can be.
- **Adding one non-cube shape broke five separate systems, one at a time, and every one was found by playing.** Collision resolution snapped to the block boundary; the fluid update deleted plants; the targeting outline drew a full cage; the raycast treated cell entry as a hit, so aiming at a slab's top from a distance placed the next slab *beside* it; and the outline flashed a full-size cage for one frame after a block was broken, because it read the shape from a ray hit resolved *before* that frame's edits. Each was the same root cause wearing a different hat: **a shape derived somewhere other than the shape table**. The fix that finally stopped the bleeding was making `collisionBoxes` the one definition every system reads. Expect this pattern whenever a long-held invariant is widened — the compiler cannot help, because nothing changed type.
- **That same root cause came back twice more, and the second time proves the first fix was incomplete.** The slab bounce was fixed by teaching *downward* collision resolution to read the shape table. Horizontal resolution was left snapping to `floor(x)`, which nobody noticed because every obstacle up to that point filled its cell horizontally. Stairs do not: the upper step starts halfway across. Walking into one from the high side resolved the player back to the **cell** boundary rather than the **step** face — a teleport of most of a block, which is exactly what the user reported. The lesson is not "fix resolution for stairs"; it is that **overlap and resolution must read the same table on every axis, and fixing one axis is not fixing the bug.** `blockingPlaneAlong` now answers "what face stopped me" for all three.
- **The one place that rule legitimately bends is selection versus collision.** Tall grass has to be walked through *and* be breakable, so it collides with nothing and selects as a slim column. Resist splitting any further: two shape tables is the most a block should need, and the second one earned its place by having a real second case rather than an anticipated one.
- **A bob faked over a constant glide is not a hop, and the physics to do it properly was already there.** The rabbit slid forward at a fixed speed with `sin(gait)` added to its height — which the user called highly unrealistic, correctly, because no part of it was a jump. Replacing it with a real launch velocity and letting the existing gravity and collision produce the arc was *less* code and gave airtime, hop length and arc height for free. The one number that has to be derived is the burst speed: all the ground is covered in the air, so it must exceed the species' average by exactly the fraction of the cycle spent landed. The pose then falls out of `velocity.y` rather than a counter — positive is push-off, zero the apex, negative the descent — which is why the legs extend at both ends of the arc and fold at the top without any of that being authored.
- **Minecraft's numbers are per-tick and ours are per-second; the conversion is not a guess.** `RESEARCH.md` §1.2 has the table. Velocity ×20, acceleration ×400, and drag becomes `d^(Δt/0.05)` — exact at 50 ms and stable at any frame rate. **We have three separate gravity constants and they are deliberately different:** the player's 32 m/s² (`Player.hpp`) matches the reference exactly, creatures use 26 (`Creature.cpp`) so they arc more gently, and dropped items use 22 (`ItemEntity.cpp`) so they settle softly. Do not "unify" them. Anything ported from a wiki page needs the conversion applied or it will be 20× or 400× wrong.
- **A physics constant with a stated derivation can still have the derivation wrong, and the check is a value you already trust.** All four of the water vertical terminals were quoted as coming from the reference's `±0.04` impulse and shipped roughly a quarter too fast, because the water-gravity term was dropped from them — so the surface bob threw the player much further out of the water than it should. The tell was free and was sitting in the same file: **the same expression with no impulse has to reproduce `kSinkSpeed`**, which is measured and correct, and it did not. A published measurement then confirmed the sprint figure to within 3%. When a group of constants share one formula, derive the one you are sure of and check it before trusting the rest.
- **The same control doing two jobs at one speed is what made the water bob violent, and it took three attempts because each fix was aimed at the wrong half.** Holding jump is a **climb** while your eyes are under and a **stroke** once they are out, and both were running at the full swim-up speed - so the player arrived at the surface still doing 2.7 m/s and coasted most of a body length clear. A stroke only has to beat the sink. The user's words after the second attempt were _"my highest point is my entire body above the water, my lowest point is i think half my body underwater ... don't half ass it"_, and they were right to be annoyed.
- **Water tuning is worth simulating rather than playtesting, and it is the rare case where automation beats handing over a build.** The vertical motion is a pure function of the constants, so `tools/simulate-swim.ps1` replays it and prints the bob, the eye height and how much of the body is out. It caught a case the eye would have missed for days: **sprint-swimming settled motionless half a metre up**, because sprint-swimming skips water gravity and that rule is about holding depth *with your head under* - at the surface it meant nothing brought the player back down. The tool **reads every constant out of the headers instead of copying them**, because a second copy of a number that already has a home is this project's most repeated bug. It also let the float height be *derived* rather than judged: `kFloatEye` is the value at which floating in deep water and standing in a single block of it come out identical, which is what makes a shallow pool feel like water instead of like a floor.
- **One threshold settles just as surely as none, and the gap between two is what makes an oscillation.** Holding jump at the water's surface decayed into standing on top of the sea with water physics still running - because the swim-up drive was gated on *any part of the body* being wet, which balances with the **feet** at the surface and a body entirely out of the water. Two fixes in one: aim the kick at the **head**, which puts the body where a treading swimmer's actually is, and latch it between a high and a low mark so it cuts out once the eyes are clear and resumes only after they have sunk back. Neither alone is enough - a single head-height threshold creeps to a stop in exactly the same way, just lower down. Keeping the low mark above zero is what stops the head dipping under on every cycle, which would have flickered the fog and eaten breath.
- **A control that is flattened for one case has to be un-flattened for the other, not left broken.** `moveDirection` drops its vertical component so that looking down cannot walk you into the ground - correct for walking, and it left sprint-swimming able only to skim, so a dive was no quicker than paddling. The fix is one scalar (`PlayerInput::lookY`) read by that one case, with the speed split between horizontal and vertical by the pitch so the total does not change with where you point.
- **A model that eases toward a target speed behaves differently from one that only applies drag, and the difference shows up wherever the two meet.** Bobbing at the water's surface lifts the whole body clear for about a sixth of a second, and our airborne branch spent it accelerating toward the *walking* speed — so swimming forward came out as a lurch on every bounce, which is what the user reported as "the bouncing is weird". The reference has no target speed in the air at all. Patched where it shows rather than rewritten, because the target speed is what makes ordinary movement feel right and only water exposes it.
- **A missing physical response reads as the physics being broken, and the honest fix was not the reference's.** Bedrock separates a slime from the player with `pushable`, which we have no equivalent of at all — so a slime simply passed through and re-entered forever. The user asked for the player's body to be a wall with the slime rebounding off it, which is *better* than the reference here rather than a compromise, and it costs one reflection: keep the tangential velocity, send the inward part back out at a restitution, lift it clear of the floor. **It needs no cooldown**, because after it fires the slime is heading away and the test cannot pass again. Two things that were not obvious: it has to fire on **contact rather than on the blow**, since a small slime does no damage in the reference and a wall does not care; and it has to **mark the creature airborne**, because the hop branch zeroes horizontal velocity on anything it still believes is standing, so a ground-level bounce came out as a hop straight up with the recoil silently discarded.
- **Two questions that both sound like "am I there yet" needed two different tests.** *Arrived* is about footing — the vertical gap within the creature's own `stepHeight`, because anything more is something to climb and both the step-up and the jump read `walking`. *Touching* is about bodies overlapping, and it is **asymmetric**, because the player is 1.8 m tall and a slime sailing over their boots is still hitting them. One symmetric `abs(dy) < height` served neither: it froze a zombie at the foot of a ledge *and* let a slime pass overhead untouched.
- **"Which axes failed to move" is not a test for being blocked, and it silently disables anything built on it.** The jump first fired on `!movedX && !movedZ`, which is never true against a wall: one axis is blocked and the other slides along it freely. Worse, a heading near an axis makes one component tiny, and a 3 cm step rarely overlaps anything, so that axis reports success while the creature is pressed flat against a block. **Probe ahead instead** - foot height blocked, step height still blocked, jump height clear - which is three questions about the world rather than an inference from a resolver. The spiders' wall-climb had been sitting on the same broken test since M20b and only worked diagonally.
- **Adding a movement ability means checking the thing that *chooses* headings, not just the thing that executes them.** The steering fan rejected any heading blocked at foot height, so creatures would have routed around every one-block rise and the new jump could never have fired once. A locomotion ability and the planner that feeds it have to agree on what counts as passable, and the planner is the half nobody remembers.

- **Converting a per-tick *drag* is not the same as converting a per-tick rate, and the naive form silently changes the answer.** A chicken's descent is scaled by 0.6 every tick, which settles at 1.95 m/s. Applying 0.6 per *frame* — or even `0.6^(dt/0.05)`, which looks like the right conversion — lands at 2.55 m/s at 120 fps, because `v = (v - g·dt)·k` has fixed point `g·dt·k/(1-k)` and both `dt` and `k` moved. **Solve for the factor that preserves the fixed point instead**: `k = terminal/(terminal + g·dt)`, which reduces to exactly the reference's constant when a frame happens to be a tick. Any drag ported from a per-tick source needs this; a rate (probability, acceleration) does not.
- **"How high it can step" and "how far it should rise" are different numbers, and using the first for the second turns a step into a teleport.** The step-up lifted by the species' whole `stepHeight`, so everything overshot a slab or a stair by the difference and dropped back under gravity, and the long-legged - horse, llama, frog at 1.0, camel at 1.5 - snapped a full block or more instantly, which is what the user saw. The distance to rise is a fact about the block being climbed and the *world* owns it; `highestSurfaceBelow` already answers it and is what landing uses. Same shape as every other bug in this file: **a value derived somewhere other than the table that owns it.**
- **A tool that writes `settings.cfg` and does not restore it produces a bug report about something else entirely, and this is the third time.** A soak set `spawn_underground=1` to test hostiles and left it there; the user launched the debug build, spawned in a flooded cave, and reported *"gravity is broken, it takes ages to fall"*. Nothing was wrong with gravity - they were swimming, at the 0.22 gravity scale and 3 m/s sink cap. The rule was already written down twice from `benchmark.ps1` leaving `frame_cap=0` behind. **Back up and restore in the same command that mutates, not in a later one** - the gap between the two is where the user launches the game. And when a bug report arrives right after a soak, check what the soak left behind *before* reading any code.
- **A physics state with no visual is indistinguishable from a physics bug.** Water had no underwater view at all: from outside it renders, and the instant your eye goes under, nothing changes except how you move. The user could only describe the symptom as gravity, which sent me through the whole collision resolver looking for a fault that was not there. **When a state changes how something behaves, ask what tells the player it is in that state** - and the fix was one full-screen tinted quad reusing the inventory's existing dim, which is the second time a "new rendering feature" turned out to be an arrangement the renderer already had.
- **A physics fudge that "works" is a fudge that nobody researched, and it will feel wrong in a way the player cannot name.** Water shipped as gravity × 0.22 with a 3 m/s sink cap and horizontal speed × 0.55. Every number was invented, none had a source, and the user's report was that swimming *felt like falling slowly* - which is precisely what a capped fall is. **The reference does not cap anything in water: it multiplies velocity by a drag factor every tick and adds a fixed impulse**, and that one structural difference is the whole of the feel. Sinking, swimming, sprint-swimming, the plunge after a dive and being carried by a current then stop being five features and become one expression with a different terminal in each. Fifteen minutes of research replaced four tuning constants with a model. **When a system's constants have no source, that is the bug report.**
- **Quote a drag system by its terminal speed, not by its impulse, and the frame-rate conversion stops being a problem to solve.** `v = (v − g·Δt)·k` moves its own fixed point when both `Δt` and `k` change, which is the trap the chicken's slow fall sprang once and had to be re-solved for. Writing it as `v = v·k + terminal·(1 − k)` with `k = d^(Δt/0.05)` is exact at *any* frame rate by construction, needs no re-solve, and reduces to the reference's own arithmetic when a frame happens to be a tick. Everything in `world/Fluid.hpp` is quoted that way for exactly this reason. **Reach for the form that cannot be wrong before reaching for the correction.**
- **Floating is a behaviour, not buoyancy, and the reference is unusually clear about it.** Nothing pushes a swimming player or mob upward. A player bobs because *they are holding jump*; a cow bobs because Bedrock gives it `minecraft:behavior.float`, an AI goal that swims up while its head is under. `can_sink: false` is a *third* thing - neutral buoyancy, which holds depth and never surfaces, which is why a squid hangs mid-water and never bobs. Conflating the three would have made every animal a cork. **Real buoyancy does exist as `minecraft:buoyant`** - and the entities that carry it are boats, XP orbs and **dropped items**, not the player. That is why something lost in a lake washes up.
- **A wiki figure that no combination of constants reproduces is probably a copy error, and saying so beats tuning to it.** The Transportation page lists sinking at 0.39 m/s; the constants give 0.50 and nothing produces 0.39. The same 0.39 appears on the Swimming page as the *horizontal* speed of swimming upstream. Chasing it would have meant inventing a drag value to hit a number that was never a sink rate. The four figures that *do* reproduce - 1.960, 3.918, 5.305 and 1.39 - agree to within 0.05%, and agreement that tight is the evidence that the model is right. **Check whether a target number is derivable before treating a mismatch as your error.**
- **`Mojang/bedrock-samples` does not contain everything, and knowing what it is missing saves a fruitless search.** It is a *behaviour pack*: mobs, items, recipes, spawn rules. It holds **no fluid data at all** and `player.json` has no water physics - only a collision box, a movement scalar, breathing and exhaustion. So the fluid constants genuinely have to come from the wiki plus the Java implementation, and the honest thing is to mark which is which. It *is* authoritative for `minecraft:breathable`, `minecraft:navigation.*` and `behavior.float`, and those settled the entire mob half in one pass.
- **Fixing how an entity moves through water is not the same job as fixing how the water moves, and shipping the first while calling the section done left the obvious half broken.** `RESEARCH.md` §9.1 documented the fluid *block* and its own note pointed only at the entity gap, so the entity half got built and the block half was never read against the code. It had been wrong since M15c: water above meant "nearly full", nothing checked whether a cell could fall before letting it run sideways, and there was no weight search at all - so a source on a tower fanned into a cone at every level on the way down. **A section that documents a system correctly is not evidence the system matches it.** When a doc has a "where we stand" note, write one for the whole section, not for the part just worked on.
- **The rule that makes a waterfall a column is a negative one, and it is easy to write the wrong one.** The instinct is "falling water spreads only downward", which Bedrock's own block-state docs say - and it is wrong, because then a fall would never pool at the bottom. The real rule is **"a cell that *can* drop does not spread sideways at all"**, applied to every cell. Mid-air cells all have air beneath them so none of them feeds a neighbour; the one that lands has nowhere to go and pools. The falling *flag* is doing a different job entirely: it marks the cell **full**, which is what gives the landing pool its seventh block.
- **"Can I drain downward" and "do I pool and spread" are two questions, and answering both with one predicate dead-ends every stream.** A cell resting on water can do *neither*: it is partway down a column, not the bottom of one. The first cut asked only "is there water or air below", which correctly kept a waterfall one block wide and then **stopped a flow permanently at the first dip it found** - once the hole filled, that direction still scored as a hole, so the weight search kept steering everything into it and nothing spread past. The fix is Bedrock's own split: drain-into needs a cell that can *receive* water (air, or a plant), while spreading sideways needs **solid ground** underfoot - with a source as the one exception, which is how a lake has a surface at all.
- **A predicate written as "is this passable?" silently became "is this a wall?" for anything new.** Water could not flow into a cell holding tall grass or a flower, so on a meadow every spread came out full of holes and looked directional. The reference **washes plants away and carries them off in the flow**, which is both the correct behaviour and the thing that makes water on grassland look like water. `World` has no idea items exist, so it records what it destroyed and the owning loop turns that into drops - the same split the mesh/upload and generation/apply boundaries already use.
- **Applying gravity and then a drag model in the same frame moves the settling speed by metres per second, and it landed in three places at once.** `v = (v - g dt) k + terminal (1 - k)` settles at `terminal - g dt k / (1 - k)`, which at 120 fps is about **5 m/s of unwanted sink** - so a cow that should bob sank like a stone and a dropped item that should float went straight to the bottom, while every constant in the table still read correct. The player's branch happened to be written as an `else` and was fine; creatures and drops both applied gravity first and then "corrected" it. **A drag model must *replace* the acceleration it is standing in for, never follow it.**
- **A spawn test that re-derives the body's shape will drop things into terrain, and once inside nothing gets them out.** `canStandAt` scanned a column of whole cells `ceil(height)` tall - a second copy of what `collisionBoxes` already owns, blind to a slab, a fence, or a body wider than the cell it stands on. It is now one `overlapsSolid` call on the real box. **The same bug shape as slabs breaking collision, arriving in the spawner.**
- **`Mojang/bedrock-samples` has a `resource_pack/fogs/` folder, and it answers "what does underwater look like" exactly.** Every biome ships a `*_fog_setting.json` with the water fog's colour, start, end and the transition that opens your visibility as your eyes adjust. Two things in it would never have been guessed: `render_distance_type: "fixed"` means the 60 is **metres** rather than a fraction of the render distance, and `fog_start: 0` is why even something close up carries a little of the colour. **The ocean colour `#1165b0` and the general default `#44AFF5` are wildly different** - the default is nearly cyan - so "the water fog colour" is not one number and picking the wrong one looks nothing like the screenshots.
- **A "more physical than the reference" idea was built, hated on sight, and reverted - and the reason it failed is worth more than the idea was.** Bedrock decides underwater fog on one bit: is the camera in a water cell. That means one block of water overhead murks the world, which sounds obviously wrong, so it got replaced with two measurements - how far water reached around the eye, and how much of each view ray lay below the surface. Both are defensible in isolation and together they were **worse in the ordinary case**: dipping your head at the shoreline left a band of "disgusting blue" hanging out over the open ocean, because the two factors disagreed about the same pixel. **The crude rule is crude on purpose - it is the only one that stays consistent across a whole screen.** Do not rebuild this unprompted.
- **A faithful feature can be the thing that feels broken, and `transition_fog` was.** The reference opens underwater visibility from a quarter of the fog distance to all of it over thirty seconds as your eyes adjust. Implemented literally it restarts on every dip, so bobbing at a shoreline - which is most of how anyone meets water - means the density is different every single time you go under and never settles. Every reference screenshot the user produced showed the settled state and none showed the murk. **Removed, and the field that fed it (`Player::submergedSeconds`) removed with it**, because a value only one caller ever reads becomes dead weight the moment that caller goes.

- **`beginHead` rebuilds the frame from `renderPosition` alone, so any lift applied outside it is silently discarded.** The axolotl's model is raised by a constant so it does not sink into the seabed; every part respected that except the head, which dropped through the body the moment it was wrapped in a head group. Nothing warned, because the head was still *somewhere* plausible. **Any helper that reconstructs a transform from scratch will throw away whatever the caller had already applied** — check what the frame contained before wrapping something in one.
- **A body's angle comes from where it is *going*, not where it is facing.** The squid looked wrong for a whole round because it stayed bolt upright while swimming horizontally. Deriving the tilt from the velocity vector — `atan2(horizontal, vertical)`, eased — gives upright when still, flat when cruising and inverted when diving, from one line and with no states.
- **A spatial hash key for graph search must be exact, never a hash.** Two cells colliding in an `unordered_map` key silently splice into one node and the route jumps through solid rock, with nothing to catch it. Packing x and z into 21 bits and y into 12 is exact for a million blocks either side of the origin and a world 96 tall — and where it *would* alias is worth writing down rather than leaving to be discovered.

## Creatures: AI, spawning, behaviour

- ⛔⛔ **A rule written for creatures does not reach the player, and the player is the one nobody tests.** `Creature.cpp` has asked `world.columnResident` before simulating since the day it was written, and says why in its own comment: *"an absent chunk reads as air, so simulating over one drops the creature out of the world and it reappears buried when the chunk returns."* It has a push-out for a body that ends up inside rock, too. **`updatePlayer` had neither.** Respawning at a world spawn you have since walked away from is the way in — you are placed in a column that is not loaded, `highestSolid` answers −1, `−1 + 1` is the bottom of the world, and if you survive that you free-fall through empty air until the chunks arrive and bury you. The user's words were *"my spawn point is some random place underground inside of blocks"*, which is the creature comment almost verbatim, five milestones later. **When you write a guard for one kind of body, grep for every other thing that falls.**
- **Two hours of theorising were beaten by four bytes off the disk.** Every explanation for the above was plausible and most were wrong, and what settled it was decoding `player.dat` (surface, in a savanna, fine) and then generating the spawn chunk in a throwaway probe: topmost solid block y=35, a dirt path, and `surfaceHeightAt` agreeing exactly. That eliminated the loaded-save path and the fresh-spawn path in one run and left respawn as the only door. **When a report and the code disagree, decode the state rather than re-reading the code.**
- **A reach test measured against the *attacker's* height is wrong for anything not standing next to you, and the symptom was "bees don't fight back".** The blow's vertical gate read `abs(toPlayer.y) < species.height`. For two things on the same ground that is roughly the box overlap; for a bee it demands that a half-block-tall flier put its feet within half a block of yours, which a flier essentially never does — so it would chase, arrive, face you, and never once sting. Everything upstream was fine: the strike registered, the anger was set, the target flipped to Player. **The correct test was already in the same function**, three lines up: `touching` computes the real asymmetric overlap (`toPlayer.y < height && toPlayer.y > -1.8`) with a comment explaining that the player is 1.8 m tall. The attack gate simply was not reading it. When a creature does everything but land the blow, suspect the gate rather than the AI — and check whether the correct predicate is already sitting beside the wrong one.
- **A neutral that flies needed no aggression flag and had none missing.** `attackDamage > 0` plus `provokedTimer` is the whole of retaliation and it worked from the first build. Both bee "bugs" reported as aggression failures were movement and reach: it could not descend, because `FlyWander` owns the dive angle and is not running while `MeleeAttack` holds the movement controller, and it could not land the blow because of the gate above. **Neither would have been found by reading the targeting code**, which is exactly where "it is not aggressing" points you.
- **"Make it original" was a requirement nobody asked for, and it would have cost weeks.** The vision documents demanded original mechanics, creatures, recipes and progression, and I carried that into `TIMELINE.md` as a hard rule for Phase 5 without ever checking it against what the user wanted. They wanted a Minecraft-like game they *controlled*, not a new genre. **Ambition inherited from a document is not the same as a requirement from the person paying for it** — and this one was written by another AI (see the note at the top of this file), which is exactly the category of instruction that needed questioning rather than enforcing.
- **Guessing coordinates to reach a test location is not testing, it is stalling.** Verifying cave lighting meant rebuilding and flying for minutes per attempt, and three rounds of guessing spawn coordinates got nowhere and irritated the user. `spawn_x`, `spawn_z` and `spawn_underground` now live in `settings.cfg`; the last one *searches* the surrounding area for the most open cave rather than requiring a column known to contain one. **When a test is hard to reach, fix the reaching** — but reach for the smallest thing that works, and check whether one already exists before building tooling. The same trick found a tree: a throwaway startup scan logged the nearest `Leaves` block, which beat four rounds of guessing at where a forest might be.
- **And a third time, in a system that was never audited because it was written after the fix.** Both of the above were fixed inside `Player.cpp`, and `SYSTEM_MEMORY.md` was updated to say `collisionBoxes` is read "by physics". Creature physics, added three milestones later at M20a, quietly grew its own whole-cell `world.isSolid` test and snapped landings to `floor(y) + 1` — so every creature hovered half a block over a slab or a stair and could not step onto one at all. It produced no bounce, because its overlap test and its resolution were wrong in the *same* direction, which is precisely why nothing looked broken. **A claim in the docs that something has one owner is not a mechanism; the mechanism is a shared function.** `world/Collision.hpp` now holds `overlapsSolid` and `highestSurfaceBelow`, and the player and creatures both call it. **When a new subsystem needs an answer an existing one already has, take the function, do not re-derive the answer** — and when fixing an invariant, grep for every other place that asks the same question rather than fixing the site that reported the bug.
- **A regex that deletes a line deletes whatever else is on it.** Removing a temporary `logInfo` from the creature spawner also removed the `return;` that a previous regex had put on the *same line*, so one spawn attempt became eight and the population overshot its cap by two. It compiled, ran, and looked almost right — the only symptom was a number that should have been 14 reading 16. **Two rules: never let a scripted edit put two statements on one line, and when a count is near but not equal to its constant, that is a bug and not rounding.**
- **"I cannot see it" is not evidence a feature is broken.** Half an hour went into hunting for creatures that were spawning correctly the whole time — the giveaway was already on screen, because the per-second triangle count rose by exactly 144 each time one appeared, and triangles are counted while *drawing*. Reading that number would have settled it immediately. **Add the count to the log before searching the world for the thing you are counting.**
- **A gate that is correct can still be a bad design, and the difference is whether anyone would call it a bug.** Restricting the grazing animals to plains and beaches was faithful to the idea and made the desert — where the world happens to spawn you — lifeless by day. Nothing was broken; it simply looked broken, which for a player is the same thing. Deserts got them. **When a rule makes a place empty, ask whether emptiness is the point or an accident of the table.**
- **The camera and the creatures do not share a yaw convention, and nothing warns you.** `Camera::forward()` is `(cos yaw, sin pitch, sin yaw)`; a creature's is `(sin yaw, 0, cos yaw)`. They are 90° apart, so a showcase laid out along the camera's axis appeared entirely off screen and read as "the creatures did not spawn" — the census in the per-second log said otherwise. Check the census before hunting for missing geometry.
- **The three-state switch in `Creature::think` was replaced at M20c (2026-08-04), and the two ideas that carried it are worth keeping in mind before adding any behaviour.** **Control flags** — three bits, Move/Look/Jump — so a chicken walks and watches at once with no special-casing, and **a row claiming nothing always runs**, which is how targeting works rather than a degenerate case. And **target-production split from target-consumption**: one row writes `target`, attack rows read it, which turned retaliation, pack anger and hunting-on-sight into rows that never mention each other. `RESEARCH.md` §8 records Bedrock's answer and §8.8 the migration; step 5 (unify the two spawners) is the only part still open. Do not port the JSON, the component groups, or a filter language — those exist because Mojang ships to third-party authors, and the component-group system carries a documented "removal resets to default" landmine.
- **Spawning animals at chunk generation was the highest value-per-line item on the list, and it is now built.** The reference runs *two* spawn systems: the continuous cycle near the player and a one-off pass at chunk generation that **ignores the mob cap entirely** and is a pure function of `(seed, chunkCoord)`. That is exactly the shape our generation already had, so it cost no threading risk. It is most of why a world now feels inhabited on arrival rather than trickling in behind you. **`m_populated` only ever grows, deliberately** — a chunk gets its herd once per session, and forgetting it on unload would breed animals out of walking back and forth, which is the same shape as the `m_litColumns` bug in reverse.
- **A creature can need the translucent pass, and the slime is the first.** Its shell is gel over an opaque core carrying the face, so `Creatures::buildMesh` now fills two meshes and the shell is added with `addMesh(..., true)`. Putting it in the opaque mesh instead would blend it against whatever was already in the framebuffer rather than against its own core — the exact lesson water taught at M15c, arriving again on an entity. The first attempt dodged the whole problem by drawing only the shell with the face mounted proud of its front, and the user rejected it on sight as blobs glued to a green box. **When a reference model has a layer you cannot render, check whether the renderer already has the pass before designing around the limitation** — this one had, since M15c.
- **A behaviour that already works as a branch is the easiest thing to move, and the hardest to notice you have broken.** Retaliation, the six-second grudge, `huntsBelowLight` and herd alerting all exist today as special cases inside `Creatures::think` and `strike`. The M20c restructure does not *add* any of them; it relocates them into table rows, which means **the acceptance test is that nothing changes** — a wolf must still ignore you until struck, a spider must still hunt only in the dark. Behaviour that is identical before and after is far easier to verify than behaviour that is new, so port the existing cases first and add `LookAtPlayer` only once they are provably unchanged.
- **A chase with no arrival condition reads as *orbiting*, and the symptom is three steps from the cause.** `MeleeAttack` steered at the player every tick for as long as it had a target, and nothing here collides with the player — so a zombie walked straight through, overshot, and had to come all the way about at a limited turn rate. That is a circle, and because reach is a distance the player sat inside it on every pass and took a hit each time. The user's words were *"attack me by walking through me, then walks around me in circles"*. **The reference does not solve this with a turn rate or a reach; it solves it with a destination** — `melee_box_attack` paths to a node *beside* the target and the path ends there. Anything that closes on something has to say where it stops, and **"it stops when it can hit" is not the same distance as "it stops when it arrives"**.
- **`walking` is sticky, and that is the second time it has bitten.** Claiming the movement controller only stops anything else *steering*; the flag survives from whichever behaviour set it last. The Bramble's fuse learned this once and the melee arrival had to learn it again — anything that stops has to say so outright.
- **A behaviour that claims no controller at all is not a degenerate case — it is the mechanism.** Bedrock's targeting goals take an empty `control_flags`, which reads like an oversight and is the whole design: because they can never conflict, they always get to run, so "decide what to attack" and "attack it" stop competing. That single property is what turns retaliation, pack anger and hunting-on-sight from three branches into three rows that never mention each other. Do not "tidy" a flagless row by giving it `Move`.
- **`canContinue` exists because *why something keeps going* is a looser question than *why it started*.** Our old hunter hysteresis — engage inside `senseRange`, give up outside `senseRange * 1.4` — was an `if`/`else if` pair inside a mood block, and it fell out for free as two predicates on one row. Whenever a state machine has an asymmetric entry and exit condition, that asymmetry is the shape a behaviour table already has a slot for.
- **A defaulted field applies silently, and "everything nocturnal burns off" was never true.** `CreatureSpecies::burnsInDay` defaults to `true`, so the Bramble and the Blackbone burned at dawn purely because their rows stopped before reaching it — nobody ever decided that, and the docs then wrote the accident down as a design choice ("the Bramble does, slimes and spiders do not"). **`RESEARCH.md` §13.4 had the exact list the whole time and only the ordinary undead are on it**: zombie, zombie villager, skeleton, stray, bogged. The user caught the creeper by eye. Two lessons: a field whose default is the *common* case will be wrong for whichever row was written without thinking about it, so **grep the table for rows that stop early** whenever a defaulted flag matters; and when a doc sentence explains why something behaves a certain way, check the behaviour was chosen before treating the explanation as evidence.

- **A function that is only defined is untested, and "it compiles" hides it perfectly.** `Explosion.hpp` shipped `explosionExposure`, `explosionImpact` and `explosionDamage` on day one, and the owning loop ran all three — **for the player only**. Nothing ever applied them to creatures, so the first playtest had Brambles levelling terrain around a completely unharmed goat, and the user found it in one sitting. This is the same shape as M11's dead `RenderStats`: the code was right, complete, and never called. **Wire a calculation to its consumer in the same change that writes it**, or put its result somewhere visible — the blast log now prints how many creatures were hit, which is the number that would have caught this without leaving the terminal.
- **The accumulator bug has appeared four times, and the fourth was in code written the same day as the note warning about it.** All four are a `+=` where several sources write one per-frame impulse. **Creature-hits-player:** seven hostiles landing a blow in one frame applied seven times the knockback, launching the player far enough that every other creature passed the 90 m despawn radius — so the whole map emptied at once and the symptom read as "everything despawns at dawn", three steps from the cause. **Player-hits-creature:** `strike` did `target->velocity += ...`, so each blow landed before the last wore off sent the animal further; the user reported it one message after the first fix, as "each time the knockback increases". **Then both directions of the blast throw.** The rule is settled: **damage sums, displacement does not.** Take the strongest, or assign. When adding any new source of knockback, grep for the shape rather than trusting that the lesson was learned — that is what failed the fourth time.
- **A stimulus flag that can only name one attacker must not be set by a third party.** `provokedTimer` means "the player did this", because `CreatureTarget` has only `None` and `Player`. Setting it from a Bramble's blast would have sent every animal caught in one after the *player*, for something a creeper did — a bug that would have read as "the animals have gone mad" and pointed nowhere near explosions. Left deliberately unset, with the reason written at the call site, because it is exactly the sort of omission a later session would "fix".

- **Never use a movement key as a modifier for a debug action.** `Shift+F8` spawned a charged Bramble, and Shift is *sneak* — so the act of spawning one left the player crouched at 1.3 m/s against a creeper doing 2.4, and backing away, which is the whole defence against an exploder, could not work. The user reported it as "it explodes no matter what", which sounds like a fuse bug and was an input one. Compounding it, the spawn was 5 m out against a 14 m sense range, so it closed and detonated in about two seconds. **A debug spawn needs its own key and enough distance to actually observe the thing** — the point of it is watching, not being hit by it.

- **A number that was never checked against the research is a guess wearing a citation.** The Bramble shipped with health 10, follow range 14, swell 2.5 m and cancel 6 m against `RESEARCH.md` §6.3's 20 / 16 / 3 / 7 - and the comment above the row *asserted* that 2.5 and 6 were Bedrock's while 3 and 7 were Java's, which is exactly backwards. Nothing was measured; the numbers were plausible, so they survived. **When a species row cites the research, open the research** - the file is right there, and the user reporting "their behaviours are off" without being able to say why is what a table full of near-misses feels like from the outside.
- **The detection modifier belongs on `canStart` and nowhere else, and the behaviour table already had the shape for it.** §2.5: a sneaking target cuts follow range to ×0.8 with a 2 m floor, and the modifier is **not** applied once the mob already has you - sneaking stops you being noticed, it is not an escape button. That is precisely the `canStart` versus `canContinue` split, so it cost two predicates and no new state. Whenever the reference distinguishes acquiring from keeping, look for an existing asymmetry before adding a flag.

- **A claimed controller stops other behaviours steering; it does not stop the creature.** `Swell` claims Move, and its own comment claimed that "is what makes it stop walking". It is not: `walking` is **sticky**, surviving from whichever behaviour last set it, so a swelling Bramble kept charging at full speed while it inflated. Anything that claims a controller in order to *stop* has to say so - the flag only decides who may write, not what the previous writer left behind.
- **Smooth what is drawn, not what collides.** The obvious fix - ramp the creature up over several frames - is wrong: the horizontal move has already happened, so a part-way body is inside the block it is climbing, which is a brand new family of stuck states. Snapping the box and easing only the *rendered* position costs one float, cannot affect physics at all, and is what every engine with stair-stepping does. The field has to say so at its definition, because the first instinct on finding it is to use it for something real - reach, targeting or separation reading a deliberately wrong position is a bug that would look like lag.
- **A statistic that is only ever *stored* is untested; a value that is only ever *derived* is unverified.** These are the same failure at two scales, and both have now bitten. `RenderStats` at M11 was computed and never displayed. The Bramble's stats at M20d were written and never checked against `RESEARCH.md`. The ore rarities were invented and never checked against anything. **When a number cites a source, open the source** — the file or the page is right there, and "the user reports the behaviour feels off but cannot say why" is what a table full of near-misses feels like from the outside.
- **A note saying something is "not done, offered" is a debt, and it does not collect itself.** The creature step-up was smoothed on 2026-08-04, and the same commit left a note that the player had the identical instant step and its camera popped on stairs — not done, because only mobs had been asked for. The user hit it the next day: _"when i climb up stairs i teleport to each step."_ The fix was four lines and reused the creature mechanism exactly. **When a fix leaves an obvious sibling case unfixed, either do it or put it somewhere that gets read** — a note in a file nobody opens is not a plan.
- **The wiki is not the Bedrock source, and treating it as one made the same number wrong twice in opposite directions.** The Bramble's swell distances shipped at 2.5 m and 6 m, were "corrected" to 3 and 7 against `RESEARCH.md` §6.3 with a note calling the originals *exactly backwards*, and went back to 2.5 and 6 on 2026-08-05. **`Mojang/bedrock-samples` is the shipped behaviour pack**; `creeper.json` states 2.5 and 6 twice over, in `behavior.swell` and again in `target_nearby_sensor`. §6.3 was quoting the wiki, which on this point is Java's. Repo memory has said "bedrock-samples beats the wiki" for two sessions and the correction went the other way anyway, because the *document* felt more authoritative than the file. **When a research doc and the shipped data disagree, fetch the file** — and when you correct a number, say which source you used, because the next session will only see the number.
- **A field with `must_see` set on every hostile in the reference was missing from ours entirely, and it was the whole bug.** Every "the mobs see me through walls" complaint traced to one absent predicate: `nearest_attackable_target` was checking distance and nothing else, and only the Bramble's fuse ever cast a sight ray. **The lesson is where to look, not what the fix was.** A behaviour that is a table of tunable numbers hides missing *predicates* far better than it hides wrong values, because a wrong value is visible in the row and an absent condition is visible nowhere. When behaviour feels wrong and every number checks out, list what the reference *tests* and diff that against what we test.
- **Units are mixed inside a single Bedrock component and reading them uniformly is a 20x error.** `nearest_attackable_target` has `scan_interval` in **ticks** and `attack_interval`, `persist_time`, `must_see_forget_duration` and `entity_types[].cooldown` in **seconds**, all on the same component. Separately, `avoid_mob_type` has **two different `max_dist` fields** on one page — goal-level default 3, per-entry default 16. And `delayed_attack` has no `cooldown_time` at all (`attack_duration` doubles as it) while silently flipping `track_target` to true. `RESEARCH.md` §1.2 covers the per-tick conversions; these are the traps it does not.
- **Three incompatible melee reach models exist and picking the wrong one is a factor of two and a half.** `melee_attack` multiplies body size by 2, `delayed_attack` by 1.5, and `melee_box_attack` throws that out and expands the mob's own AABB by `horizontal_reach` 0.8. **Every modern Bedrock mob uses the box form**, which is why a flat 1.5 m reach gave our silverfish a polar bear's bite and our polar bear a silverfish's. Reach is a fact about the body, and the body already knows its own width.
- **A soft separation impulse has to respect the world, or a herd buries its own members.** `separate` wrote straight to `position` with no collision test, so creatures crowding a wall shoved each other into it - and a body inside a block finds every move it tries overlapping, so it is stuck permanently. Testing before committing is two lines. **Any code that writes a position directly rather than going through the resolver needs the same check.**
- **Something will end up inside terrain anyway, so there has to be a way out.** A shove, a spawn over ground that had not finished loading, or a player filling in the block a creature stood in all put a body inside a block, and every one of those is permanent without an escape. Climbing out is cheap, runs only when actually overlapping, and covers every entry point at once - spawn eggs, the debug key and the showcase included - which is far better than validating each of them separately.
- **Our spawn shell was 12-30 m against the reference's 24-44.** Close enough to watch one appear out of nothing, which is most of what "they spawn anywhere" meant.
- **Minecraft has no water-body size test, and I invented one before checking.** I wrote a flood-fill to stop fish spawning in puddles. The reference does it with a **biome tag** — `spawns_underwater` plus an ocean/river filter — which costs nothing and is why a village well never fills with cod. What the spawn rules actually carry is `height_filter`, `density_limit`, `herd`, `distance_filter` and `brightness_filter`, and every one of those is a number rather than a search. **Fetch `behavior_pack/spawn_rules/<mob>.json` before designing a spawn condition.**
- **A debug view that freezes the world cannot review anything that moves, and pretending otherwise wastes a round.** `creature_showcase` was used to check a squid's tentacles and a pufferfish's inflation, both of which are animations, and it showed neither. It got one accommodation — an inflating species shows its three *stages* instead of three angles, because two thirds of its model is otherwise unreviewable — and everything else goes through a spawn egg. Two older faults surfaced in the same pass and both were the showcase writing to the real world: it placed its subjects before `loadCreatures()` ran, and it **saved the result on exit**, dropping the whole roster into the player's save.
- **A system that "sort of works" usually has a constant naming exactly how much it can see, and that constant is the bug report.** The steering fan was described as local planning for four milestones; what settled what it could and could not do was `kProbeDistance = 0.9` — **less than one block ahead**, with no memory. Every symptom followed from that one number: a wall longer than the probe gets hugged rather than rounded, a dead end gets oscillated in, and a detour that temporarily increases the distance to the goal can never be chosen, because every candidate is scored on the heading it wants *right now*. **Look for the horizon constant before theorising about the behaviour.**
- **Two independent triggers for the same expensive work will find a state where one of them fires every frame.** A route wants rebuilding when it is finished *and* when the target has moved, and letting either fire on its own meant a chaser dancing in and out of contact cleared its route on the frame it arrived and searched again on the next one, forever. Gating **both** behind one cadence costs nothing, because the fallback in the meantime is the behaviour that existed before. Whenever a second reason to redo something is added, check what happens when it is true continuously.
- **Reconstructing a path walks it backwards, so truncating it truncates the wrong end.** Capping the route at 32 waypoints while walking parents back from the goal keeps the 32 nearest the *goal* — so the reversed route begins somewhere the creature is not, and it sets off in a plausible but completely wrong direction. Count the chain first, then write it out forwards. The general shape: **any cap applied during a reversal is applied to the wrong end.**
- **A default that only matters before the first tick still matters, because the review tool never ticks.** `Creature::onGround` defaulted to `false`, and the frozen showcase therefore drew every magma cube permanently mid-leap with its slabs flung apart — the one pose that is not worth reviewing. Defaulting it to `true` costs nothing, since `step` recomputes it every tick before anything reads it. **When a pose is chosen by simulation state, ask what that state reads as in the showcase.**
- **A sphere cannot describe an animal, and the one we were picking with covered most of a chicken and a fraction of a camel.** `findAimed` tested a fixed 0.6 m radius around the body's *middle* — not the species' height, not its width, not its scale. So a camel, 2.375 m tall, could only be struck in a band around its belly, and the user's report was exactly that: *"if i hit a camel's leg, near its foot, it won't register."* It is the project's usual bug wearing a new hat — a shape derived somewhere other than the table that owns it — and the fix is the usual one: a ray against `bodyBox`, which is what physics and spawning already use. **The tell was in the constant**: a single radius with no species term in it cannot be right for a roster spanning a silverfish and a camel.
- **The blow that kills something is still a blow, so its knockback still applies.** The death fall shipped preserving the creature's momentum "so it settles onto the ground", which sounded reasonable and meant a struck animal *slid* along the floor for the whole second it was tipping over. The user's words were *"even if it's going 300 miles an hour, it needs to immediately stop."* Two places had to agree: the velocity itself, and the older "a shove overrides its own intent while `hurtTimer` lasts" rule, which cheerfully put the knockback back every frame. **When a new state means "stop", grep for everything that writes motion, not just the thing that reads it.**

## Interface, HUD and inventory

- ⛔⛔ **A field that means two things has to travel with the whole stack, or every hand-over path drops it separately.** `ItemStack::damage` is a tool's wear *and* it is which contents a stowbox holds. **Eleven** paths rebuilt a stack from an item and a count and threw it away: the death drop, the Q-drop, the throw, both halves of a chest, furnace and explosion spills, the screen give-back and the shift-click craft loop — each of them silently repairing the tool and emptying the box, **including on death**, when surviving your death is the entire point of the box. `slots::rightClick` did the same while merely splitting a stack, which is the most ordinary click in the game. And three separate "can this merge?" tests matched on `item` alone, destroying the same field from the other direction while also making two single tools of the same type unswappable. None of it is greppable: nothing has the wrong type, every line is plausible, and it survived a `/W4` build and every audit until an item was thrown on the floor. The fix is one function that takes the **whole stack** (`dropStack`) and one `constexpr` predicate that owns "may this accept these" (`slots::roomFor`, used by `add`, `hasRoomFor`, `quickMove`, `gather` and `distribute`) — and because both are `constexpr`, the `static_assert`s beside them can never rot.

- ⛔ **A HUD that rebuilds on demand will run any animation you write and show none of it.** The bow's drawn picture and its pullback were both computed correctly every single frame, and `hudDirty` was set only when the draw *started* and when it *ended* - so the hotbar was built once at the beginning of the pull and not again until the arrow left. The user saw "a normal bow frame and then a charged frame, literally nothing in between", which reads as a broken animation and is actually a missing rebuild. **This is the dig progress bar's bug a second time**: any transient HUD element has to ask for the frames it changes on, and the condition now names all four continuous cases. When an animation appears not to run, check that anything is being uploaded before touching the maths.
- **A probe that measures the wrong quad reports a real bug that is not there.** The first version of the bow check took the leftmost vertex with `layer >= 0` - which is the slot's *interior tint*, not the icon, because `TextureLayer::White` is an ordinary layer index. It reported `STALLS` at every draw time and looked like damning confirmation. Filtering on the sprite the icon actually uses showed it moving at every step. **Before believing a probe, check it is looking at the thing you changed** - the sibling of "a probe that answers no everywhere is as likely to be broken as the thing it tests".
- ⛔ **The dig bar's bug, fifth outfit: the rebuild trigger and the thing drawn asked different questions, and they agreed in the middle.** The bubble row was drawn on a *continuous* test (`airFraction < 1`) and the HUD was rebuilt on an *integer* one (`ceil(fraction × 10)` changed). Those match while a bubble count is falling and disagree at both ends of it, which is why one bug produced two symptoms that look unrelated: going under, `ceil(0.999 × 10)` is still ten, so nothing dirtied the HUD and the row did not appear **for a second and a half**; coming up, the count reached ten while the row was still on screen and then never moved again, so the row **hung there until something unrelated dirtied the HUD**. A near-miss of a comparison is worse than no comparison, because it works in the case you test by hand. **Give the element one function that answers "what is drawn", let the builder and the trigger both call it, and compare its whole result** — `hud::airRow()` returns shown/full/bursting and `Main.cpp` compares the struct.
- ⛔ **"The last one left" is not the same as "the one being lost", and only one of them is an animation.** The air row painted the bursting sprite on the final surviving bubble, so what the player saw was a permanent warning parked on the end of the row and never a bubble actually going. The reference computes **two** counts — `ceil(air × 10 / 300)` drawn against `ceil((air − 2) × 10 / 300)` whole — and draws the difference bursting *after* the whole ones, so the pop is the last two ticks of the life of the bubble on its way out. **When a sprite is meant to read as a transition, check it is keyed off the thing transitioning**; keying it off a survivor gives a state, not an event.

- **Animate a slot's contents by size, not by position.** The bow's draw was first built as a slide back along the arrow's axis, clamped to the room left in the slot. Two things went wrong and both are general. The clamp was applied to the *result* rather than the rate, so the icon hit the wall at 49% charge and sat still for the whole second half — `travel = min(ceiling, room) * charge` is continuous and still bounded, where `min(ceiling * charge, room)` is not. And even once it moved properly it read as **the bow slipping into the bottom-left corner**, because a slot is a frame and anything that leaves its middle looks dropped rather than pulled. Shrinking the icon in place says the same thing, needs no clamp at all because a fraction of its own size is a limit by construction, and stays centred.

- **Contrast is the feature; decoration is not.** The first search field was grey text on a mid-grey slab, chosen to match the storage slots' recess, and the user's verdict was that its visibility "sucks". A text field is not a slot: it is **a pale border round a near-black box with white text on it**, which is what every edition of the reference draws, and the reason is that a field has to be readable against artwork it does not control.
- **A caret that does not blink reads as a character.** The first cut appended a literal `_` to the query, which is indistinguishable from having typed one. Blink it — the reference's own cadence is six ticks on, six off — and drive the phase from the caller's clock rather than a static inside the draw function, so the builder stays a pure function of what it is given.
- **A text field must earn the keyboard by being clicked, not by being on screen.** Focusing it because its tab was selected meant `E` silently stopped closing the inventory, with nothing on screen saying where typing was going. Focus on click, release on any click elsewhere, and show it — the border brightens, the hint goes, the caret starts. The hit test and the drawn rectangle then have to read **one set of constants**, or the box you can see and the box you can click drift apart.

- ⛔ **A complaint about one element is not a licence to add another.** The report was that the bow's three-frame pull looked choppy. The answer built was a whole **first-person held-item view** in the corner of the screen — swing arc, walk bob, equip animation, its own source file and its own screen layer — and the user's entire reaction to it was *"the hell is this grass block in the bottom right corner?? remove it."* It was deleted the same day. The fix that shipped is four lines: the bow's icon **grows on the charge curve inside the slot it already occupies**. **When a thing looks wrong, fix that thing where it already lives** — the smallest change that could possibly work is also the one that cannot be rejected on sight for reasons unrelated to the bug.
- ⛔ **Six hundred new blocks made everything else unfindable, and nothing in the build reported it.** After M20w the Construction tab held 402 entries against a card showing seven rows of seven; TNT sits at row 16 of 58, reachable only by sixteen notches of the wheel. The user's report was simply *"i can't find tnt in the inventory"*. Counting the catalogue at startup — which the game already logs — says nothing about whether a human can *reach* an entry. **When a list grows by an order of magnitude, the way in has to grow with it**; the fix was the search field that had been sitting deferred in the build order.
- **Truncating a per-frame scroll delta throws away every fraction.** `static_cast<int>(consumeScrollDelta())` is right for a mouse, which reports whole detents, and gives exactly zero forever on a precision trackpad, which reports fractions — the list simply never moves and there is nothing to see. Carry the remainder across frames.

- ⛔ **A blended fragment still writes depth, so "nearest" is not the same as "on top".** The stack on the cursor is drawn nearest of everything in the inventory mesh - and the catalogue icons behind it are a *separate* mesh, drawn after it because they need a scissor. Wherever the held item's own artwork was transparent it had already stamped its depth, so the icons behind were rejected and a rectangular hole opened in the list. The right-hand card looked perfect, because its icons are in the same mesh and drawn earlier; the user reported it as "transparency is broken on the left card". **While a HUD shares a depth-testing pipeline, ordering is the fix and a depth value is not** - the carried stack and its tooltip moved to a third screen layer drawn after the clipped one.
- ⛔ **Two boxes of one icon at the same depth: the second one loses.** `appendBlockIcon` drew a stair's two boxes at a single depth under `VK_COMPARE_OP_LESS`, so wherever the step overlapped the slab's lit top face the *slab* won and the step read with the top face's shading. The user described it as the lighting on the top prism being wrong. Each box now offsets by how near its centre is to the eye, `(x + y + z)` - a real painter's key rather than an index, which is also what makes a fence's far arm correct when it is drawn before the post.
- **An icon that is a set of boxes should be drawn as those boxes.** Vines drew as cubes of vine, fences as cubes of planks, walls as cubes of stone, gates as cubes of oak. Stairs had already been fixed once for precisely this reason and none of the others was ever looked at. `usesFlatIcon` now owns "draw the picture flat" for plants, carpets, ladders, vines and panes, and everything else goes through the box path - so a new shape is right the day it is added rather than the day someone notices.

- ⛔ **The targeting outline measured only the block's *height* and assumed a full cell across.** So a ladder, a pane, iron bars, a fence and a torch were all caged as if they were cubes — the user reported it as "it shouldn't show up as a block", and it had been wrong for every non-cube since shapes existed. It is the union of `selectionBoxes` on all three axes now, offset to the low corner. **A bug that has been there since the feature was written surfaces when the newest thing makes it obvious; the symptom names the arrival, not the rule.**
- **A thin block should not draw as an isometric cube in the slot.** Plants already drew flat; ladders, panes, bars and lanterns did not, and read as solid blocks in the inventory. The lantern additionally has to draw only the part of its sheet its body is painted on, because that sheet is a net rather than a picture.

- **Do not claim a bug is fixed when the cause was never actually found.** During the same session a fix was shipped for the "first click after switching block types does nothing" report with an honest admission that the root cause could not be located by reading the code, only that the suspect mechanism (a press queue) had been removed. That framing was correct and should be the default: state what was changed, state what remains unverified, and ask for a re-test. Claiming a confident diagnosis that later proves wrong costs far more trust than admitting uncertainty up front.
- **A HUD element drawn in the wrong depth order is invisible, and nothing reports it.** The hotbar's selection highlight was drawn correctly for two iterations and could not be seen, because the bar's frame sat nearer the camera and covered it completely. There is no error, no warning, and the geometry is genuinely being submitted. **When a HUD element does not appear, check its depth against everything drawn near it before touching its geometry.** The general fix that worked: give the selected cell an entire depth band nearer than every other cell, rather than trying to interleave one quad among them.
- **Numbers without labels are not a UI, and avoiding a font atlas was a false economy.** The first M11 overlay drew seven-segment digits in nine colour-swatched rows and logged the colour key to the console. The user's verdict was blunt: _"what is that.. no labels, nothing, font also looks like a joke. color scheme and detailing looks like a joke."_ They were right — a reader cannot hold a nine-colour key in their head, and hand-drawn segments render zeros as broken `C` shapes. The replacement is a hinted 1-bit Consolas atlas generated by `tools/make-font.ps1`, bound as a third texture, and it was less code than the segment renderer it deleted. **When a diagnostic needs words, build the font.**
- **That guard is not airtight, and the failure is silent and mildly dangerous.** A later run captured the VS Code window instead of the game and sent it a one-second left-button hold; nothing was damaged, but the measurement read `0` and looked like a broken fix rather than a broken test. **Check the screenshot before trusting the number it came with** — the image said "this is the editor" immediately, while the count said nothing. When a click-driven test disagrees with a code reading, suspect the harness first.
- **UI artwork should be shipped as a texture, not rebuilt out of primitives.** The first inventory screen was hand-drawn from quads and looked nothing like the concept. The artist's image is now composited into the HUD sheet by `tools/make-hud-sheet.ps1` and drawn as one sprite, with item icons on top — which is how the original does it too. Two details make it work: the art arrives as an **integer upscale**, so sampling every Nth pixel recovers the real grid exactly where resizing would blur it; and every slot position is **measured off the art in its own pixels** and scaled through one constant, so a revised mock-up needs only those numbers changed.
- **A guard meant to skip pointless work made an item impossible to retrieve.** Creative mode skipped item pickup entirely, on the reasoning that a creative inventory has no need of what it collects. The consequence was that anything dropped in creative could never be picked up — and because the attraction code pulls a drop toward the player and then falls through to gravity once it arrives, the item visibly bounced off the player forever. Two bugs stacked: **the skip should have been "do not spawn drops", not "do not collect them"**, and the attract branch needed no lower distance bound, because falling through to physics *at the moment of arrival* is what produced the oscillation. When skipping work for a mode, check what the skipped work was holding up.
- **A UI action that commits only on release gives the player nothing to judge by.** The first click-drag distributed a stack across slots when the button came *up*, which is correct and felt broken: nothing moved during the sweep, so there was no way to tell whether it was working. The fix is not to add a preview alongside the real thing — it is to **apply the real thing every frame and replay it from scratch as the drag grows.** That needs each dragged slot's contents from *before* the drag, so the distribution can be rewound and recomputed rather than applied incrementally, which would compound. Rewinding also makes "a sweep that never left its first slot" trivially an ordinary click.
- **Do not arm a drag from the click that filled the cursor.** Picking a stack up leaves it on the cursor with the button still down, so the obvious implementation starts a sweep immediately — and then moving away from the slot you just took from scatters the stack back out. A sweep may only begin from a cursor that was **already** carrying something when the button went down.
- **Measure distance from the corner, not from the centre, or a cut corner becomes a curve.** The first crafting table top used a centred Manhattan radius (`|x-7.5| + |y-7.5| <= 10.5`) and then painted a rim over every boundary pixel. That draws a *diamond* whose traced, randomised outline reads as a circle — the user's reaction was "why are the textures curved and circular, the whole point of minecraft is to be blocky." The reference is simply `min(x+y, ...) < 4` from the nearest corner, which produces a 45° step with nothing to trace. **The general trap: any silhouette built by testing a boundary and then decorating it will round itself off.** Build the shape so the steps fall out of the arithmetic instead.
- **Derive a new UI panel from the existing one rather than drawing a second.** The crafting table's panel is generated from the inventory's by `tools/make-hud-sheet.ps1`: frame, backdrop, storage rows and hotbar copied verbatim, top section cleared, and the *same 18×18 cell* stamped into new positions. The cells are literally the same pixels, so the two screens cannot drift apart, and revising the artwork updates both. The layout numbers came from measuring the reference GUI, which shared our rows exactly and differed by one pixel in its columns — worth checking before inventing coordinates.
- **A shared sheet size is a drift hazard the compiler cannot see.** `kSheetSize` was duplicated in `Hotbar.cpp` and `InventoryScreen.cpp`; adding the crafting panel grew the image and would have skewed every HUD sprite in whichever file was forgotten. It now lives once in `HudPrimitives.hpp`. **When an asset's dimensions appear in code, they belong in exactly one place.**
- **⛔ There are TWO HUD sheets and adding a panel breaks the one you did not regenerate.** `tools/make-hud-sheet.ps1` writes ours into `assets/`; `tools/make-reference-hud.ps1` writes Mojang's beside the exe, and the game **prefers the reference copy**. Adding the hopper panel on 2026-08-10 grew both sheets, but only the first script was re-run — so the game read the new offsets out of the old, shorter image and every hotbar icon sampled the wrong rows. The user reported it as "hotbar is broken". The size check caught it and **printed the exact fix, naming both scripts**; I had run one of the two. **Whenever the sheet's height changes, run both, and read the error text rather than skimming it.** The same trap exists for every reference-preferred asset: block textures, the font and the sounds all have an `assets/` copy and a staged copy.
- **The user is a tool; ask them before building test scaffolding.** Verifying the crafting screen looked like it needed a temporary code hook to force the panel open. The user interrupted with _"or you could have me verify it? i'm a tool remember"_ — which is exactly the working agreement already written at the top of this file. Automating a playtest is worth it for something repeatable or hard to reach; for "does this screen render", hand over a build and ask. **Said again on 2026-08-01** after a click-driven test misfired into the editor: _"remember i can be a testing tool when i'm here."_ The rule now has a sharper edge — **anything needing aim, a held button, or a judgement about feel goes to the user while they are present**, because the harness cannot aim and has twice sent input to the wrong window. **Said a third time on 2026-08-02, in capitals** — _"REMEMBER I'M A TOOLLLLL"_ — after a showcase capture burned four commands on a window that had opened the inventory by itself and then vanished. Two rounds of a screenshot harness not working is the signal to stop, not to try a fifth time: **restore the settings and the save, hand over the build, and say what to look at.** Note the flip side, though: the earlier report that a block icon was missing turned out to be **their own play session** having moved the item, so reproduce from a clean state before diagnosing.
- **`Add-Type` cannot redefine a type, so a helper class in a script has to be versioned.** Editing an inline C# block fails forever in an already-running shell: adding mouse-click support to `capture-window.ps1` meant renaming `WinCap3` to `WinCap4`, or any shell that had already loaded the old definition would silently keep using it and the new method would appear to be missing. Guard with a `PSTypeName` check and version the class name. The same trap applies to every `Add-Type` block in `tools/`.
- **A gap you can justify from a measurement is not a random gap.** The catalogue tabs are 22 units wide on a 25-unit pitch, so the 3-unit space between them falls out of the reference's own numbers rather than being chosen; Search is then pushed a further 21 units right and lands 3 short of the card edge, which is exactly the card's frame width. Two of those numbers were originally guessed (I had Search flush right at 124 against the measured 121). **When a layout constant can be derived from the measurement, derive it and say so in the comment** — otherwise nobody can tell later which numbers were read and which were invented.
- **A HUD that only rebuilds on demand must be told when a thing stops, not just while it happens.** The dig progress bar was drawn whenever `breakProgress > 0`, and the rebuild condition used the same test — so the frame progress returned to zero, nothing asked for a rebuild and **the last bar drawn stayed on screen forever**. It looked like digging was stuck. Any transient HUD element needs its disappearance to dirty the HUD as much as its appearance does.
- **Automated playtesting cannot aim.** `SetCursorPos` does nothing while the game holds the cursor, because the camera reads *raw* input and raw input only sees real movement — `mouse_event(MOUSEEVENTF_MOVE, …)` is what works. Before that was understood, a dig test aimed past the 12 m reach at nothing and looked like a broken mining system; the giveaway was **no selection outline in the screenshot**. Check for the outline before concluding anything about breaking or placing.
- **An instant action inside a per-frame loop repeats at the frame rate, and the symptom looked like a spell being cast.** Timed digging (M19c) short-circuits creative mode with `seconds = 0`, which makes `breakProgress` reach 1.0 *every frame* the button is held. Each break exposes the block behind it, so one 60 ms click stripped twelve blocks in a straight line out to the full reach - the user described it as "it throws lightning in that direction and decimates all blocks in click range". Survival was never affected, because a timed break is paced by its own progress bar, which is exactly why it survived every test. **The fix is a cooldown on the instant path only**; putting one on both would make ordinary mining stutter. Whenever a duration can legitimately be zero, ask what happens when the work it guards is free.
- **A held-button feature needs testing at both ends of its range.** This bug required only holding left click in creative, which is the single most common thing a player does, and it shipped anyway because verification was automated screenshots rather than play. The user found it in seconds. **Anything driven by a held button should be exercised held, not clicked once.**
- **An icon draws two side faces at once, and they do not point the same way.** `appendBlockIcon` passed `BlockFace::Side` with no direction for both the front and the right quad, so a furnace showed its mouth on both — in the hotbar, the inventory and the catalogue. The world mesher had been fixed for this a day earlier and the icon was never looked at, because "the furnace faces the right way now" was true of the only place anyone had checked. **A defaulted parameter that means "I have nothing to tell you" will be passed by every caller that has something to tell you and did not think about it.** `blockFacing` and `quarterTurn` answer `Unknown` for the great majority of blocks, so every other icon is byte-identical — which is the property that made the change safe to make at all.
- **A dropped non-block item rendered nothing at all, and had done since tools existed.** `ItemEntities::buildMesh` opened with `if (!isBlockItem(drop.item)) continue;` — so a pickaxe thrown on the floor fell, bobbed, could be walked over and picked up, and was **invisible the whole time**. Nothing reported it because everything else about the drop worked. It only surfaced when spawn eggs made non-block drops common. The fix was already sitting in the same function: the crossed-quad path that plants use, which exists precisely because a sprite has no cube to wrap. **When a code path is guarded by "is this the common case", check what the uncommon case actually does rather than assuming it is handled elsewhere.**
- **An icon drawn from a shape table still has to read the shape table.** `appendBlockIcon` drew one isometric box at `shapeHeight`, so stairs showed as a plain cobblestone cube in the hotbar, the inventory and as a drop. Slabs were already right, which is *why* nobody looked: half a cube is one box, and a stair is two. Same root cause as every other bug in this file — a shape derived somewhere other than the table that owns it — arriving in the one place nobody thinks of as geometry.
- **A catalogue entry is a source, not a container slot, and that one fact settled every question about it.** Measured from `Mojang/bedrock-samples`: the right card's slots are `container_slot_button_prototype`, while catalogue cells are `creative_no_coalesce_container_slot_button` with actions `recipe_select`/`_secondary`/`_tertiary`. It follows without further research that an entry **never depletes, can never be placed into, and has no double-click gather path** — three behaviours we would otherwise have had to decide. **The empty space around the entries is the bin**, which is why Bedrock needs no trash slot. Worth reading the UI data before designing a UI; it answers questions you did not know to ask.
- **A hit test that succeeds and then does nothing swallows the click.** A catalogue cell past the end of the item list still hit-tests as a cell, so the first implementation returned "yes, a cell" and then `continue`d — and the empty lower half of a short tab silently ate every click instead of falling through to the bin. **When two regions overlap and the inner one is sometimes inert, "did I hit it" and "did I consume it" are different answers** and the code needs both.
- **Units are mixed inside a single shipped component, and `inhale_time` is the trap.** `minecraft:breathable` gives `total_supply: 15` in **seconds** while the internal counter is 300 **ticks**, and `suffocate_time` is documented as seconds while its default of −20 is plainly the tick threshold. Worse, **the wiki and the shipped data disagree on the refill rate** - the wiki says one bubble every 4 ticks (40 total), `inhale_time: 3.75` says 75 ticks. The wiki's figure looks like "+4 air units per tick" read wrong. Shipped data wins, and the disagreement is written down rather than silently resolved.
- **Widening a run of item ids moves every id behind it, and item ids are in the player's save.** Adding ten spawn eggs to the existing run would have shifted Coal, the ingots and the buckets, silently turning a saved inventory into different items. The answer was a **second run at the very end** of both the item enum and the sprite layers, with `allItems()` re-ordering the two so the catalogue still shows one unbroken block. **Anything persisted by number can only ever be appended to.**
- **A state existing in the shipped UI data is not evidence the game ever shows it.** `INTERFACE.md` §3.1 lists six catalogue cell backgrounds read out of Mojang's own UI files, one of them a *selected recipe*, and that was built on the strength of the table alone. The user's objection came in two parts and both were right: *"why should that even happen in survival? minecraft doesn't do that"*, and then *"if i hover and it temporarily brightens then THAT is fine, persistent brightness isn't."* Nothing set it usefully either — click-to-fill acts on the click rather than remembering it — so it was a highlight that meant nothing and stayed lit behind an item already taken. **Data read from a reference tells you what the format can express, not what the player sees; the second needs a screenshot or the person who has played it.**
- **An accessible thing should look like the ordinary thing, not like a highlight.** The catalogue's "you can have this" cell was first the panel's face colour (so it vanished into the card, a hole with a bevel round it), then a lifted near-white (so every reachable entry glowed). Both were wrong for the same reason: the neutral state was being *designed*. It is the `139,139,139` recess every other slot on the screen already uses, sampled from inside that very tile — so only the entries you *cannot* have stand out, which is the whole job.
- **A shared buffer stops being shareable the moment its contents have to outlive the screen.** The furnace and the smithing table both borrow the crafting grid's first two slots, and that works because the screen is only ever a *view* onto something else, or the contents are ingredients that get handed back on close. A chest is neither: twenty-seven against a maximum grid of nine, and its stacks belong to a block that will still hold them tomorrow. It got `Region::Chest`, resolved straight into the block's own storage. **The test is not "how many slots" but "who owns what is in them" — and if the answer is not the screen, do not lend it the screen's buffer.**
- **The same gesture can mean different things on different screens, and shift-click is where that bites.** Everywhere else a shift-click moves a stack between the hotbar and the storage rows. With a chest open the only useful move is between the two *containers*, so `quickMoveTargets` takes an earlier branch and hands back the other one. Reusing the general rule would have shuffled items within the inventory while the chest sat there untouched — working code that does the wrong thing.
- **Widening a predicate silently kills every case that sat behind it.** `isFurnace` was widened to answer true for smokers, which is exactly what made the smoker cost no new screen — and it also made `blockName`'s early `if (isFurnace(id)) return "Furnace";` swallow all eight smoker ids, leaving their own `case` labels as dead code the compiler was perfectly happy with. Every smoker in the catalogue read "Furnace". **When you widen a family test, grep for every existing early-out that uses it and check what now falls into it** — the danger is not the code you added, it is the code that already asked the question.
- **A buffer shared between screens has to be emptied by whoever borrowed it.** The furnace lends the crafting grid its first two slots and, on close, deliberately does *not* hand them back — correct, because they were only a view onto the block. But it did not clear them either, so the copies survived: open a furnace with coal in it, close it, press `E`, and the coal was sitting in the 2×2 crafting grid — and closing *that* screen handed it to the player while the furnace kept its own. Free items, in two keystrokes. **"Not mine to give back" and "safe to leave lying there" are different claims**; a view must be discarded at the same moment it stops being a view. The same hole let an exploded furnace or chest be looted twice, because the blast spawned the contents as drops while the open screen still showed a live copy — destroying a container now closes any screen open on it.
- **Prefer deriving a relationship from world state over remembering it.** Two chests standing together form one container. The obvious design stores which is joined to which — and then that has to go into the save file, survive a reload, and be repaired every time a neighbour is broken or placed. Deriving it instead (walk to the start of the run of matching chests, pair off in twos) costs a handful of lines, needs no saved state at all, and **cannot disagree with itself**, because both halves run the same calculation and get the same answer. The price is one named divergence: a long row re-pairs when a chest in the middle is removed. Worth it every time.
- **A probe that answers "no" everywhere is as likely to be broken as the thing it tests.** The chest-pairing probe reported every arrangement as unpaired, which looked like a damning result — it was placing its test blocks at y 120 in a world only 96 tall, so `setBlock` did nothing and every lookup read air. **Before believing a negative result, check the probe can produce a positive one.**

## Saving, settings and persistence

- ⛔⛔ **An emergency save has to run while the objects are still alive, which means a destructor beside them, not a `catch`.** Until 2026-08-17 there was **no autosave of any kind**: everything except terrain was written once, after the frame loop, inside a `try` whose `catch` logged and exited without saving. Terrain is written as it changes, so a `VK_ERROR_DEVICE_LOST` — the one crash this project has actually seen — left a world that looks played and has been *robbed*: your buildings stand, your inventory, chests, furnaces and every creature are gone. The obvious fix is to save in the handler and it is a use-after-free waiting to happen, because unwinding destroys the block's locals **before** an enclosing handler runs. A `SaveOnUnwind` object declared next to `world` runs first, since destruction is the reverse of construction, and it must swallow its own exceptions — a throw out of a destructor during unwinding is `std::terminate`, and a second failure must never replace the first as the reason the game died. A ~30 s timer and a save on screen close cover the ordinary cases; the destructor covers the one you cannot predict.
- ⛔ **An autosave saves the world, and the screen is holding part of it.** The first version wrote the inventory and the hotbar and silently dropped the cursor stack and the crafting grid — a crash mid-craft ate whatever was on the cursor, which is exactly when the player is most likely to be holding something they care about. The instinct is to reuse the give-back that already puts those stacks away on close: **do not.** `rewindDrag` reassigns `heldStack` every frame, so a give-back run at save time double-adds and the autosave becomes an item duplicator. Copy the inventory, add the screen's stacks to the copy, write the copy. `forEachScreenStack` is now the single owner of "which stacks exist only while a screen is up", and one caller knowing about them is precisely how the first version was wrong. **Ground items are still not saved at all** — `WorldStore` has no drops table, so a reload deletes anything lying on the floor; recorded rather than fixed.

- ⛔ **`settings.cfg` on disk is often a *partial* file, so a `-replace` that sets a test value silently does nothing.** A soak meant to run in a forced thunderstorm ran in clear weather for fifty seconds and reported a clean result, because `start_weather` was not in the file and there was nothing to replace. The log said `weather 0.00/0.00` and I nearly read that as the weather system being broken. **Append the key rather than replacing it, and check the log says the state you asked for was actually reached** — a test that quietly tests nothing is worse than no test.
- ⛔⛔ **A format-version migration is a promise that old data stays, and that promise is also a promise that old damage stays.** A playtest reported water hanging in mid-air. A probe found it: a 32x2 sheet of water *sources* at y=64 in the chunk at the origin, left there by a long-deleted test harness that had written into the real world. Generating the same chunk from the same seed produced none of it, so the fault was a fact on disk and nothing in the current code could touch it. **Bumping `kChunkFormatVersion` did nothing** - the bad file was **version 2**, and the loader carried an upgrade path that read and widened version 2 rather than rejecting it. The fix was to retire that path. Two lessons: **when a version bump fails to change anything, read the actual bytes on disk before doubting the fix**, and **a lossless migration path silently makes every older fault permanent**. Cost of retiring it here: 4 files out of 138.
- **An out-of-date version is not a fault and must not warn.** Rejecting a stale chunk is the version number doing its job; warning about it prints one line per chunk in the world and buries anything real. A wrong magic number, seed or coordinate *is* a fault and still warns. Splitting the two checks is three lines.
- **A probe that scans "around the player" cannot be compared between two runs.** The first attempt at the mid-air water probe scanned a box around the spawn position, and the two worlds being compared spawned the player in different places - so the two answers were not measuring the same region and the contradiction looked like a real difference in the world. A fixed absolute region settles it. Print the residency of the region too: an unloaded chunk reads as air, so "zero" and "not loaded" look identical.

- **A migration test placed where the player is not standing proves nothing, and it fails *silently* by passing.** Legacy fixture chunks were written at `(0,2,0)` and `(1,2,0)`, the game was run, and neither produced a warning — which read as success. It was not: the saved player position was `(-330, 40, 2249)`, chunk `(-11, *, 70)`, so **neither fixture was ever opened**. The tell was that both files were still byte-for-byte their original size afterwards, and that the *deliberately corrupt* fixture passed too. **Read `player.dat` first and put fixtures in the chunk column the player actually loads**, and always include a fixture that must be rejected — a test where nothing can fail is not a test.
- **A truncation check needs the file to be short *overall*, not short in the section you care about.** To prove the reader had really moved from 32,768 to 65,536 bytes, a fixture was written with one block byte missing — and it loaded cleanly, which briefly looked like the widen had not taken. It had: the 4096-byte waterlogged section follows the blocks in the same file, so the stream simply read one byte of *that* and `gcount()` was satisfied. **Adjacent sections silently cover for each other.** Truncating the file to end early — a v3 header over a 32,768-byte body — produced the expected `Truncated chunk data` immediately, and that version discriminates precisely because a reader still consuming the old width would have accepted it.
- **Amdahl's law arrived immediately and should have been predicted.** Eleven workers cut generation and meshing by 3.3×, and total startup by only 1.6×, because uploading meshes to the GPU is serial main-thread work that threading cannot touch. The fix was not to tune the threading further but to **measure the two phases separately** so the remaining serial cost was visible instead of being blamed on poor scaling. Split a timer before optimising against it.
- **A screenshot tool that repositions the window will also save that state.** Forcing the camera to look east for a sun screenshot meant the game saved that orientation to `player.dat` on exit, so the next normal run started staring at the sky. Harmless here, but any test hook that mutates persisted state needs to either restore it or run against a throwaway save.
- **A diagnostic that reports the configuration instead of the state will send you the wrong way.** Passives appeared not to spawn, so a line was added logging the biome at `spawn_x`/`spawn_z` — which said `Mountains` while the player stood in a desert, because **a saved player position overrides the spawn setting entirely** and the log never knew. Two rounds of "why is the biome table broken" came out of trusting it. The line now reports the biome under the player's *actual* position. **Log the value the code will use, not the value that was requested.** (The save is at `saves/world_<seed>/player.dat`, not `saves/player.dat` — deleting the wrong path fails silently and looks like the setting being ignored again.)
- **Do not link a build while the game is running.** The release link failed with `LNK1104: cannot open file 'bin\game.exe'` because the process still held it during shutdown. Worse, the tempting fix is to force-kill the game, and **force-killing skips the save path entirely** — `world.saveAll()` and the furnace write both run after the window closes. Send `Alt+F4` and wait for `Shutting down.` in the log instead.
- **A loading screen that streams around the wrong point finishes honestly and still leaves you waiting.** The world was loaded around `spawn_x`/`spawn_z` and the player was then placed at the *saved* position, which in a real save is thousands of blocks away - so the bar measured a piece of world nobody was about to stand in, the game started, and the real area streamed in underneath the player. Every checkpoint was telling the truth about the wrong thing. **When a progress bar disagrees with what you can see, check what it is measuring before changing how it is measured.** The fix was to read the saved position *before* the loading screen, since it is what the world has to be centred on; only the fresh-world placement has to wait for chunks, because it reads blocks.

## Build, tooling and PowerShell

- ⛔⛔ **`powershell -File script.ps1 -Path a,b,c` passes ONE string, not three — and `check-ascii.ps1` reported that string clean.** `-File` marshals every argument as text, so the script's `[string[]]$Path` receives the single literal `"a,b,c"`; `ReadAllBytes` on it fails and the loop still printed `a,b,c: 0 bytes, BOM False, NUL 0, non-ASCII 0, U+FFFD 0` and exited 0. **Every field reads green and the only tell is the `0 bytes`.** This is the mandated encoding check named in `CLAUDE.md`'s Critical box, run after every file write, so it had presumably been silently passing multi-file invocations for as long as it existed — and on 2026-08-19 it certified four source files that it never opened. **The script was correct; the invocation lied**, which is why no amount of reading it would have found this. It now prints `*** NOT FOUND - NOTHING WAS CHECKED ***` and exits 1. Two habits follow, and the second is the general one: **pass one path per call, or invoke with `&` instead of `-File`** when you need an array; and **a checker that can run against nothing must say so**, because empty input satisfies every check ever written. The same trap is waiting in any `-File` invocation that takes an array. **Its exit code moved twice on 2026-08-19 and that is the more useful fact.** At 07:15 it exited 1 only for a path it could not open, so a present file carrying a planted BOM, a NUL or a U+FFFD was reported correctly on its own line **and still exited 0**; by 07:30 it exited 1 on damage too, with an explicit `exit 0` so a clean run stops inheriting a stale code from the previous command. Read the printed line whenever you are unsure which version you are holding.

- ⛔ **`Start-Process -RedirectStandardOutput` only flushes at 4 KB, so a short probe run writes an empty file.** Two probe results vanished this way and looked like the game had failed to start — the log was zero bytes and stderr was empty. The successful captures were all exactly ~4136 bytes, which is the giveaway: it is stdio's buffer, not the program. Either let the run last long enough for the per-second log lines to push past it (about thirty seconds), or let the process exit cleanly instead of killing it. **A zero-byte log is a buffering result, not a crash.**
- **A build can fail purely because the game is still open.** `LINK : fatal error LNK1104` on `bin\game.exe` with exit code 1104 means the executable is locked by a running instance — usually one launched by `run.ps1` in the other terminal. Nothing is wrong with the code; close it and rebuild.

- **A force-killed process loses everything still sitting in its stdout buffer, so a 0-byte log can mean "ran fine, killed too early".** A debug run was given 150 seconds, did not finish generating, was killed, and produced **zero bytes on both stdout and stderr** — identical in appearance to never having started. The existing lesson about block-buffered stdout covers the *waiting* case; this is the *killing* case, and it destroys the evidence rather than delaying it. **Let the process exit gracefully** (`CloseMainWindow`, then wait generously) and check `HasExited` before trusting an empty log. Where the work is slow only because of the build — a worldgen census is identical in both — run it on release.
- **PowerShell's comma binds tighter than arithmetic, so `@($size - 1, 3)` is `$size - (1,3)`, not `@(($size-1), 3)`.** This produced a nonsense argument that surfaced far from the line that caused it. Parenthesise every arithmetic expression used as an array element.
- **A plain shell cannot build this project, and the failure looks like a broken install.** CMake's Ninja generator locates MSVC through `PATH`/`INCLUDE`/`LIB`, which only exist inside a Developer environment. Verified directly by configuring from a scrubbed shell: `No CMAKE_CXX_COMPILER could be found`. An earlier version of `SYSTEM_MEMORY.md` confidently claimed CMake would find MSVC on its own via the Visual Studio registry — that is true only for the `Visual Studio` generators, not for Ninja. **The claim was written from plausible reasoning and was simply wrong; it was caught only because it was actually tested before being trusted.** `tools/dev-env.ps1` now exists so this is one command instead of a rediscovery.
- **VS Code's CMake Tools needs `architecture`/`toolset` with `"strategy": "external"` in the preset**, or it cannot build a Ninja + MSVC project either — same root cause as above. Those fields are ignored by CMake itself for Ninja, so they are free on the command line and load-bearing in the editor. Separately, `Cannot configure: No configure preset is active` just means no preset has been selected yet (`Ctrl+Shift+P` → `CMake: Select Configure Preset`); it is not an error in the preset file.
- **The VS Code integrated terminal mangles multi-line PowerShell input.** Observed 2026-07-30: a multi-line script with a `function` definition and blank lines came back as the single garbage token `inue'` — the tail of `SilentlyContinue'`, meaning the input was fragmented mid-paste. Nothing was wrong with the script itself. **Always send one line, chaining with `;`.** For anything genuinely long, write a `.ps1` file and invoke it, or base64-encode it and use `powershell -EncodedCommand`.
- **⛔ It happened again on 2026-08-10 and that time it destroyed a source file, because the fragment was still *runnable*.** The same tell appeared — the output was `ding($false)`, the tail of `New-Object System.Text.UTF8Encoding($false)` — but the surviving fragment was a valid loop, and it rewrote `game/src/hud/InventoryScreen.cpp` substituting `u`→`{` and `S`→NUL throughout, so `#include` became `#incl{de`. **That is not mechanically reversible**: a real `{` and a substituted one are the same byte. The 2026-07-30 entry above only ever cost a re-run, which is exactly why the rule reads as advice rather than as a hard stop. It is a hard stop. **What saved it was VS Code's own local history** — `%APPDATA%\Code\User\History\<hash>\entries.json` names the file each folder belongs to, and the newest `.cpp` in it carried every edit of the session. Look there **before** reaching for git, which would have thrown away hours of uncommitted work. After any scripted rewrite, count NUL bytes per file; zero is the signal that nothing was substituted.
- **`Start-Process -UseNewEnvironment` is broken in PowerShell 5.1** — it strips variables PowerShell itself needs and dies with `Internal Windows PowerShell error ... 8009001d`. To test something in a clean environment, launch a child shell and reset `Path` from the Machine/User registry values by hand instead.
- **`cl.exe` not being on `PATH` does not mean MSVC is missing.** MSVC deliberately only appears on `PATH` inside a "Developer Command Prompt." To check whether MSVC exists at all, look for `vswhere.exe` at `%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe` instead. (On this machine it genuinely was missing — but the check matters, because the naive `Get-Command cl` test reports "missing" even on a correctly configured machine.)
- **Recover a reference image's native pixel grid by measurement, not by eye.** The HUD art arrived as a 1480×328 WebP. Probing runs of identical pixels along one row showed every run was a multiple of 8, proving an exact 8× upscale and giving a lossless 185×41 original by nearest-neighbour downscale. Guessing the scale would have produced half-pixel seams that look like anti-aliasing bugs forever after. `tools/probe-image.ps1` exists for this; `tools/convert-image.ps1` handles WebP, which neither stb_image nor System.Drawing can read.
- **Widening `Vertex::color` from vec3 to vec4 silently made the crosshair invisible.** `engine::Vertex{{...}, {c.r, c.g, c.b}, ...}` still compiles against a `float[4]` — aggregate initialisation just zero-fills the missing element, which is alpha, which with blending enabled means fully transparent. No warning at `/W4`. **After changing a vertex field's width, grep every brace-initialised `Vertex` rather than trusting the compiler.**
- **`SendKeys` goes to whatever currently has focus, and `SetForegroundWindow` usually fails from a background process.** An unverified send while automating a screenshot typed `F5` into VS Code instead of the game — which is the *start debugging* shortcut. `tools/capture-window.ps1` now compares `GetForegroundWindow()` against the target handle and refuses to send or capture otherwise. It also waits ~1.2 s after focusing, because the game clears buffered input on focus change and swallows a key sent immediately.
- **A benchmark that writes the settings file the game is played from will be mistaken for a bug — twice.** The M13b sweep set `frame_cap=0` and left it there, so the user launched the game and reported the frame limiter broken. It was fixed, then broken again by the very next sweep. The limiter was fine both times. `tools/benchmark.ps1` now backs the file up and restores it in a `finally` block. **Any tool that mutates state the user also uses must restore it, not merely intend to.**
- **A `Get-Process` main window handle can be the console, not the game.** `tools/capture-window.ps1` captured a black console window because the run was launched *without* output redirection, so `game.exe` owned a console whose handle won. It also needs `AttachThreadInput` to reliably take the foreground: `SetForegroundWindow` alone silently fails from a background process, and since the capture reads screen pixels, a window that is not on top captures whatever is. `GetClientRect` plus `ClientToScreen` is the tidier way to find the region, and it silently leaves the origin at (0,0) when the conversion fails, capturing the top-left of the desktop instead — `GetWindowRect` gives absolute coordinates with nothing to go wrong.

- **A threshold is meaningless without the distribution it is quoted against, and ours are not uniform.** `kEntranceThreshold` was 0.46 with a 0.22 ramp, so a cave mouth needed the entrance field to reach 0.68. Two octaves of `octaves2D` have a mean |v| of **0.27** and clear 0.46 on 8.6% of columns — 0.68 is the top percent or two, so cave mouths were arithmetically possible and never seen. `Climate.cpp` had already learned this once and carries a measured per-field gain because of it; the cave constants never got the same treatment. **Any new `> someConstant` test on a noise field needs a measured quantile first**, and value noise is much narrower than gradient noise, so instinct from Perlin numbers is wrong here.
- **A census window has to be wider than the field you are censusing.** The worldgen probe generates 11x11 chunks = 352 blocks. The cave entrance field has a 230-block wavelength, so that window covers about one and a half cells of it and happened to sit in a low one: the field never exceeded 0.46 inside it while exceeding it on 8.6% of the world. The probe reported **zero** cave mouths twice and both readings were true and useless. The wide 2048-block loop is the one to trust for anything low-frequency; the chunk loop is only good for things that vary per block.
- **"Snow where it should not be" was the freeze rule reading the climate noise instead of the biome's own constant.** The reference's `freeze_top_layer` asks `Biome.warmEnoughToRain`, which reads a **fixed number off the biome** (plains 0.8, snowy plains 0.0) and never the field that placed it. Ours passed `climate.temperature`. The field is continuous across a biome edge while the biome flips at a hard box boundary, so every warm column sitting in the cold tail whitened and a hundred of them read as a snowy biome leaking onto the plains. Same shape as every other bug here: **a value derived somewhere other than the table that owns it.** The biome row now carries `warmth`, and the surface-ice rule asks the same function instead of a hand-written `Frozen` tag that meant the same thing and could disagree with it.
- **`steep` belongs to peaks and slopes only, and we had added it to three ordinary biomes.** `minecraft:steep` appears exactly five times in the reference's surface rules, all inside frozen peaks, jagged peaks and snowy slopes. Ours also had it on Meadow, Windswept Hills and Gravelly Hills — which draws a stone line along every hillside in 11% of the world, reported as "random streaks of rock where there should be grass". CLAUDE.md already said not to do this; the table had it anyway. **When a rule has a written boundary, grep the table for rows that violate it rather than trusting the note.**
- **Stretching a field toward uniform doubles every biome at the end of an axis.** The per-field gains were tuned to put mean |v| near 0.40, which is a sigma of about 0.5; the reference draws its band edges across a bell-shaped field nearer 0.35. Erosion's low end is exactly "mountain country", so it was where that showed — measured 0.41 against the reference's ~0.28, and peaks plus snowy slopes came to 8% of the world. Dropping the erosion gain to 1.75 took mean |v| to 0.31 and peaks from 3.43% to 2.60%. **The gains and the band edges have to describe the same distribution or neither means what it says.**
- **A cave mouth and a crater differ only in which carver was allowed near the surface, and I shipped the crater.** Relaxing the shared surface margin inside an entrance region opened *every* cave system at ground level, cheese caverns included — so a playtest found a hole the size of the cavern going down to bedrock, with the trees that had been standing on it left in mid-air. The research I had already commissioned said it in one line: the reference gates on `sloped_cheese < 1.5625 -> entrances only`, and "that single range test is the whole 'caves don't eat the landscape' rule". **A narrow tunnel may reach daylight; a cavern never may.** Tunnels and caverns now carry separate surface margins and only the tunnel one is ever relaxed. If research hands you a named rule, port the rule — not the half of it that produces the effect you were chasing.
- **Anything placed against the *uncarved* height floats the moment carving reaches the surface.** Trees are built at `surfaceHeightAt`, which is the height before caves, and `buildTree` force-writes a dirt block under the trunk with `onlyIntoAir = false` — so a tree over a cave mouth got a trunk, a canopy *and* a dirt block hanging in the air. The reference never has this problem because features read the `OCEAN_FLOOR` heightmap, the topmost **solid** block scanned down from the sky, so a void can never be chosen; `surfaceCarvedAt` is our stand-in. **The water fill has the same trap** — it places water above the terrain top, so carving that top strands it, and a submerged column is now excluded from cave mouths outright.
- **The FLOATING-TERRAIN counter could not see floating trees, by design.** It files logs, leaves, dirt and plants as `treeCellsAbove` and counts only everything *else*, which was right for the bug it was written for and blind to this one. It read 0 throughout the entire regression. **A counter proves the absence of the bug it was written for and nothing else.** FLOATING-DECOR now counts anything sitting on top of a carved hole.
- **A biome boundary is a contour line of a smooth field, so it comes out smooth — the reference breaks it with a domain warp and we had none.** Sea ice met open water along an edge straight enough that a playtest called it a rectangle. The reference offsets every climate sample by `shift_x`/`shift_z` before sampling: a ~32-block noise worth up to ±15 blocks of displacement, applied to all five fields at once. One warp in `climateAt` fixes every boundary in the world, ice included. Warping each field separately would pull them out of register with each other instead.
- **`steep` is one facing in the reference, not four.** Its condition compares the column one step north against one step south and fires only when the north side is the high one — which is why a Minecraft summit shows bare rock on one face and keeps its snow on the other. Ours tested all four neighbours symmetrically, so it fired on every slope from either side and frosted whole mountains in stone: jagged peaks were 38% stone, and 19% after the port.
- **A structure built by more than one chunk must draw its random numbers in the same order every time, and iterate the same cells whether or not it can write them.** Trees straddle chunk borders and are generated once per chunk they touch, so the two copies have to agree block for block. Two rules keep that true: every draw comes from a stream re-seeded off the cell's own hash, and the canopy loops run their **full** range with `place()` silently dropping out-of-chunk writes — because skipping the loop for cells outside the chunk would desynchronise the roll sequence and split the tree down the seam.
- **Widening what a structure can build means widening `kReach` in the same commit.** It is the margin saying how far outside its cell a structure may reach, and chunk generation only visits cells within it. A branching oak throws foliage about `height/4 * 1.33` sideways plus a two-block blob, so 3 was no longer enough; too small and one chunk builds the far half of a tree and its neighbour does not.
- **Falling blocks belong to the game layer, not to `World`.** `World` has no idea entities exist, so it does what it already does for a plant a flow swept aside: takes the block out of the grid and *reports* it. The entity owns the motion and writes the block back when it lands. Stepping the block down one cell per scheduled update was a tenth of the code and read as teleporting — the whole point of a falling block is watching it accelerate. Reference numbers: gravity 0.04 blocks/tick² (16 b/s², **half** the player's), drag 0.98 applied after acceleration, terminal 1.96 b/tick.
- **A fast entity has to test every cell it passes through, not the one it ends in.** At terminal velocity a falling block covers a third of a block per frame at 120 fps and half a metre at 60 — and on a frame hitch, several blocks. Checking only the destination cell drops it straight through a one-block floor.
- **`MeshPushConstants` outgrew the 128 bytes Vulkan guarantees, and that is fine *if it is checked*.** It reached 144. Measured rather than assumed: this machine's RTX 4070 reports **256**, logged at startup beside the device name. `GraphicsPipeline` already declared the range as `sizeof(MeshPushConstants)` so nothing needed widening by hand, and the renderer throws if `maxPushConstantsSize` is smaller than the block — a device that cannot hold it says so instead of corrupting every draw. **An earlier version of this note said never to exceed 128; that was caution rather than a measurement, and it cost a round of contorting data into spare `vec4` lanes.** M23a then took it back to **80** for a better reason than caution: 80 bytes of it were identical for every draw in the frame and belonged in a uniform buffer.
- **Fog measured on `gl_Position.w` is a cylinder, not a sphere, and the corners of the screen give it away.** `gl_Position.w` is depth along the *view axis*; at the corner of a wide view it is only about 0.7 of the true distance to the fragment. A ramp ending at the render distance therefore left the corners barely fogged, and a playtest reported chunks visibly loading "out of the corners of my eyes" while the centre of the screen looked right. **Radial distance is `length(fragWorldPosition - eye)`**, which needs the eye in the push constants and needs the fragment position to genuinely be world space — true for world geometry drawn with an identity model matrix, and *not* true for the sky, which is drawn camera-local and so keeps view depth. Minecraft has the same split and names it: `FogShape.SPHERE` versus `FogShape.CYLINDER`.
- **The sun is drawn through the world pipeline, so any new full-screen effect has to decide about it explicitly.** Distance fog fading everything to the sky colour dissolves the sun into the sky it is meant to sit in front of. The distinction was already in the data — world geometry is drawn `lit`, the sky and the block outline are not — so "fog start above zero also means world-geometry-only" needed no new state. Underwater fog keeps a start of zero and still fogs everything, which is what a submerged view wants, so the old behaviour is unchanged rather than special-cased.
- **Never hand-author art. Adding anything visible means adding a row to `tools/make-reference-blocks.ps1`, and that is the whole job.** Mojang's textures are staged into `blocks-reference/` beside each exe and the game prefers them over `assets/`; the user's friend is authoring the original art separately. I spent a chunk of a session drawing eleven food sprites as ASCII pixel art in the texture tool before finding out, and the user was rightly angry. `ASSETS-REFERENCE.md` says this on its second screen. **`assets/` still needs a file per layer or the load fails, so give it the cheapest possible placeholder — `New-FlatTexture` with a plausible colour — and never more than that.** Time spent making a placeholder pretty is time spent on work someone else is already doing.
- **The noise fields here are concentrated, not uniform, and this has now caught me three times.** `value2D`/`octaves2D` pile up around their midpoint: a two-octave field has mean |v| near 0.27 and hardly ever reaches 0.7. So (1) `kEntranceThreshold` at 0.46 with a 0.22 ramp needed 0.68 to open fully, and cave mouths were never seen; (2) the per-field climate gains had to be *measured* rather than assumed; and (3) choosing a flower with `region * 8` gave most of the world whichever species sat in the middle of the array — a playtest reported "way too many blue flowers", which was oxeye daisy, azure bluet and allium at indices 3, 4 and 5. **The rule: smooth noise decides *where* something changes; a flat hash decides *what* it changes to.** A species, an index or a variant comes from `hash2D(seed, cellX, cellZ) % count` over a coarse cell — coherent regions *and* an even spread — with a fraction of per-block re-rolls mixed in, or every region is a monoculture with a seam at its edge.
- **A generator fix does nothing for chunks already on disk, and `saves/` fills faster than you think.** Water flow calls `setBlock`, which marks a chunk modified, so merely standing near an ocean writes it out. After the waterlogging bug was fixed the playtester still saw it, because 118 saved chunks held the damage. The save header already carries a version: **bump it and old chunks are regenerated.** Chunks now have their own version constant separate from the player file's, so doing that does not cost the player their inventory. **Before claiming a worldgen fix works, ask whether the world you tested was generated or loaded.**
- **"Both presets clean, zero validation errors" did not catch a bug that was plainly visible on screen.** It was true and the hole in the ocean was still there. What would have caught it: the census probe, and looking at `saves/`.
- **`isIce` exists because `slipperiness` named `PackedIce` alone.** Adding plain `Ice` and generating it on frozen oceans silently made every frozen surface in the world walk like stone — the ice physics were fine, there was just no ice left for them to act on. Three other rules asked the same narrow question and were all silently wrong too: explosion resistance, the catalogue tab, and the floating-terrain probe's exclusion list. **This is the "widen a family test, grep for everything that already asks it" rule, and it is worth grepping even when you are only *adding* a sibling.**
- **A number copied from the reference into a field with a different unit is invisible to every check we have.** `panicSpeedScale` multiplies our `runSpeed`. Bedrock mobs have a single `movement` speed and scale *that*, so its `panic.speed_multiplier` is in a different unit entirely. The villager and wandering trader were given the reference's 0.6 verbatim while every animal was given a value authored in our unit (1.2–1.5, meaning *faster* than a run) — **one column holding two units at once.** A struck trader bolted at `1.8 × 0.65 = 1.17` against a walk of 1.0, and a playtest reported he "was walking as if i wasn't a threat". It compiled, it validated, and the behaviour tree was working perfectly: Panic held the movement controller the whole time. **The comment beside it even claimed an effect the arithmetic did not produce** — that the villager "flees slower than it walks", when 1.6 × 0.65 = 1.04 is *faster* than its 0.9 walk. When porting a tuning value, write down what it is measured *against*, and sanity-check it against a neighbouring row that was authored natively.
- **A behaviour can be firing correctly and still look broken, so establish which half is wrong before changing either.** The instinct here was "the trader has no flee state, add one". It had one, at priority 1, holding `ControlMove`, entered correctly. Reading the state machine and the single line that turns intent into velocity separated "the AI is wrong" from "the number is in the wrong unit", and only the second was true. **Grepping for the *consumer* of a field — who multiplies it, who reads it — is usually faster than grepping for the field itself.**
- **An edit that ends where a heading begins will eat the heading, and nothing will tell you.** Replacing a block ending `---\n\n## World Persistence` with one ending `---` silently deleted the heading and orphaned its entire section under the one above. Markdown does not complain, the diff looks plausible, and it only surfaces when a human reads the file. **After any structural edit to a document, grep for the headings on both sides of it.** This has now happened in both `LESSONS.md` and `SYSTEM_MEMORY.md`.

- **A thresholded noise field has no size cap, and that is why ore veins came out the size of a room.** Wherever the field stayed above the threshold the blob kept growing. The reference never generates ore that way: it attempts `count` veins per chunk and each lays at most `size` blocks, and the largest `vein_size` anywhere in vanilla is 20 (max 52 blocks). Porting `OreFeature` — a spindle of overlapping spheres along a short randomly-angled segment, radius tapered by `sin(pi t)` and re-rolled per step — capped our worst coal seam at exactly its theoretical 23 and left total density within 3% of where it was. **The taper is what makes a vein read as a vessel rather than a dot**, and it only shows at `size >= 12`; below that vanilla veins genuinely are blobs.
- **A feature whose roll count depends on which chunk is asking will split along chunk borders.** The air-exposure roll was originally taken *after* the "is this block inside my chunk" test, so a vein straddling a border consumed a different number of values from each side and the two halves disagreed — and because one stream fed every vein in the column, everything after it diverged too. Two rules fix it: roll for **every** cell the shape covers regardless of where it lands, and give each vein a stream of its own drawn from the column's, which also lets a vein nowhere near this chunk be skipped without shifting the ones behind it.
- **The player spawned with their eyes under water and it took a render-culling measurement to notice.** `spawn_x`/`spawn_z` were used verbatim, and that column happened to be seabed — eye at y 23.6 against a sea level of 24. It was invisible in the logs and only surfaced because the underwater fog distance was being used as a cull radius and threw away 93% of the world. Spawn now searches outward in rings for a column above the waterline that is not a cave mouth. **A measurement that comes out absurd is usually telling you about a different bug.**
- **A population tied to a radius has to scale by that radius *squared*.** Creature range moved from a fixed 90 m to the render distance; keeping the count at 14 would have spread the same animals over ten times the area and made a wider view read as an emptier world. Capacity now scales by area from the 90 m figure the tuning was done at, with a hard ceiling — measured at 9 chunks: 44 creatures, 12 species on screen, 120 fps, GPU 1.55 ms, so the ceiling is the binding constraint rather than the frame time.
- **`â€”` in terminal output is not a corrupt file; it is the console.** An onboarding review reported mojibake in `Biome.hpp`, and the grep output did show `â€”`. The file was fine — PowerShell's console renders a legitimate UTF-8 em-dash through cp1252 and produces exactly that sequence. **The test that actually answers the question is counting `U+FFFD` replacement characters** (`$text.ToCharArray() | Where-Object { $_ -eq [char]0xFFFD }`); zero means the bytes are valid UTF-8 no matter what the terminal drew. Eleven other source files "fail" a naive non-ASCII grep for the same harmless reason. Real corruption *did* happen once in this project — `Set-Content` defaults to ANSI and silently mangled nine em-dashes in the docs — so the concern is legitimate, but the two are distinguished by the U+FFFD count, never by eye.
- **⛔ The U+FFFD test does NOT catch double-encoding, which is the corruption `Get-Content` actually causes.** Bare `Get-Content` in PowerShell 5.1 reads a UTF-8 file *with no BOM* as Windows-1252, so every multi-byte character becomes two or three separate chars; writing it back as UTF-8 then encodes each of those again. The result is `â€”` **stored in the file** rather than merely displayed — and it is perfectly valid UTF-8, so **`U+FFFD` counts zero and the file passes the project's own check.** This corrupted four documents during the 2026-08-06 cleanup and was caught only because a later `replace_string_in_file` could no longer match an em-dash it had just written.
  - **Read with `Get-Content -Encoding UTF8`** (or `[System.IO.File]::ReadAllText`, which honours the BOM), and write with `[System.IO.File]::WriteAllLines(path, lines, (New-Object System.Text.UTF8Encoding($false)))`.
  - **The reliable detector is the character `Ã` (U+00C3).** It is the first half of every double-encoded sequence and appears in essentially no honest English prose. Count it alongside `U+FFFD`, never instead of it — they catch different failures.
  - **Back up before any scripted rewrite of a document.** The only reason this cost minutes rather than a day is that every file had been copied to `docs-backup/` first.
- **PowerShell variable names are case insensitive, and it bit again in that very tool.** A `-FloatEye` parameter and the `$floatEye` read out of the header are **the same variable**, so the override silently did nothing and a sweep printed four identical rows. Same fault as the `$B`/`$b` colour-mixing bug. Any script parameter that shadows a local needs a distinct name, not a distinct case. **Third instance, 2026-08-19, and it is the worst shape of the three because it corrupted a *control* rather than a result**: a probe declared `$LF` as a `[char]10` constant and later used `$lf` as a counter, so the constant was overwritten with a number — and the control still returned the expected value, because claim and control were both computed from the same corrupted variable. **A silently-merged variable is how a control comes to differ from its claim in no variable at all.** Distinct names, never distinct case, and prefer `$chLF`-style prefixes for character constants that share a word with a counter.
- **A generator that rewrites part of a file in place has to be idempotent, and the way it fails is that the block grows.** `tools/index-docs.ps1` writes an index between two comment markers, and its closing marker is followed by a blank line. The stripper removed the markers and everything between them but *not* that blank line, so every run left one behind and added another — the file grew a line each time and `-Check` reported stale for ever, immediately after a successful write. **A block that owns trailing whitespace has to strip that whitespace too.** The test that catches it costs nothing: run the generator twice and require the second run to report no change.
- **An index of line numbers is the project's own commonest bug shape, so it is generated rather than written.** A hand-kept table of section names and lengths sat at the top of this file and had gone out of date twice in a single day. `tools/index-docs.ps1` derives every index from the headings, and `tools/check-index.ps1` re-reads all 704 entries and asserts each one names the heading actually on that line. **If you are about to type a line number into prose, generate it instead.**
- ⛔ **PowerShell 5.1 reads a BOM-less `.ps1` as Windows-1252, so a script's own string literals get mangled — the encoding trap turned on the script rather than the data.** A one-shot written with the create-file tool (UTF-8, no BOM) contained an em-dash and a `⚠` in the prose it was writing out; PowerShell decoded the script itself as ANSI, and what landed in the document was `â€"` and `âš `. Reading with `-Encoding UTF8` and writing with `UTF8Encoding($false)` protects the *data* and does nothing for the *source*. **Keep generated scripts pure ASCII, or put the non-ASCII prose in a separate file the script reads with `-Encoding UTF8`.** `tools/check-encoding.ps1` caught it immediately, which is the whole reason it exists.
- ⛔⛔ **`Out-File`, `>` and `Set-Content` put a BOM into two more source files on 2026-08-18 — and this project had already written that rule down twice before.** The first time cost the `Explosion.cpp` incident; the second is the entry above about never rewriting a source file with PowerShell; this is the third and fourth. **Writing the rule down did not work, and the reason is worth more than the rule**: every one of these was an agent reaching for the shell's *default* way to write a file, at the end of a long task, when the edit felt trivial. The default is the trap, so nothing about knowing the rule helps at the moment it is broken. Two things do. **Write with `[System.IO.File]::WriteAllText($p, $s, [System.Text.UTF8Encoding]::new($false))` or `AppendAllText`, always, including for one-line changes** — there is no size of edit at which the redirect is safe. And **run `tools/check-ascii.ps1 -Path <file>` after every source write**: it existed, it takes under a second, it reports bytes, BOM, NUL, non-ASCII and U+FFFD in one line, and **neither agent that added a BOM ran it.** For markdown use `tools/check-encoding.ps1` instead, because documents carry intentional non-ASCII and `check-ascii` would report it as damage.
- ⛔⛔ **The mandated write method is itself the source of mixed line endings, and the fleet spent an evening blaming the wrong tool.** Four files were found damaged on 2026-08-19 — `World.cpp` 114 bare LF in 4,411 CRLF, `ItemEntity.cpp` 53 in 761, `Structures.cpp`, and `GAPS.md` at **4,725, which is the whole file** — and the broadcast diagnosis was "the `edit` tool writes LF, so any file touched by both methods is mixed." **That is backwards, and acting on it would have made things worse**, because it points away from the safe method and toward the one doing the damage. Four controlled writes to one scratch file settle it: the **`edit` tool preserved CRLF** (49 → 100 bytes, exactly +51 = three 17-byte CRLF lines, bare LF 0); **`WriteAllText` of a here-string wrote bare LF** (0 CRLF, 2 LF); `WriteAllText` with an explicit `` `r`n `` was clean; `WriteAllText` with `` `n `` wrote three. The cause is that **a here-string carries whatever newline the transport delivered, and the transport delivers LF** — so the project's own mandated `WriteAllText` is the dangerous call, not the editor. The corrected theory also explains the *distribution*, which the original did not: a whole-file `WriteAllText` gives you `GAPS.md`'s 4,725, and a partial rewrite whose replacement block came from a here-string gives you exactly the mixed minority seen in the two `.cpp` files. **So: keep writing with `WriteAllText` for the BOM reason above, but normalise the string first — ``$s = $s -replace "`r`n|`r|`n", "`r`n"`` — or use the `edit` tool, which needs no help.** **Three alternations, and backticks rather than backslashes, and both details were got wrong once each.** A two-step `` `r`n `` → `` `n `` → `` `r`n `` **leaks a lone CR**, because the first replacement only matches pairs and the second only matches bare LF, so a CR that is part of neither is named by nothing and survives: fed one lone CR, one CRLF and one bare LF it returns CRLF 2, **lone CR 1**. And writing the replacement as `"\r\n"` is far worse than it looks — **PowerShell escapes with a backtick, and .NET's replacement string does not process backslash escapes either**, so `"\r\n"` inserts the four literal characters `\r\n` and the measured output is **CRLF 0, bare LF 0**: every line ending in the file destroyed and replaced with visible text. The pattern side is safe either way (`'\r\n|\r|\n'` and `` "`r`n|`r|`n" `` agree exactly); **it is the replacement side that must be real characters.** The control that proves the fix is narrow: on input containing no lone CR, the two-step and three-alternation forms are byte-identical (`-ceq` True), so they differ **only and exactly** on the case named. This repository is CRLF throughout — **210 of 210 project text files under `game\src`, `engine`, `tools` and the root documents, measured**, and the detector that says so is not vacuous, because the same sweep widened to the reference pack finds thousands of LF-only files and six mixed ones. Verify by arithmetic rather than by eye: the byte delta after a repair must equal the count of bare LF exactly, or something else changed too. **The `edit` tool is safe on a pure-CRLF file — which is this whole tree — but it is NOT a repair strategy, and "mildly corrective" was wrong.** Two measurements settle it. Replacing a span with CRLF neighbours and no internal newline wrote CRLF and healed the region (4 CRLF / 2 LF became 5 / 1). Replacing a span whose only internal newline was a bare LF wrote **LF**, and a 29-byte subject went from 2 bare LF to **3** — the pre-existing one survived *and* the newly inserted line got one too, so the edit made the file one worse. The model that explains both is that **`edit` infers a convention from the span it replaces, not from the file**, which means on a mixed file the outcome depends on local context and can propagate LF rather than heal it. **The only reliable repair is still: normalise the whole string, write it, then re-count bare LF as the acceptance test.**
- ⛔ **The read side is the worse half, and it does not need a damaged file at all.** The bullet above is about repairing files; this is about the sweeps that search them, and *"no repair outstanding"* says nothing about a sweep. In a tree that is **210 of 210 pure CRLF**, a pattern authored in a here-string arrives carrying **bare LF**, so it matches nothing — and scores a clean `0` **across every file in the repository**, which reads exactly like a completed sweep with nothing to find. The polarity is worth memorising: a **bare `\n`** pattern is False on CRLF and True on LF; a **literal `\r\n`** pattern is the reverse. A mixed file merely shrinks a count; an LF-borne pattern against a CRLF tree **zeroes it**, and zero is the answer everyone was hoping for. **The check is one line and mechanical — ask whether the pattern string contains a literal newline at all.** No newline in the pattern means immune by construction and nothing is owed, which covers most sweeps outright: of nine patterns used across one agent's work, six contained no newline, two were `\s*`-guarded, and exactly one was at risk. **But the test is one-sided, and reading it as two-sided is the mistake waiting there**: `False` alone proves immunity, while `True` means **go look**, not **wrong** — a pattern with `\s*` around a real newline contains one and is immune anyway. So `True` is a prompt for the decisive test, never a verdict. **Keep the two axes apart and this stops being confusing**: *authoring* is whether the pattern string holds a literal newline byte — a single-quoted one-line PowerShell string makes `\r?\n` a four-character escape, never a byte — and *matching semantics* is whether the regex tolerates both endings. A pattern can be immune on one axis and deliberately sensitive on the other, which is a control's design rather than a defect. Then prove it rather than claiming it: run every pattern over the identical text in both endings and require agreement. Predicting "one at risk" and measuring **exactly one disagreement, 0 against CRLF versus 2 against LF, while the otherwise-identical guarded twin held 2 and 2**, is what makes the other eight results worth anything.
- ⚠ **A negative control that cannot match on either ending has demonstrated nothing, and this one is easy to bank by mistake.** Two separate attempts to demonstrate the shrink scored 0 on both endings and were briefly read as "no shrink exists". Both had the same hole: the planted wrap carried a `//` comment continuation on the second line, and the at-risk pattern had no provision for it, so the pattern failed for a reason that had nothing to do with line endings. **A control must differ from the claim in exactly one variable.** Fixing it so the guarded and unguarded patterns were character-for-character identical apart from the guard turned 0/0 into 0-versus-2 immediately.
- ⛔ **A mixed file silently shrinks every newline-spanning search — it misses rather than failing loudly — but only if the pattern deserves it.** The reason two attempts at the demonstration scored zero on both endings is the useful half: **`\s` matches `\r`**, so any pattern written as `foo\s*\n\s*bar` is *already immune* and always was. The shrink needs a **literal `\n` not preceded by `\s*`** — which is exactly what you get from a here-string containing real newlines. Demonstrated at last on two real adjacent lines of `Material.hpp`: **0 matches against the CRLF file, 1 against the same text as LF**, reproduced on `LESSONS.md`. **This is what decides whether a multi-line claim needs re-running**: a sweep written with `\s*` around its newlines was never at risk; one built from a here-string always was. Match with `\r?\n` regardless, and prove a sweep is ending-blind by running it over the identical text in both endings and requiring agreement — on a corpus where the count is **not zero**, because two zeros agree trivially.
- ⛔⛔ **A build verdict must rest on an exit code, because every text pattern has a hole and empty output satisfies all of them.** Three separate ways this produced a false "clean" in one evening. A grep for `error C` misses `cl : Command line error D8036` and `LINK : fatal error LNK1104`, so **a compile that never started reported "0 warnings, 0 errors"** — the friendliest possible failure to mistake for success. `%ERRORLEVEL%` inside a single-line `&`-chained `cmd` **expands at parse time**, printing `EXIT=0` for a build that failed; use `cmd /v:on` with `!ERRORLEVEL!`, or read `$LASTEXITCODE` from PowerShell afterwards. And gating on `Test-Path some.exe` is satisfied by **a stale artefact from an earlier successful run**, so the probe you execute may not be the code you just compiled — delete the output first and report its byte size and timestamp. **No pattern can fool an exit code.** Keep a text scan as a *supplement* for warnings, widened to `error|fatal|warning|D\d{4}|C1\d{3}`, and prove it works by planting one failure of each class: a bad flag, an undeclared identifier and an unused parameter.
- ⛔ **An exit code answers the question the tool asks, which is rarely the question you have** — and this is the counterweight to the entry above, because believing both at once is how a false green survives a careful reader. `check-ascii.ps1` is the worked example, and **its exit code changed twice inside one hour on 2026-08-19**: at 07:15 it exited 1 only for a path it could not open, so a present file carrying a planted BOM, a NUL or a U+FFFD **exited 0 while printing the damage on its own output line**, and gating on that code certified a corrupted file with the project's own encoding checker; by 07:30 it had been repaired to exit 1 on damage as well, with an explicit `exit 0` so a clean run stops inheriting a stale code. Both were measured, and the second falsified this very bullet twenty minutes after it was written — which is the durable half. **A written claim about a tool's exit semantics rots faster than a claim about code, because tools get repaired by whoever is annoyed by them, and nothing recompiles your sentence.** **Characterise a tool before trusting its verdict**: plant the failure it is meant to catch, separately plant one it is *not* meant to catch, and confirm the code moves only for the first. The matching PowerShell trap is that **a script which calls `exit` only on failure leaves `$LASTEXITCODE` holding whatever the previous command set**, so on success you are reading a stale value from something unrelated. Set a sentinel first — `cmd /c exit 99` — and if 99 survives, the script returned normally without writing a code. That one control proves the success path was taken *and* whether the code carries any information on success: it survived all 13 rows of a clean sweep at 07:15 and no longer survives, because the repair added the missing `exit 0`. **Plant the damage into a copy of a real file rather than a synthetic fixture**, so every detector is shown able to report non-zero on the same text that reads zero — a clean report from a detector never exercised is worth nothing.
- ⛔ **`nul`, `con`, `aux`, `prn`, `com1`-`com9` and `lpt1`-`lpt9` are device names, and Windows keeps them reserved *whatever extension you add* — so a probe file called `nul.hpp` is written successfully and does not exist.** `WriteAllBytes` returns without error, `Test-Path` then denies the file exists, and `ReadAllBytes` on the same path hands back real bytes from the null device. Two agents hit this within an hour on 2026-08-19 while building encoding controls, and the shape is the theme of that whole evening in one sentence: **a write that reports success while doing nothing, feeding a checker that then reports on nothing.** Both halves are individually plausible and neither raises an error. Deleting one afterwards needs the extended-path prefix — `Remove-Item '\\?\C:\full\path\NUL.obj'` — because the ordinary path is resolved to the device again. Name probe files with a prefix that cannot collide, and if a control ever passes *too* easily, check the file it was supposed to write actually exists and has a plausible size.
- ⚠ **Anchor a prose insertion on the *end* of the preceding bullet, never the *start* of the following one** — a start-anchor swallows the words it matched and merges two bullets into one, leaving a sentence that begins mid-clause. This cost two repairs within one hour on 2026-08-19, **the second fifteen minutes after the first had been written up as a finding**, so knowing the rule demonstrably does not prevent it and the anchor choice is the actual fix. Both were caught only by reading the sentence back: neither changed a line count, so **the byte-and-line arithmetic that catches a line-ending fault is completely blind to this one.** After any edit to a document, print the first hundred characters of each line you touched and confirm every one still starts with its bullet marker.
- **Three PowerShell 5.1 details that each cost an investigation.** A function returning **exactly one** object unrolls to a scalar whose `.Count` is the empty string, so `.Count -gt 0` is **False** — zero works, two works, one does not, and a tool whose test cases happened to be 5, 3, 0 and 0 looked correct for hours. **Wrap every collection in `@()`.** The related trap is `return , $hits`, where the comma wrap makes `@()` collect the whole array as a single element so every count reads `1`. `1..0` counts **down**, so a loop meant to be empty runs twice. And `` `u{FEFF} `` is PowerShell 6+ syntax that is **inert** in 5.1 — it does not error, it just is not an escape, so a BOM check written with it silently tests nothing.

## Process and working method

- ⛔⛔ **Built, clean, reviewed — and never called.** `CLAUDE.md`'s bug shape #15 in one line; this is what it has cost. Armour, Haste, Jump Boost, Slow Falling, sixteen `SoundEvent` rows, fifteen staged recordings, all forty-one brews and the rail curves were each **complete, correct and unreachable**, some of them for milestones. Three status effects had `constexpr` helpers with the reference's own formulas in them and **zero callers**. `SoundMaterial` never returned `Coral` and had no `Ladder` at all, so recordings that had been staged, decoded and counted at startup could not sound *by construction*. The whole brewing system was behind one missing entry in `isInteractive`, so right-clicking the stand placed a block against it. **Nothing warns about any of this**: an unused `constexpr` helper in a header is not dead code, a table row nothing indexes is data, and a startup probe that counts what *loaded* says everything is fine. The check is mechanical and cheap — **grep a new table's accessor for call sites before believing the feature works**, and when something is described as "one predicate away", the predicate *is* the feature. `GAPS.md` is the standing list of these.
- ⛔⛔ **A defaulted parameter is how an existing caller keeps the old, broken behaviour.** Five signatures gained defaults in one session and one of them inverted its own bug onto the player. `strike` gained `fromId`/`ignoreId` so that a mob's arrow hitting a bystander stopped being blamed on you — correct, and it fixed wolves turning hostile. But the player's **own two launch sites** kept the default owner, and an unknown owner *means* "the player", and the sweep skips whoever is being blamed: so every arrow the player fired passed straight through every mob within about fifteen blocks. A rare annoyance became an unmissable one, and the compiler had nothing to say because every call still compiled. **When you add a defaulted parameter to fix a bug, grep and update every call site in the same change** — a default is for callers that do not exist yet, never for the ones you are in the middle of fixing.
- ⛔⛔ **Six agents in parallel produce individually-correct, collectively-wrong work, and it is the seams that need an owner.** On 2026-08-17 six agents implemented fixes in parallel, five reviewers audited the result, and four more agents fixed what the review found. Every one of the six did its own brief correctly. The two worst faults of the day were **created by that work** and both lived between briefs: the projectile owner default above, and an autosave that wrote everything except the cursor stack and the crafting grid, because the agent that wrote the save did not own the screen code holding them. This is not the banned two-window arrangement — these were subagents under one session writing disjoint files — but it fails in the same place, which is **whatever no brief mentions**. Budget a review pass that owns the seams rather than any file, and expect it to find more than the implementations did.

- ⛔⛔ **A probe that moves has to move at a real speed, in real time, or it measures nothing.** The first version of the fly-across probe advanced a fixed step per iteration and looped as fast as the CPU allowed — which worked out at **160 m/s**, eight times the game's own sprint-fly, and gave the world the whole main thread instead of a 3 ms slice of a frame. It reported a disaster that does not happen. Pacing it to 120 fps and 22 m/s changed the answer completely: the same code that looked broken was keeping up exactly. **Print the speed your probe is actually simulating.**
- **The bug you are hunting may need a *shape* of movement, not just movement.** A straight-line flight never stranded a chunk, because a queued re-mesh stays in range until it is dispatched. It takes a **reversal** to put a queued chunk behind you and out of range before its turn comes — which is exactly what the report said: *"if i fly and then move around"*. Read the report for the manoeuvre, not just the symptom.
- **When the process is using a gigabyte and a half, ask what is resident, not what is allocated per frame.** Everything about this project's performance work had been about geometry, so the search started there. The answer was **audio**: 497 recordings and eight music tracks decoded up to interleaved stereo `float` and held for the session, four times what a mono sixteen-bit clip needs. 552 MB, in a subsystem nobody had thought about since the day it was written.

- ⛔⛔ **A `finally` does not run if you kill the terminal it is in.** A benchmark script that backs up `settings.cfg`, appends test values and restores them in a `finally` is correct — and I killed the terminal mid-run to restart it, so the restore never happened and **`frame_cap=0`, `render_distance=6`, `detail_distance=6` sat appended to the player's config for the rest of the session.** The parser takes the last occurrence of a key, so the frame limiter was silently off: exactly the bug that has been filed against a working system twice already. It was caught by grepping the file at the end, not by the script. **Before finishing, read `settings.cfg` back and check every key appears once.** And prefer letting a slow script finish over killing it.
- ⛔⛔ **When you fix a `default:` that was returning a real value, count how many defaults there are.** `blastResistance` answered 6.0 — rock — for two whole table runs nobody had range-handled, so sixteen stained glass blocks, every flower in the first run, an end rod and two torches were all blast-proof. The fix at the bottom of the function looked complete, and the harness immediately found six more: **the run-2 branch carried its own second copy of the same rock default**, so control never reached the new rules. A per-run catch-all is not a shortcut, it is another place for the identical bug to live. Make the inner one **fall through** to the outer rule rather than repeating it.
- **A harness is worth extending the moment it finds something, because the same shape is usually elsewhere.** "A soft block must not resist a blast like rock" and "a plant must not have hardness" are two lines each, and between them they found nine more faults after the first fix — including the second default above. **Encode the rule you just fixed as a check before moving on.**
- ⚠ **A harness reporting a fault in something you did not touch is as likely to be using the wrong function.** It reported all 57 spawn eggs sharing the name "Spawn Egg", which is true of `itemDisplayName` and false of `displayNameOf` — the latter lives in `Creature.hpp` precisely because `Item.hpp` cannot see the roster. **Check which function the player's screen actually calls before believing the harness.** The same run's "two blocks are both called Snow" *was* real.
- ⚠ **`logError` writes to stderr.** A probe run with `-RedirectStandardError` to a separate file shows a clean stdout log and every finding in the file nobody read. Read both, or log findings at info level.
- **`. .\tools\dev-env.ps1` is per terminal, and a terminal that gets recycled loses it.** The symptom is `fatal error C1083: Cannot open include file: 'array'` — the standard library, not your code. Re-run dev-env at the start of any new command chain rather than debugging the include paths.
- ⚠ **`Write-Host` does not go down the pipeline.** A wrapper script that captures a helper's output and greps it for the summary line gets nothing, and fails with `You cannot call a method on a null-valued expression`. Return a `[pscustomobject]` as well.

- ⛔ **A screenshot harness with a stale game still running photographs the stale game, and every build looks identical.** Three captures in a row came back pixel-for-pixel the same across two rebuilds and a settings change, and the natural reading — "my code is not running" — sent the investigation into the shader. The truth was that the first launch had never been killed, and `capture-window.ps1` finds a window *by title*, so it kept photographing the first instance. **Kill every `game` process before launching, and assert none survive afterwards.** What settled it was painting the suspect branch magenta: an unmistakable colour proves in one run whether code executes, where reasoning about it proves nothing.

- **Identical output across a change is evidence about the harness, not about the change.** Two builds that differ in the shader cannot render the same pixels. Treat byte-identical results as a broken measurement until proven otherwise — the same rule as "a measurement that comes out absurd is usually telling you about a different bug".
- ⛔ **`-Keys` on the screenshot harness is unreliable, and a debug feature that can only be reached by a keypress cannot be tested.** Forcing the weather took five runs before it fired twice; two of those runs were then read as "the sky is not responding" and sent the investigation into the wrong shader. **Give any state a settings entry as well as a key.** `start_weather` made it deterministic in one edit, and it is a better feature for the player too.

- ⛔ **Filter a build's output for `error` as well as `error C`, or a shader failure looks like a clean build.** `glslc` writes `ERROR: file.frag:120:` which matches neither `: error` nor `error C`, so a broken shader passes the MSVC-shaped filter, the stale `.spv` stays on disk, and the game runs the previous version. Two rounds of "my change had no effect" traced to exactly this.
- ⛔ **A sweep over parameter values silently re-runs the previous binary whenever one of them fails to compile.** Three contrast factors were tried for the crack shading by rewriting a constant and rebuilding a throwaway probe each time; **1.5 and 2.0 printed byte-identical histograms.** The reason was not the maths — PowerShell renders `2.0` as `2`, so the generated literal was `2f`, which is not a valid C++ float, that compile failed, and the loop ran the exe left over from the previous iteration. This is "identical output is evidence about the harness" again, and the sweep is the shape that hides it: **the failure and the stale result are one iteration apart, so nothing looks out of order.** Delete the exe at the top of each iteration, or check the compile's exit code before running it.
- ⛔ **Summing octaves narrows a distribution — do not reach for fbm when you want spread.** Randomising crack darkness with two octaves of noise would have made almost every crack the same mid-grey, because averaging N samples pulls toward their mean; it is the same distribution trap this project has now been caught by five times. One octave is still a bell: **measured, 42% of 72000 samples landed in the middle fifth of the range.** If what you want is *variety*, say so numerically — print the histogram, then widen about the centre until the end bins carry a real share. Guessing the constant is how you end up shipping one colour and calling it a range.
- **Adding fifty blocks at once means the duplicates are invisible until the compiler finds them — and it only finds half.** Nine new items were written from a research list; five of them (`ClayBall`, `Brick`, `Sugar`, `Paper`, `Book`) already existed, and nothing failed, because a duplicate *enumerator name* is an error but a duplicate *concept* is not. What caught it was the texture staging script: the reference paths for those five were already in the list, and seeing the same filename twice is what prompted the check. **Before adding a batch, grep the enum for every name in it.** The staging tool is a useful second opinion precisely because it is keyed on the reference's names rather than ours.
- **"The nearest one" can be 155 blocks away, and a probe that assumes otherwise reports a clean pass on a test that never ran.** A probe meant to strike a bee and watch it retaliate picked the nearest bee, aimed at it, and reported `struck=0` — because the nearest bee was 155 blocks off and drifting, far past both the probe's 64-block reach and the 12-block sense range. Nothing was wrong with the code under test. **Printing the distance beside the result is what exposed it**; the bare failure looked like a real bug. The fix was to *place* a subject three blocks in front of the player rather than hope one wandered close, which turned an inconclusive run into an unambiguous one — strike lands, anger set, target flips, distance holds near one block.
- **Do not infer world orientation from a rotating object's face colours.** While diagnosing the above, the cube was spinning on an arbitrary axis, so face colours (painted in model space) said nothing about which way was up. A confident claim that "left and right faces are both visible, culling is broken" was made from exactly this reasoning and was simply false — both screenshots showed three mutually adjacent faces, entirely valid. **The user proposed the fix: freeze the object in a known orientation, put it on a ground plane, and move the camera instead.** That turns "the camera is above, so I must see the bright top face" into a falsifiable test. Adopt this shape of experiment for any future "is the geometry right?" question.
- **A wide field of view close up looks like a bug and is not.** At 60° with the camera near the subject, perspective foreshortening made a cube look like a warped trapezoid. The projection maths was verified correct (horizontal and vertical scale factors produce identical on-screen size for a square). Fixed by dropping to 45° and moving the camera back. Check the maths before changing it, but treat "looks wrong" as worth investigating either way.
- **Say what success is supposed to look like before showing it.** The user reasonably asked whether "it just shows a shade of blue, that's it" was a problem — it is precisely the milestone's goal, but that had not been stated plainly enough up front. When a deliberately minimal result could be mistaken for a broken one, name it in advance.
- **A stuck input looks exactly like a physics bug, and the reported symptom was three layers away from the cause.** On 2026-07-30 the user reported that crouching at a block edge sometimes let them fall off, but only after holding Shift + Ctrl and breaking blocks for a while. Two theories were pursued and both were wrong: first that auto step-up bypassed the sneak edge guard (a real hole, fixed, but not this bug), then that some subtle ordering in the guard was at fault. The actual cause was that `glfwGetMouseButton` had latched to "pressed" — the user's trackpad double-tap generated a press whose release was never delivered — so the game was silently mining whatever the crosshair touched, **including the ground under the player's own feet**. The edge guard was working perfectly; there was genuinely nothing left to stand on. **Two lessons.** Polled hardware state you cannot reset is a liability: track button state from press/release events in our own `Window` and clear it on focus loss, so there is always a way out. And when a physics symptom only appears in combination with an *input* action, suspect the input before rewriting the physics — the tell was that the second report ("blocks break wherever I look, whether or not I'm touching the trackpad") was the *same bug* stated plainly, and it arrived only after two failed fixes to unrelated code.
- **A single timed run is not a measurement.** Startup timings varied by 40% run to run, which produced a table where one worker looked slower than zero and eight looked slower than four. Three runs reporting the minimum made the picture monotonic and obvious. Always repeat before concluding anything from a wall-clock figure.
- **Verify culling by the fraction of geometry drawn, not by looking for holes.** Frustum culling that is slightly too aggressive removes geometry only at the screen edge, exactly where it is hardest to notice in a screenshot. Comparing triangles drawn against the total gave 29.4% versus the ~28% a 100° horizontal field of view should cover — a single number that would have moved obviously if the planes were wrong.
- **The hardcoded `surface <= kSeaLevel ? Sand : Grass` was spotted by the user, not by me.** It had sat there since M5, producing a dead-flat contour line across the entire world, and it meant most block types could never occur naturally because height was the only input. It was a reasonable first pass that had stopped being reasonable several milestones earlier, and nothing flagged it. **A placeholder with no owner becomes permanent** — when one goes in, name the milestone meant to replace it.
- **Creative mode means "placing is free", nothing more.** It has now caused two bugs by meaning more than that: suppressing pickup left dropped items orbiting the player forever, and suppressing drops meant breaking a block in creative destroyed it. The second was the user's call to fix (2026-07-31): _"items should drop no matter what gamemode i'm in."_ **Resist adding mode checks to anything except the cost of placing** — every one is a branch where the two modes can behave differently and only one gets tested.
- **Whether it is one thing or three is a question about the art, not the code.** The furnace's burn indicator is *three* flame licks in the reference, and the user's own mockup shows three grey strokes — so the first attempt drew three, and at 4 pixels wide each they read as lit matches. The user had said "**the** flame is orange bordered and yellow in the middle/core", which describes a rim and a core, and a rim and a core need width. One flame across the full 14 pixels worked immediately. **Read the user's description as a spec and the reference as context, not the other way round.**
- **Every structural image metric here is computed on luminance, so none of them see colour.** A muted pink and a vivid coral can match face for face on brightness and look nothing alike - which is exactly how a washed-out pig passed. The `Sat` and `Lum` checks exist because the rest of the table is blind by construction, not by oversight.
- **Redirected stdout is block-buffered, so an empty log does not mean a dead process.** Checking for a startup line 18 seconds into a run found a zero-byte file and looked like a crash; the process was healthy and simply had not filled its buffer. Check `Get-Process` before diagnosing, and read the log after the run has produced enough output — or close the game, which flushes.
- **A band narrower than the formula predicts is information about where the box is buried.** The wolf's body shows 6 columns where 12 are predicted, and crucially at `u+d+w` rather than `u+d` — so it is the *top* rect that is transparent, and after the torso's quarter turn that face points forward, which is where the mane must sit. Reading the missing rect placed a second box before any code was written. **Do not treat an unexpected width as a measurement error; work out which rect is absent and ask what covers it.**
- **Research the reference before designing a system, not after — and use the right model for the agents.** Two rounds of subagent research (2026-08-02/03) produced `RESEARCH.md`, and the first round was materially worse because the `model` parameter was omitted and the calls fell through to a small default; three of four reports had to be topped up by hand. **Always pass `model: "Claude Opus 5 (copilot)"`.** Launch several agents in one block so they run in parallel, and give each a tight scope with "exact numbers, note Java vs Bedrock, cite the page" in the prompt — the reports that asked for provenance came back with derivations that reproduce the wiki's published figures exactly, and the ones that did not came back as prose.
- **The villager's arm join is good enough and the user is done with it.** Direct signal, 2026-08-03: _"not perfect but eh, whatever i guess. i'm tired of iterating through hands."_ It is closed, not solved. **Do not re-open it unprompted** — and if it ever is re-opened, the remaining honest options are per-face culling for buried faces, or dropping the crossbar and accepting the loss of the crossed-arms pose. Nudging the numbers again is not one of them.
- **A drive that fades out as it approaches its target cannot oscillate, and a latch that does not fade cannot be gentle.** Tapering the kick to nothing as the head clears is first-order, so it settles dead - the player floats motionless, which was the *first* complaint. Latching it across a band cannot settle, but at the climb speed it overshoots enormously, which was the *second*. The answer is both: latch the band **and** drop the speed. Neither alone is a fix, and I shipped each of them alone once.
- **A probe that stops sampling when it changes phase measures the phase, not the thing.** A TNT fuse reported 4 blinks where 16 were expected, which looked exactly like a broken fuse; the sampler was written as `if (phase == 1 && ...)` and phase 1 lasted 1.2 seconds. The fuse was perfect. **Any counter that must span the whole run has to be outside the phase machine**, and the giveaway is that the wrong number is suspiciously close to the phase duration divided by the tick.
- **Give a destructive test room, or it eats its own evidence.** Two charges and a pair of chests laid out over twelve blocks all fell inside one blast, so the run reported a short fuse, no landing and no chests — three failures, none real. Separating the sites by forty-eight blocks turned every one of them green. **Anything that destroys terrain needs its subjects further apart than the effect it is being tested for.**
- **A one-run table audit is worth more than a day of reading, and it is about a hundred lines.** After two batches added ~160 blocks and ~90 items, a temporary startup pass walked every block and every item and cross-checked the tables against the *loaded textures*: cutout blocks whose art has no holes, opaque blocks whose art is see-through, item icons that are solid squares, layer indices out of range, layers nothing references, duplicate names, `itemForBlock`/`blockForItem` round-trips, zero hardness, unreachable harvest tiers, blocks that mine into nothing, recipes wanting items outside the catalogue, and recipes shadowed by an earlier identical pattern. It found four real bugs in its first run, in code that had passed "both presets clean, zero validation errors" many times. **The cross-check against the texture array is what made it powerful** — the tables agree with themselves perfectly; it is the art they disagree with.
- **Read a probe's zeros against what it can actually reach.** The same audit reported 78 "unused" texture layers, and every one was a false positive: the water and fire animation frames are swapped in at draw time and never named by `blockTextureLayer`, and the direction-dependent faces (furnace, smoker, smithing table, chest halves) only appear when a real `FaceDirection` or `ChestHalf` is passed, which a table walk has none of. **Before believing a diagnostic's negative, ask which code paths it never exercised.**
- **Vision and aiming are different questions and must not share a ray.** `seesPlayer` called `raycast`, which walks `selectionBoxes` - correct for "what am I pointing at", because you must be able to aim at a tuft of grass, and badly wrong for sight. **Tall grass therefore hid the player from a creeper**, so a Bramble crossing a meadow lost sight of you every few steps and its fuse stuttered up and down. `Block.hpp` has owned the right predicate the whole time: `isOpaque`, documented as *blocks vision* and deliberately kept distinct from *blocks movement*. `hasLineOfSight` now lives beside `raycast` so there is one ray walker and two questions, rather than two walkers.
- **A model that reads as "lying down" is usually missing a rotation, not a height.** The spider's body sat at 0.40 with its eight legs as level bars either side of it, and the comment justifying that said the reference "angles each leg down at the knee, which an axis-aligned box cannot do". It does not need a knee: the reference droops each whole leg 45 degrees (33.3 on the inner pairs) about its own forward axis from a joint level with the body, and **that droop is the entire reason the animal stands off the ground**. Rebuilt from `geometry.spider.v1.8`, where thorax, head, abdomen and all eight joints share one height. The tips end about a tenth of a block *under* the floor, which the reference does too and which is what makes it read as planted.
- **A positional table stops being a table once it outgrows a screen, and the tell is a field you cannot set without restating a dozen others.** `kSpecies` was thirty-six rows of bare values with "every field with a default goes at the end" as the rule holding it together. That rule works exactly until you need to set a late field on a row written before it existed — at which point you are retyping playtested numbers to reach the one you care about, which is the highest-risk edit in the codebase. Converting to designated initialisers cost nothing at runtime, made rows say only what makes them different, and turned a mis-ordered value from a silently wrong animal into a compile error. **Do this before the field you need forces you to, not after.**
- **The old distance hysteresis was standing in for something the reference does differently, and the honest fix was to copy the mechanism rather than keep the workaround.** `senseRange * 1.4` existed because a target sitting exactly on the sense boundary flipped every frame. Bedrock has no such widening: it scans on a `scan_interval` of ten ticks, so the question is simply not asked often enough to flicker. Taking the cadence removed the fudge *and* paid for a line-of-sight ray for the whole roster. **A constant with a comment explaining why it is a fudge is usually a missing mechanism.**
- **"Small and decorative" is not a reason to skip an axis, and the bug it caused looked nothing like the shortcut that caused it.** Dropped items resolved *vertically only*, documented as not worth the cost. So a drop could enter a block sideways - and once inside, the ground probe found **that** block and snapped the drop to its top, which the user reported as items climbing steps. **A partial resolver does not fail partially; it fails somewhere else entirely.** The fix was to call `overlapsSolid` and `highestSurfaceBelow` like everything else that collides, which is what `SYSTEM_MEMORY.md` had said to do all along.
- **A test written as "equals the empty case" instead of "answers the question" quietly excludes everything that arrives later.** A fluid drew its face where `ahead == BlockId::Air`. That was true while nothing but air ever sat beside water, and it silently became wrong for every non-cube added since: a lily pad, a torch, a fence, a pane of glass or a plant standing next to water **deleted the water's surface there**, because the neighbour is not air and does not supply the water's own face either. The right question already existed as `occludesFace`, one line away in the same expression's other branch. **When a predicate compares against a specific value where a general test exists, treat it as a bug waiting for its second case** — and note that the symptom was reported as "lily pads make holes in the water", which named the newest arrival rather than the eight-month-old rule.
- **Fixing the cell a thing occupies does not fix the face beside it.** Waterlogging was built so a plant and water could share a cell, and it correctly fixed seagrass and kelp. When lily pads produced *the same visual symptom*, the obvious reading was that they had missed the same fix — and they had not: the pad genuinely sits in the air cell above the surface, and the hole came from the neighbouring water's face rules instead. **Two identical symptoms in the same subsystem can still have different causes; the second one is not automatically the first one repeated.** What settled it was counting the erased faces in the generated world and grouping them by which block erased them: 204, every one a lily pad, kelp and seagrass at zero.
- **A rule stated once in prose and re-implemented in a helper is the same second copy that keeps costing this project.** `SYSTEM_MEMORY.md` said plainly that a recipe pattern is *stored at its own size, not padded to the grid*, and the matcher compares against the bounding box of what is in the grid. The `tool()` helper padded every shape to 3×3 anyway. Result: **the axe, shovel, sword and hoe were uncraftable at every tier — sixteen recipes — and only the pickaxe worked**, because it is the one shape that fills all three columns. Nothing failed, nothing warned, and the tools appeared in the catalogue as normal. The check that caught it is worth keeping: **feed every recipe's own pattern back into the matcher and assert it makes itself.**
- **A `default:` that returns a real value hides every block that was never considered — and the second run's default was "needs a tool".** `harvestTier` gave every one of ~200 appended blocks the wood tier, so punching a plank, a wool block, a plant or any soil in that run destroyed it and dropped nothing. The function's own comment stated the correct rule two lines above the bug — *only stone-family blocks withhold their drop* — so the fix was to write the comment as code (`harvestTool(block) == Pickaxe ? kWoodTier : kHandTier`). **The same shape twice in one function:** `harvestTool`'s run default was `Pickaxe`, which had quietly reached seventeen plants. Both were invisible in creative, which is how the whole batch shipped.
- **"Where can it rest" is two questions, and asking only the second lets two things occupy one cell.** A falling block scanned downward for the first cell whose *neighbour below* would hold it, and never checked whether the cell itself was free. Two sand falling down one shaft therefore chose the same floor, and the second settled into the first and converted it into a dropped item — three sand in a column left two blocks and a stray drop. **Any "find a resting place" loop needs both halves: this cell is enterable, and the one under it is not.**
- **Light removal has to mirror light *propagation*, including its shortcuts.** Sky light falls straight down at full strength, but removal used the textbook "neighbour is dimmer than the source" test, which can never fire on a cell lit at exactly the same level. So placing a roof cleared one cell and stopped: **a sealed room stayed at full daylight.** The general lesson is that a flood fill and its inverse are one algorithm written twice, and **every special case in the forward direction needs its mirror in the reverse one** or the asymmetry shows up as light that will not go away.
- **A destructive test needs the terrain the feature actually assumes, not just space.** The lava-quenched-by-water scenario reported a real-looking failure — water never reached the lava — and the code was perfectly correct: the source sat one block from the edge of the test platform, so the flow found a drop and ran off the side, which is documented, intended behaviour. **Walls are part of a fluid rig.** The tell was that the "bug" reproduced the one water behaviour that had already been investigated and confirmed correct once before.
- **A world that is doing nothing is a testable claim, and it is one line.** Snapshotting every block in a 96×48×96 box around the player, waiting six seconds with no input, and re-comparing catches any oscillation anywhere in the loaded world rather than at the one site a scenario happens to look at. It is the general form of the water-flicker hunt that took most of a session, it costs two passes over an array, and it prints a per-block-type breakdown of anything that moved. **Run it before building test sites, while the world is still pristine.**
- **Fuzz the pure functions that move items between slots; it is twenty lines and it covers the worst bug class here.** Five hundred random benches, forty random operations each, asserting after *every single operation* that the multiset of items is unchanged, no count exceeds its stack limit, and no slot holds a positive count of `None`. Item duplication has been the most severe bug this project has had, and the operations are free functions over two stacks, so nothing needs a UI to exercise them.
- **"Is there a recipe for it" is the wrong question; "what produces this entry at all" is the right one.** Walking the catalogue and asking which block drop, recipe, smelt or mob kill yields each item turned a vague sense that content was missing into a list of 88 names — and the list immediately sorted itself into three kinds: things needing a *recipe* (fixable now), things needing a *system* like farming (not a bug), and things that are creative-only **on purpose** because they drop a resource rather than themselves, exactly as the reference does. **Do not treat a long unreachable list as one problem.**
- **A family that repeats is a loop, and writing it out by hand is how one cell gets the wrong ingredient.** Eleven woods times eleven shared shapes is 121 recipes; sixteen colours times three dyed families is another 48. Generated from a small table each, they are a dozen lines and cannot disagree with themselves. The rule that makes it safe is that **every run involved is declared in the same order** — wool, terracotta, concrete powder and the dyes are all white-first — so a colour is one offset rather than four lookups.
- **When most of a table becomes generated, add the check that a generated row is still a valid row.** Two cheap assertions: feed each recipe's own pattern back through the matcher and confirm it produces itself, and key every pattern into a map to catch two recipes claiming the same shape (the second is dead code the compiler is perfectly happy with). Running both over 340 recipes costs milliseconds at startup, and they are what makes generating three hundred rows safer than writing thirty.


- ⛔⛔ **A control has to do five things, and every one of them was learned by a control that failed while looking like it worked.** "The control fired" is not the test. **(1) It must differ from the claim in exactly one variable** — this is the capstone and it subsumes the rest, because every failure below is two variables moving or none. The purest case: a probe declared `$LF` as a `[char]10` constant and later assigned `$lf` as a counter, **which in PowerShell is the same variable**, so claim and control were both computed from the corrupted value and the control returned exactly what was expected while testing nothing at all — differing in *no* variable. **(2) It must fire** — a zero from a test that cannot produce a one is not evidence. **(3) It must differ in the expected direction** — one agent's detector was broken by `return , $hits`, so its claim returned `1` and its control returned `1`; the control "fired" and proved nothing. **(4) It must differ by a plausible magnitude** — a control landing at 2 against claims that range 0–2 has distinguished nothing; plant *n* faults and require the instrument to report *n*, not merely non-zero. **(5) It must not be vacuous on both sides** — a citation sweep compared over two line endings agreed 0 to 0 across thirteen files, which is no agreement at all; re-run on a corpus that actually contains the thing (`LESSONS.md` 4/4, `DECISIONS.md` 7/7, `UI.md` 399/399) and report a 0/0 case as vacuous rather than counting it as a pass. **A graded control satisfies all five at once**: hold everything fixed, plant one fault and see 1, plant three and see 3, against a claim of 0.
- ⛔ **A uniform result is the instrument, not the world — but check, do not assume.** Twice in one evening a suspicious cluster of identical values exposed a broken tool: eleven unrelated functions all reporting "exactly 1 use", and seven unrelated names plus three unrelated controls all returning `1` from the comma-wrapped detector above. The tell in both cases was *sameness across things that have no reason to be the same*. The counter-example matters just as much: a fourth agent's four identical `1`s turned out to be real, because `InventoryScreen.cpp` genuinely has four consecutive `case` labels. **So uniformity is a prompt to check the instrument, never proof that it is broken** — and the way to settle it is to look at the thing itself, not to re-run the count. Corollary for a negative result: thirteen files all reading zero is the same shape, so add a **corpus control** — run the identical instrument over material you never touched and require a non-zero. If nothing in the tree can produce a one, the tool is the finding.
- ⛔⛔ **A name is only unreached if a *bare-name* search also finds nothing, and this produced four near-regressions in a single evening.** `CLAUDE.md`'s bug shape #15 says to grep a new function's own name for call sites before believing a feature works. That grep has a large false-positive class, because **a value routed through a struct, a table or an index cast is invisible to a call-site sweep**: about forty names in `Creature.cpp` appear exactly once because they sit in a behaviour table *without parentheses*; sixteen `CreatureKind` enumerators are written by an index cast and never named; `MeshDetail`'s LOD is read out of a struct; and `familyRoughness` was reported as having "19 references, all inside its own file, zero live callers" when the live path runs `buildMaterialTable` → `packMaterial` → a packed row → `setMaterialTable`, which the GPU then reads — decoding the row proved an iron door gets roughness 0.302 and metallic 1.000, exactly right, and the "fix" would have been a regression. **Each of those four would have caused one if acted on.** So the rule gains a second half: **before declaring anything dead, search the bare name with no parenthesis, and follow the data as well as the calls.** And when you *do* report something unreached, date the claim and name what would falsify it — negative claims rot fastest, because the world only has to move once.

## Survival, audio and the M21-M22 round

- **Assigning a handle at the call site rather than at the single point of insertion means everything the system creates itself gets zero.** `Creature::id` was set in the two places a *spawn egg* and the *debug showcase* built a creature, and in none of the three places the world spawned one — so a natural population all carried id 0 and the brand-new "who am I fleeing from" lookup answered "the first creature in the array" for every animal in the world. The probe printed `wolf(id 0 ...)` next to `sheep(id 0 ...)` and it was obvious; nothing else would ever have shown it. **One private `add()` owns insertion and owns the id.**
- **`floor(position.y)` on a body resting exactly on a surface names the block it is standing *on*, not the one under its feet.** Grazing therefore always turned the wrong block — the sheep ate the air it was standing in. A `+0.1f` nudge before the floor fixes it, and the real fix is that `grazeCell` is now the only place that arithmetic exists. **This is the third time a floor-boundary has cost a bug here**; the first two were spawn placement and step-up.
- **"Is there anything here to act on" is part of a behaviour, not an optimisation on top of it.** A sheep on snow mimed eating for 1.8 seconds and turned nothing, because the start condition only asked whether the animal *could* graze, never whether a graze block was under it. The reference states this explicitly as `eat_and_replace_block_pairs` — the behaviour will not begin without a pair to act on. **If a behaviour has a target, its start condition must find one.**
- **A stale branch left behind after a feature is rebuilt does not conflict, it just wins.** M21 gave eating a held 1.6-second timer; the old instant-consume branch from the days before hunger was still sitting forty lines below, so every meal was swallowed whole on the first frame and the new timer could never finish. Both compiled. **The survival tests called `feedPlayer` directly rather than through a click, which is exactly the gap that let it through** — when you replace a mechanism, test the *path the player takes*, not the function you wrote.
- **Twelve foods were edible and worth nothing, because edibility and food value were two tables.** Rotten flesh, cooked rabbit and cooked salmon among them — all three listed in the research. `isEdible` now asks `foodValue`, so the two cannot disagree. Same shape as every other "second copy" bug here, and the cheapest possible fix once it is seen.
- **`/W0` after a global `/W4` trades one warning for another.** Silencing a third-party C file that way produces `D9025: overriding '/W4' with '/W0'` — the project-wide flag is still on the command line. **Disable by number** (`/wd4701;/wd4244;...`), which also documents which warnings the file legitimately trips.
- **`NOMINMAX` is mandatory before any single-header library that pulls in `windows.h`.** Without it, `min` and `max` are macros and every `std::min` in the translation unit fails with an error that names the standard library rather than the header that caused it. `WIN32_LEAN_AND_MEAN` belongs in the same line.
- **A probe whose "expected" comment looks wrong can still be right — check the arithmetic before changing the code.** The status bars reported 160 and 124 vertices against a comment predicting something else; 160/4 is exactly 40 icons and 124/4 is exactly 31, which is what a full health row plus a partly-eaten food row should be. **The probe was correct and the comment was a guess.**
- **A sound bank fails silently, so the only honest test is to ask every row whether it found anything.** An event whose stem does not match what the staging script wrote simply never sounds — no warning, no error, nothing at startup. Loading 497 files and then asserting that all 57 events and all 102 voice banks are non-empty takes one pass and is the difference between "it built" and "it works". **Any table split across a script and a source file needs a probe that walks both ends.**

## Villages, villagers and the golem (M29d)

- **A structure's spacing and separation are quoted in CHUNKS, and our chunk is twice the reference's.** Writing the reference's 34 into a chunk-counted field would have doubled every village distance in the world with nothing to report it. Both constants are stated in **blocks** and never converted. Fifth appearance of the ported-number-in-the-wrong-unit trap.
- **The workshop's counter overwrote the job block on two of its four facings.** The counter runs along whichever wall faces the street, and the workstation sat at a fixed interior corner; for two facings that is the same cell. Every second shop had a slab where its trade should have been, and both a clean build and a clean soak had nothing to say about it. **A probe that counted job sites in a generated village found it in one run.** Fittings go in before the thing they are fitted around, and the job block is placed last.
- **The site test was rejecting 79 per cent of every biome-eligible cell and nothing said so.** Villages came out one per 2000 blocks and looked plausible. What found it was a **reject histogram** — four counters naming which gate fired — rather than a total. Plan::reject exists for exactly that. **A rarity that "seems about right" is a measurement nobody took.**
- **Solve the whole layout before touching a chunk, and hash every per-cell cosmetic choice off its world position.** The layout stream must draw in a fixed order whatever the outcome, which is the placeVein rule again; but anything decided per cell — which path block stays grass, which stone is mossy, what a farm cell is planted with — should not come from the stream at all. Hashing on (x, z) removes the ordering hazard rather than managing it.
- **Widening the behaviour table past sixteen rows would have overflowed 
unningBehaviours silently**, and the symptom would have read as a pathing bug: a behaviour that forgot it was running gets asked canStart instead of canContinue, which is a tighter question. The static_assert caught it because it was written when the mask was. **Assert the width of any bitmask indexed by a table's size.**
- **A behaviour priority is an ORDER, not a number to port.** Bedrock puts work at 7 and 
andom_stroll at 11; ours strolls at 6, so copying the 7 would have left a villager ambling through its own working day. The same reading is what keeps Sleep above Avoid so a sleeping villager does not flee.
- **Ask the narrow family before the wide one, every time.** professionForJobSite tests isBlastFurnace and isSmoker before anything reaches isFurnace; the other order would make every cooker in the world an armourer's. This is the "widening a predicate kills the early-outs behind it" trap seen from the other side — the fix is the same, and it is to know which test is the general one.
- **A claim is per-creature state and nothing else may record it.** Two villagers must not own one bed; the answer is a linear scan of a population measured in tens, not a map on the container. Same argument the double chest settled. It cannot go stale, needs no save of its own, and breaking the block simply makes the next scan drop the claim.
- **A bed is two blocks and only one of them is claimable.** Claiming either half would let two villagers own the same bed. `bedIsHead` is the whole of the fix.
- **Verify a structure by generating it and counting, not by looking at it.** Floor holes, air under the floor, missing doors and blocked doorsteps are four counters over a whole generated village, and all four must read zero. None of them fails loudly, and three of them are invisible from any angle you would happen to stand at.

## Block models and the playtest round (M29e)

- **A `static_assert` that compares one side of a derivation against itself proves nothing.** Eleven model layer constants were written as `extraBlockInfo(...).layer`, which is an *offset into the appended run*, and asserted against `extraBlockInfo(...).layer`. `blockTextureLayer` adds `kTableSpritesFirst` to that offset, so every one of them pointed at a nether plant — a lily pad for the brewing stand's rod, weeping vines for the bell — and the build was clean. **Assert the whole expression the real reader evaluates, base and all.** What found it was a twenty-line startup probe printing the file name at each layer index.
- **A workstation that is a `Full` cube is a hole in the world.** Six of them were, so each culled its neighbours' faces *and* kept its own opaque, and a cauldron showed you the sky through the two blocks behind it. Two questions, not one: is it `Model` (so it never occludes) and is it `isCutout` (so its transparent texels are discarded). Both are now `static_assert`ed for all seven.
- **A thin collision box has to know which side you came from.** `blockingPlaneAlong` took the nearest face over every overlapping box — right for a full cube, wrong for a 3/16-thick door leaf. Standing inside the leaf's cell, the plane *behind* you was picked and you were pushed out the far side. This is the "walking into a door teleports you" report, and it could never have shown on a cube.
- **A screen with no crafting grid still runs the crafting-grid maths.** `craftSlotCentre` does `index % layout.craftSize`, the stonecutter's `craftSize` is 0, and an integer divide by zero is a **hardware fault**, not an exception. The game froze and exited the instant a stonecutter was right-clicked, and it read as a memory leak. **Any `%` or `/` by a layout field needs the zero case named at the top of the function.**
- **`isSkyTransparent` is a list, and every mostly-air shape has to be on it.** Fences, walls and gates were missing, so a fence line stopped sky light dead and painted a hard black stripe on everything beside it. Villages are fenced, which is where it showed. Ask it alongside `isSolid` and `occludesFace` whenever a shape is added.
- **The mesher's automatic U flip is right for a texture that reads the same from every side and wrong for one that marks a world direction.** A bed's side is red mattress at the join and white pillow at the head; the flip put the pillow at the wrong end on exactly one of the two long sides. `ModelBox::unmirror` names the one face to undo it on. The same block needed `lidTurns` for the same underlying reason — the box mesher cannot rotate a model, so anything asymmetric has to say which way round it goes.
- **A block texture may be a net rather than a face, and the transparent rows are the clue.** `red_bed_foot_side.png` is nine-sixteenths of a bed: rows 0–7 empty, 7–13 the mattress and frame, 13–16 **three 3×3 leg faces packed side by side**. Sampling it as a plain face from row 0 drew mostly nothing and no legs at all. **Dump the pixels before choosing a rectangle** — `tools/dump-pixels.ps1` answers it in one command.
- **Having found one net, the next texture was assumed to be a net too, and it was not.** The bed's *top* is one whole face — the reference's `template_bed_head` samples `up` at uv 0,0–16,16 over the entire cell — with the pillow in rows 0–7, the shadow it casts in row 8 and the blanket in rows 9–15. Row 8 was read as a transparent separator dividing two pieces, so the head's lid rect was cropped to the pillow; that stretched the pillow over the whole head half and deleted the blanket, leaving red only from the join outward. **Row 8 is a dark red line and there is nothing transparent in the image at all**, which the same one-command pixel dump would have said. Having just been caught by a net is exactly when the next texture gets called one; the fix is not to look harder but to **read the reference's own model JSON, which states the rectangle outright** — and it is already in the repo under `reference/.../models/block/`.
- **A dyeable item's own layer has real holes in it.** Leather armour's trim and buckles live in a separate untinted `_overlay` sprite; staging only the tintable half gave every leather piece random transparent gaps. The staging script's overlay support existed for grass and applied its tint unconditionally, so an overlay without one threw — the tint is optional now.
- **A model box needs its own texture layer, not just its own rectangle.** A bell is stone posts and a plank bar round a gold bell; a grindstone is a stone wheel between two log legs. Without `sideLayer`/`lidLayer` every post came out gold.
- **The reference's block JSON is the fastest way to get a model right, and it is already in the repo.** `reference/minecraft-assets-26.2/.../models/block/*.json` gave the bell's stone posts, the grindstone's five boxes and the brewing stand's three base plates verbatim. Guessing geometry costs a render each time; reading it costs one command.
- **Redirected stdout does not flush when a process is force-killed.** Two probe runs came back with a zero-byte log before `CloseMainWindow()` plus `WaitForExit` was used instead of `Stop-Process -Force`. If a probe prints nothing, suspect the kill before the probe.

### The table sweep of 2026-08-10, and what "solid cube" was hiding

A temporary startup sweep walking every block, item, recipe and model, cross-checked against the **loaded texture array**, went from 485 faults to 0 in five passes. Everything it found had been shipping for milestones and had passed `/W4` and a clean soak every time.

- ⛔⛔ **Twenty blocks were full opaque cubes drawn from art with real holes in it.** Copper grates are a lattice, an anvil and a campfire and the sculk pair are shorter than their cell, scaffolding and the enchanting table and the spawner are open frames, the end portal frame is thirteen texels tall and a hopper is a funnel. The test that found them is one line: **a face that fills its cell and is not a cutout must have no transparent texel in its art.** Twenty blocks, one predicate, and not one of them was visible in any table.
- ⛔⛔ **Marking them cutout is only half the fix, and shipping only that half is worse than nothing.** The user's words were *"the end block and other block has a gap in between it"*: once the empty texels stopped drawing as opaque black they stopped drawing at all, so a thirteen-texel frame became a thirteen-texel frame with a cube-shaped hole above it. **A block whose art has holes because the model is smaller needs the model, not the discard.** Ten of the twenty got real geometry out of `models/block/*.json`; the three that really are cubes — the grates and the spawner — kept theirs.
- ⛔ **192 trapdoors stopped sky light dead.** `isSkyTransparent` is a list of shapes, fences and walls and gates were added to it at M29e, and trapdoors were missed by the same pass. One line. **Every time a shape is added, ask that list.**
- ⛔⛔ **The compass and the clock had been given the cauldron's recipe**, seven ingots in a U instead of four round a redstone — and because the compass is declared first it **shadowed the cauldron, which could not be crafted at all**. The guard that catches this is two lines and was already the rule: key every pattern into a map and assert no two claim one shape. It had been written for a generated table and never run over the hand-written rows.
- ⛔ **A composter's contents were invisible at every level below the last.** The plate samples the middle twelve texels of the tub's *top* texture, and `composter_top.png` is a rim with a transparent middle — the reference ships `composter_compost.png` for exactly this and it had simply never been staged. Its rim went the other way at level eight: the ready picture is painted *only* in the middle, so the four walls' top faces drew nothing. **A model box that samples a rectangle nobody would ever paint is telling you the texture is missing, not that the rectangle is wrong.**
- **The check that found both is worth keeping in any future sweep: for every model box, take the rectangle and the layer the mesher will actually use and assert at least one texel in it is opaque.** Side and lid separately, because a net paints them in different corners. It caught the bed's invisible legs, the composter twice and the stonecutter's saw.
- ⛔ **A probe that logs its findings needs no cap on the count until you have read the summary.** The first pass printed 220 of 485 faults and the grouping looked like three categories; the missing 265 were a fourth that mattered more. **Log the total separately from the detail.**
- **Two of the first pass's faults were the test being wrong, not the code**, and both took a minute to settle: a `Hovering` block is a full cube lifted a texel, not a mostly-air shape, and a composter's contents plate is sealed inside the tub so its side faces are culled and never draw. **When a sweep fires on something the eye says is fine, check the predicate before the code.**

### Measuring every block against its own model (M29g)

- ⭐⭐ **The reference states the size of nearly every block we have, and for twenty milestones nothing read it.** `models/block/*.json` names every element's `from` and `to` in sixteenths. `block_probe=1` dumps what we draw and `tools/check-models.ps1` compares the two: **2184 blocks, 1293 with a model to check against, 0 disagreeing.** The player asked for this outright — *"i think you should be able to code up a script to do it for you"* — and they were right that it should have existed already. **A guessed extent is the most expensive bug shape here; this is the tool that ends it.**
- ⛔ **The first run reported 175 wrong and almost none of them were.** Four things a naïve comparison gets wrong, and any one of them buries the real faults: **a block usually has several models** (a stair has three, a door four, a fence two), **a rotation is not a disagreement** so compare sorted extents, **a connecting shape has to be dumped unconnected** or a lone fence is measured against a crossroads, and **some families are named differently on each side**. **A checker's first output is a statement about the checker.** Get it to near-zero before believing a single line of it.
- **Name the deliberate divergences in the tool rather than widening the tolerance.** Three blocks disagree on purpose; each carries a sentence saying why, and a *different* disagreement on one of them still shows up. A tolerance wide enough to hide them would hide real faults too.
- ⛔⛔ **"Read the reference's JSON" fails silently when there is no JSON, and the bell is the case.** `bell_floor.json` is the frame alone — the bell body is a block entity and appears in no block model anywhere. Three attempts went into geometry that had to come off the artwork instead, and the two faults were the same both times: **the body shared a plane with the bar it hangs from**, and **it sampled the transparent column beside its own texture**, drawing a stripe down its face. When the JSON does not have it, say so and measure the art — do not keep guessing and calling it the reference.
- ⛔⛔ **A fault the world hides can be plain everywhere else.** Every non-flat block item was dropped as **one spinning cube** wearing its side texture: a bell was a gold brick, a torch a cube of torch, an anvil a black cube. The placed blocks had all been fixed; nobody had thrown one on the floor. **When you fix how a block is drawn, ask where else it is drawn** — the slot picture, the dropped entity and the thrown entity are three more places.
- **The fix was to give the floor and the hotbar one owner.** `iconBoxes` answers "which boxes is this block drawn as" for both, and the slot picture's private copy of that switch is gone. Two renderers of the same thing will drift; one function cannot.
- ⚠ **A fix aimed at one class must not swallow the next.** The player caught this before it shipped: *"it shouldn't apply to objects that have 2d drop sprites."* Flowers, ladders, vines, panes, tools and buckets are *pictures*, and turning them into little cubes is the same mistake pointing the other way. `usesFlatIcon` is the line between the two and it is asked first.
- ⛔⛔ **A temporary showcase that writes to the world and is left in the build is not clutter, it is data loss.** The bell showcase cleared a 21×19×14 region around the spawn point and moved the player, on **every startup** — and it survived a round of "cleanup" because the check used was a grep for the word `TEMPORARY` in a terminal whose output was scrolled past. The player then ran that build. **Two rules, and the second is the one that was missing: a probe that only reads may live for a few builds, but a probe that calls `setBlock` is deleted in the same command that stops the game. And the check that it is gone must be a build that *runs*, not a grep** — `soak.ps1` printed `bell showcase up` as a warning line the moment anyone looked at its output.

### The bed took four rounds of playtesting, and all four were one mistake

The user's words at the end were *"THIS WAS SO IRRITATING"*, and they were right: the legs, the top, the head's end face and the two missing legs were four separate bug reports about **one block**, and every one of them came from the same root cause. Worth reading as a set rather than as four entries.

- ⛔⛔ **THE ROOT CAUSE, once, for all four: a multi-face block was implemented by guessing rectangles out of the images instead of reading the model that names them.** The reference's `template_bed_head.json` and `template_bed_foot.json` state every face of every box — `from`, `to`, the `uv` rect *and which texture* — for the mattress and both legs. Ten lines of JSON, already sitting in `reference/.../models/block/`, answered all four reports before any of them was filed. **When a block is more than a cube, open its JSON first. Not the pixels, not the wiki, not reasoning about the art — the JSON.**
- **A guess that produces a plausible picture is the expensive kind.** Every wrong version rendered *something*: a mattress with no legs, a pillow stretched over half the bed, a headboard half red. None crashed, none warned, both presets were clean at `/W4` and the soak was clean every time. Only the user's eye caught any of it, four separate times.
- **Reading one net right does not license reading the next one the same way.** The bed's *side* really is a net (three leg faces packed into rows 13–16), and having just decoded it I called the *top* a net too — reading row 8 as a transparent separator. It is a dark red shadow line, nothing in that image is transparent, and the top is one whole face at uv 0,0–16,16. **The pattern you just learned is the one you are about to over-apply.**
- **A texture the reference does not have is a texture the reference has *somewhere else*.** The headboard came out half red because it was drawn from the long-side image, which is half pillow and half blanket. The reference does not stretch or crop that image — it ships a **separate `bed_head_north.png`**, shared by all sixteen colours, white across the whole mattress band. **If a face looks like it needs a sub-rectangle nobody would ever paint, the answer is usually a texture you have not staged.**
- ⛔ **We stage one image where the reference ships two, and that asymmetry has to be paid for somewhere.** `*_bed_head_east.png` and `*_bed_head_west.png` are mirrors, and staging only `east` is what made `ModelBox::unmirror` necessary at all. **It also moves the leg faces**: the foot's side image packs its legs at u 0–9 and the head's at u 7–16. Reading the foot's corner for both left the head's two legs sampling empty texture, so every bed in the world stood on two legs — and nobody noticed until the user looked underneath. **Staging half of a mirrored pair is a decision with consequences at every rect that image feeds, not just the one you were thinking about.**
- **Verifying "the bed" is not one picture.** A two-block block has a top, two long sides, two end faces, four legs and four facings, and no single camera angle shows more than a third of that. Three of the four reports were for faces the previous round's screenshot could not physically have contained. **List the faces before rendering, and place the subject once per face you need to see** — a bed per facing, one turned so its head end points at the camera, and one seen from behind.
- **`capture-window.ps1` silently photographed VS Code again**, exactly as the memory note warns, while `Get-Process game` showed the window alive and titled correctly. It is a foreground-focus race, not a missing window. **Check the shot actually contains the game before reading anything into it**; a longer `-DelaySeconds` fixed it.
- **`-Look` is relative and it accumulates across captures in one run.** That is what makes several angles possible without rebuilding — and it also means a second capture inherits the first one's turn, so the way back is an equal and opposite `-Look`, not a fresh one.
- **`. .\tools\dev-env.ps1` is per terminal, and a new terminal appears without warning.** The build failed with `fatal error C1083: Cannot open include file: 'array'`, which reads as a broken include path and is really just MSVC not being on `PATH` yet.

---

## Redstone blocks, and four ways to be wrong about them (M29h)

The signal engine is deliberately not built - the user ruled it out as "a week worth of work on its own minimum" - so this round is seven hundred block states that place, break, craft, drop and draw, and nothing that carries a signal. Every lesson below is about the *blocks*.

- **Never rewrite a source file with PowerShell, and this is the second time it has cost something.** A three-token variable rename went through `(Get-Content x) -replace ... | Set-Content -Encoding Ascii x`, and **`-Encoding Ascii` turns every byte of a multi-byte UTF-8 character into a question mark** - so six em-dashes in `Main.cpp`'s comments became `???`. No BOM, no NUL bytes, compiled clean, invisible in the build output. The existing rule already said to use the editor's own edit tool for source; a rename felt too small to bother, and that is exactly the judgement that keeps being wrong.
  - **The detector is a run count, not a character count.** A single `?` is legitimate everywhere - ternaries, prose. Two or three in a row is damage, one run per lost character. `tools/find-question-runs.ps1` enumerates them and `tools/check-ascii.ps1` reports bytes, BOM, NULs, non-ASCII and U+FFFD per file.
  - **File size proves nothing here.** A three-byte character becomes three question marks, so the byte count is identical before and after.
- **A count is not a diagnostic.** The material-table check reported "1 texture layer is claimed by two different material families" and nothing else, which says something is wrong and gives you nowhere to look. Making it name the layer and both families found the fault **in one run**: layer 0 is stone, and the appended target and note-block runs had no case in `blockTextureLayer`, so every state of both was drawing as stone. **When a check counts something, make it name the first one.**
- **A checker's first output is a statement about the checker. Third time.** `check-models.ps1` reported twenty disagreements; nineteen were the tool. It compares height *where it sits* and the two horizontal extents only as sizes, which handles the reference turning a model four ways about the vertical - but not a block **mounted on a wall or a ceiling**, which the redstone round introduced. A lightning rod lying on its side is four by four by sixteen against the reference's four by sixteen by four. Now: if the three extents match as a *set*, it is a rotation and not a disagreement, and a genuinely wrong size still changes the set.
- **`blockFacing` was missed for the fourth time, and the fourth time was reported by the user.** *"sticky piston has the sticky texture on all sides, it should only be on the push-y side. same for the block that spits fireworks and the angry looking block."* The furnace, the chest and the smithing table each taught this once already: **the world was right every time.** The mesher hands every face a real direction, so a miss here is invisible in the world and wrong in every inventory slot and every dropped item, because an icon draws two side quads at once and has no direction to give either of them.
  - The guard that ends it is an assert over **the whole expression an icon really evaluates**, `blockFacing` included: `machineFaceRole(facing, Side, blockFacing(id))` must be the front and `machineFaceRole(facing, Side, quarterTurn(blockFacing(id)))` must be a plain side. An assert that compares one half of a derivation with itself would have passed the entire time.
- **The mesher starts from `collisionBoxes`, so a block that collides with nothing draws nothing.** A button, a pressure plate, redstone dust, a rail and a tripwire are all walked straight through. The ladder had already carved out its own branch for this; five more needed one, and putting the extents in that branch *and* in the targeting outline would have been two copies of the same numbers. `uncollidableDrawnBoxes` and `drawsWithoutColliding` are the one owner.
- **Grep every new enumerator name before adding it.** `RedstoneLampLit` already existed in the fifth table run; adding it again would have been a duplicate the compiler catches loudly and a wasted hour if it had not.
- **A predicate that has to widen may need to move rather than be copied.** `isTorchBlock` sat seven hundred lines above `isRedstoneTorch`, so widening it meant either moving it or writing the torch's three id ranges out a second time. Moving it was free - all eleven callers are below - and the second copy would have been the project's commonest bug.
- **A layer constant needed early can be a literal, as long as the assert is real.** The redstone sprite run has to be readable by `postModel`, which is two thousand lines above the run that could compute it. `kRedstoneSpritesFirst = 1157` with `static_assert(kRedstoneSpritesFirst == kComposterCompostSprite + 1)` is the same arrangement `kTableLayerBase` already uses, and the assert is what stops the literal quietly rotting.
- **Sixteen tinted copies beat a runtime tint we do not have.** The reference ships redstone dust grey and multiplies a colour in from `power/15`; copied raw it is a white cross. Baking the reference's own formula into sixteen staged layers costs sixteen layers and makes a wire's picture one subtraction from its id.
- **A cut shape is still the cheapest way to add a family.** Buttons and pressure plates are `ShapedFamily` rows carrying a parent block, so twenty-six families arrived with no art, no name row, no hardness row, no tool row and no blast row.

## Potions, signs and the last three hundred entries (M29i–M29k)

- ⛔⛔ **I added three blocks that had been in the game for seven milestones.** Three enumerators, three table rows, six sprite names, a light-emission branch, a staging block, a Main.cpp loader entry and two bumped asserts — all for froglights that had sat in the third table run since M22. **The compiler caught it in one build**, which is the only reason it cost minutes; the same mistake in the *item* enum would have compiled and shipped a duplicate. The rule was already written down after `RedstoneLampLit` in M29h and it was not followed, because by the time you are three thousand ids into an enum, "does this exist?" feels like a question you already know the answer to. **Grep the name. Every time. It is four seconds.**
- **A run bounded by `kLastItem` breaks the moment a run is added after it.** `isLingeringPotion` read `item <= ItemId::kLastItem`, which was true and correct — until twenty-three pottery sherds followed it and every sherd became a lingering potion. **Bound a run by its own last member, never by the end of the world**, and the same applies to `isExtraItem`, which is bounded at `DragonBreath` rather than at `kLastItem` for exactly this reason.
- **Where a table already has a `pitch` parameter, eight variants can cost one sprite and one recording.** Eight goat horns differ only in the note; `goatHornPitch` steps two octaves and that is the whole implementation. **Ask what the variants actually differ by before staging eight of anything.**
- **Two projectile kinds can serve forty-one items if the item rides on the shot.** `Shot::payload` and `Landing::payload` carry which brew was thrown, so the species table stays at five rows. A row per potion would have been eighty-two.
- **Where a mechanic writes to the world, it does not belong in a system that only reads it.** A splash potion's effect and a lingering potion's cloud are applied in `Main.cpp` off the landing queue, exactly as an egg's chick is. `Projectile.cpp` still has no idea a player exists.
- **A name that is an ordinary English word is free; a coined one is not.** *Sheaf*, *mourner*, *snort* and *angler* describe what is pressed into a sherd and nobody owns them. A **track title** is the other kind, so all twenty-two disc names are ours — and the honest thing is to say so in the divergence list rather than to pick synonyms and hope.
- ⚠ **`tools/check-ascii.ps1` and `tools/find-question-runs.ps1` take a positional `-Path`.** Called bare in a chained command they stop on an interactive prompt, which looks exactly like a hung build.

## Mining, drops and loot: five shapes a clean build cannot see

**None of this was found by playing.** Everything below came out of a `/W4` build, a 45-second soak, four reviewers and a handful of throwaway statistical probes, and every one of the five shapes is a bug that a clean build and a clean soak *both* reported as fine. That is the point of the section: these are the failures that survive the bar this project usually clears.

- ⛔⛔ **A table sized by an enum guarantees a row exists; it does not guarantee anyone chose it.** `kMining` is `std::array<MiningRow, kBlockIdCount>` and every element is initialised, so appending a block run gets a row for free — and that row is whatever the final `default:` said. "Sized by the enum, so a new block cannot be missed" was written down as a safety property and it was **false**: the size assert only ever proved the array was long enough. What makes it true is the **sentinel** — the `default:` returns a row that cannot be a real block, and a strided sweep rejects it, so a new run is a **build error instead of a playtest finding**. This is the same shape as *"a `default:` that returns a real value hides every missing entry"*, one level up: the hiding place moved from the switch to the array's own completeness. **The fix for a silent default is never care; it is a value that cannot be mistaken for an answer, plus something that reads it.** 100 ids had already fallen through this exact hole to hardness 1.0 — the wool family, sixteen concretes, sixteen terracottas — and nobody noticed for twenty milestones because 1.0 is a plausible number.
- ⛔ **Two rolls are not two questions.** Gravel drops gravel *or* flint. Written as two independent rolls — 10% flint, 90% gravel — roughly one break in eleven pays **both**, because nothing connects them. Written as one roll compared against two ranges, exactly one wins. The distinction is not a detail of the random number generator, it is the **shape of the question**: mutually exclusive outcomes are one draw read twice, independent outcomes are two draws. `BlockDrops.hpp` puts it in the data — **same salt means alternatives off one roll, different salt means independent rolls** — so the author of a row has to state which one they mean, and the reader can see it. **Anywhere you write "or" in a drop description, ask whether the code can produce "and".**
- ⛔ **A position-derived roll is deterministic, and for anything renewable that is a farm.** Hashing the block position means the same action always gives the same result — which is *exactly right* for a one-shot block, because it survives a save/reload and cannot be re-rolled by quitting. Applied to leaves it produced a known **"apple cell": one position that pays an apple every single time**, at 100%, forever, on a block that grows back. **Determinism is a property you want for anything the world only offers once, and a constant you have handed the player for anything that regrows.** The split is written into the table as `rerollsEachBreak` — crops, stems, leaves, melon, pumpkin and mushroom blocks mix in a session-local nonce; ore, gravel and glass do not — and there are asserts in both directions, that no nonce reaches a one-shot block and that a nonce genuinely moves a melon. **Ask of every hashed roll: can the player make this block again?**
- ⛔ **A `static_assert` that only ever evaluates one context proves that context and nothing else.** The block-drop sweep ran every id through `resolveBreak` **bare-handed** and passed, which is why it read as coverage. It had never evaluated a single row with shears, a pickaxe or Silk Touch in hand, so every leaf, every ore and every glass block was checked in the one configuration where they are least interesting. The mining sweep had the same shape: a `static_assert` comparing one side of a derivation against itself. **A sweep's value is the product of the ids it walks and the contexts it walks them in, and only the first number is visible in the code.** When an assert passes on the first try over three thousand rows, look at what it is holding *fixed*.
- ⛔⛔ **A guaranteed drop where the reference has a pool entry is a 24× error, and no assert can catch it.** The witch paid a guaranteed 4–8 redstone — **6.0 per kill against the reference's 0.25**. Every sanity check in the file passed, because each of them is true: the range is ordered, the item exists, the count is positive, the weights sum to the stored total. **A sanity check has no opinion about what the reference says.** The error was structural rather than numeric — redstone belongs *inside* the shared rare pool at weight 1, competing with the other six, and pulling it out to be its own guaranteed drop multiplied it by twenty-four while leaving every number individually defensible. This is the whole argument for reading the behaviour pack rather than the wiki, and for **writing the reference's own structure down, not just its numbers**: only 19 of 58 species had drops that matched, and none of the other 39 looked wrong.
- **When four accessors each carry their own range tests, they will disagree, and the disagreement is invisible.** `blockHardness`, `harvestTier`, `harvestTool` and `yieldsDrop` each walked the block runs themselves and **already covered different sets of them** before anything was changed. One table of `{hardness, tool, tier, toolRequired}` and four one-line readers cannot drift, and the 291 ids whose harvest requirement moved are the size of the drift that had accumulated. **Four functions asking the same question about the same id is one table waiting to be written** — the `ShapedFamily` lesson again, applied to behaviour rather than to shape.
- **The cheapest way to close a duplication exploit can be a block id rather than a save-format field.** A generated chest that rolls its contents at generation time cannot stay a pure function of `(seed, chunkCoord)`; one that rolls on first *interaction* can, but then something must remember it has been rolled. A **marker block id** does that: rolling converts it to a plain chest with `setBlock`, which flags the chunk modified, so the chunk is loaded rather than regenerated from then on — **no new field, no format bump, and the flag was already there for another reason**. What made it necessary was a detail two systems away: `saveChests` discards an empty chest rather than writing it, so *open → empty → save → reload* would have found an unrolled marker and paid twice. **Before adding a field to the save format, ask which existing write already makes the world remember.**
- **A latched context beats reading live state at the moment you need it.** `DeathContext` records cause, killer and burning **at the tick health crosses zero**, because eight separate sites lower health and each overwrites the last. The position is latched too, and quantised — `separate()` shoves corpses apart every frame, so a roll keyed on the creature's live position had quietly become frame-rate dependent for the second time. **If a value is read once, at one instant, by one consumer, latch it at that instant; anything that reads it later is reading a different thing.** The same latch is what let `DeathCause` stop using `0` to mean both "the player" and "nobody".

## Sixteen agents in parallel: seams, comments and asserts (2026-08-18)

**Twenty-one survey agents read the codebase, sixteen fix agents wrote in parallel, eight reviewers read the diff.** It landed a five-figure change across ninety-odd files, both presets clean at `/W4`. It is the largest single round this project has run and the second one to fail in the same place as the first: **not inside any brief, but between them.** `CLAUDE.md`'s bug shape #14 - a rule that did not travel - and the "six agents in parallel" entry above are the same lesson at a third scale, so what is here is what *only* the bigger round could teach.

- ⛔⛔ **A parallel round hides a behaviour change inside a refactor's label, and every reviewer then checks the label.** Creatures gained a **damage-immunity window** they had never had. Two comments called it a de-duplication of a rule that "had split three ways" - it had existed exactly **once**, in `Player.cpp`, and the creature half was written that day. So the reviewers assigned "were the shared constants extracted cleanly?" all answered **yes, correctly**, and not one of them thought to time a kill: fast weapons roughly halve against mobs and a converging pack loses most of its blows. It is right per Bedrock and it is a **gameplay change**, and the only reason it is written down here rather than discovered by the user is a reviewer whose whole brief was the seams between agents. **A refactor's commit message is a claim about the past, and it is the one claim in the diff nobody verifies.** Grep the "duplicate" it says it removed and count the copies yourself; if the answer is one, the label is wrong and something new just shipped.
- ⛔⛔ **A wrong comment is worse than no comment, and a parallel round manufactures them.** About twenty landed - most written by one agent *about another agent's code*, and true only until that agent's work landed on top. Two were load-bearing. One told a reader a guard was **redundant**, when deleting it corrupts every chest in the world; one told a reader an **overload did not exist**, when it did, which is an invitation to add a second one and make every existing call ambiguous. Nothing warns, nothing tests, and a confident comment in this codebase's voice is trusted precisely because the rest of them have earned it. **Write comments about code you are editing; where you must describe someone else's, say when you read it.** A comment that names a file and a line is checkable; one that asserts a fact about the whole tree is not.
- ⛔⛔ **"Built, clean, and never called" survives even a round convened to hunt it.** `GAPS.md` exists for this shape, the round's brief named it, and it produced two fresh ones: `World::takeFizzes` and `World::blastFuse` were each built, documented at length, reviewed by more than one agent - and **wired to nothing**. `takeFizzes` was worse than merely silent: the buffer it drains is **capped**, so with no drainer `recordFizz` became a **permanent no-op after the first 64 events** and every lava-meets-water hiss for the rest of the session was lost. **A producer with a capped buffer and no consumer is not a dormant feature, it is a bug in the producer**, and it is the case where "unused" stops being free. Both are called now, from `Main.cpp`. The check has not changed and still takes four seconds: **grep the new function's name and count call sites outside its own header.**
- ⛔ **A completeness sweep over one system finds correctness bugs in another, and that is a reason to run one.** Wiring the orphaned `SoundEvent` rows was bookkeeping - until the ladder, fire, lava and breath events had to be *placed*, and placing the bow's shot turned up a **fourth hand-rolled tool-wear path**: the bow read the durability constant directly instead of going through the wear helper, so it wore wrong and snapped in silence. **The missing sound was the symptom; the missing call to the owner was the disease.** An event that nothing plays is a map of the places nobody has walked through recently, so **when a sweep says a row is unreachable, go and look at the code that should have reached it** rather than adding the call and moving on.
- ⛔ **Widening a predicate reaches the consumers that were relying on its narrowness.** `isWashedAway` grew to cover rails, carpets, redstone, tripwire and snow, which is the reference's real list and correct. Three sites in `Main.cpp` - lighting a fire and the two seed-sowing paths - spelled "is this cell free?" as `inCell == Air || isWashedAway(inCell)`, which was *nearly* right only while the predicate happened to mean cross plants, vines and torches. The day it widened, all three silently gained the power to **destroy a rail, a carpet, a redstone line, tripwire or any depth of snow, with no drop and no feedback**. The correct predicate, `isReplaceable`, was already in the file and **four lines above one of them** - it needed only `&& !isFluid` to become the answer, which is what `cellIsFree` now is. This is bug shape #2 - widening a family predicate kills every early-out behind it - and the general form is worse than the `isFurnace` case, because a dead `case` label is at least dead: **a widened predicate in a boolean expression does not stop working, it starts doing more.** `isReplaceable` is now deliberately *decoupled* from it with asserts in both directions, so the next widening cannot re-couple them by accident.
- ⛔ **An assert can be born wrong, and the replacement for a wrong assert is the likeliest place for the same defect.** Several added this round proved a derivation against itself - shape #11, already in `CLAUDE.md`'s opening box, added anyway. One reviewer found that the *fix* for a tautological assert was tautological in the same way, which is the part worth remembering: whoever rewrites it is holding the wrong mental model that produced it, so it reproduces. **Have the replacement checked by someone who did not write the original**, or write it to fail first - change the value it guards, watch the build break, put the value back.
- ⚠ **MSVC's C4062 is off by default, even at `/W4`, so a `switch` with no `default:` over an enum is silent.** An agent wrote it down as a safety property, then **measured it** and found its own claim false: C4061/C4062 are off-by-default warnings and this build does not enable them, so a missing `case` compiles quietly. What actually holds a switch complete here is a `static_assert` on `Enum::Count` plus a second one that walks the cases, because the count alone cannot see a label move between groups. **Measure the compiler's behaviour before documenting it as a guarantee** - and note the shape: the agent was right to check, and checking is what turned a false safety claim into two real asserts.

⛔ **A line number in a comment is a citation that rots silently, and the rot rate is far worse than anyone guesses.** A census of every `file.cpp:NNN` reference across all 139 source files found **18 of them, and 13 were stale** - 72%. Two pointed 600+ lines away, one named a closing brace, one cited the *lava* row of `RESEARCH.md` under a sentence about suffocation, and two carried the same wrong number because one was copied from the other — **`Fluid.hpp:31` and `Tick.hpp:17`, both still wrong, and the twinning is the tell**: a citation that is wrong in two files at once was never checked in either, it was propagated. They rot without anyone touching them: an agent editing the *cited* file breaks a citation in a file it never opened, so no review of either diff can catch it. **Cite the symbol, not the line** - "the `!isNetherWoodBlock` guard in `blockFuelSeconds`" survives every edit that "`Smelting.cpp:347`" does not, and it is no longer to type. `Block.hpp` now holds **zero** line-numbered cross-references and that is a property worth keeping; it is greppable in one command, so it can be checked rather than hoped for.
⛔⛔ **Verifying that a citation still points at the right line is the cheap half; verifying that what it *claims* is still true is the half that matters.** Auditing one file found 16 checkable claims about code elsewhere and **4 false - a 25% rot rate in a single file**, and not one of them was a wrong line number. The worst told a reader that `Creature.hpp` asserts `kSpawnEggLayers` against the species count. It does not - it asserts the sum of *both* egg runs - so a reader adding a 59th species and trusting the comment would widen the first run, which is **precisely the edit the comment 35 lines below forbids**, because those item ids are in saved inventories on disk. **A comment that argues for a save-corrupting edit is worse than no comment**, and nothing in this project catches one. Rank them by what a reader would *do*: a comment recommending a breaking edit, then one claiming a guard that does not exist, then one calling a load-bearing thing redundant, and only last a wrong line number.

**Negative claims rot fastest, because the world only has to move once.** A third of the false four was a note marked **"No caller"** — true when it was written about the *display* side, and false by the time a *gameplay* caller appeared, which is a change made in a different file by someone who never saw the note. "X does not exist", "nothing calls this", "there is no id for Y" and "this is redundant" are all claims about the **absence** of something, and absence is exactly what a future edit adds. A positive claim ("this is called from `Main.cpp`") gets checked the moment it is wrong, because the thing it names is still there to look at; a negative one is invisible until it has already misled someone. **So date every negative claim and name what would make it false** — "no caller as of 2026-08-18; grep `takeFizzes` outside its own header" is checkable in four seconds and self-destructs honestly. And be aware of the direction of the damage: the `!isNetherWood` guard in `blockFuelSeconds` was once described as belt and braces, and that word alone is enough to make a tidy-minded reader delete the only thing standing between a warped fence gate and the furnace.

⛔⛔ **A generated audit table that *reconstructs* the logic instead of *calling* it will be trusted over the code it claims to describe.** `blocks.csv` was believed to be a dump of the shipping functions. It was a **model of them** - a separate implementation, written to produce the same answers - and it contained answers the real function has no branch capable of producing. Several agents spent real time chasing a 92-id "gap" that existed only in the table. The failure is not that the model drifted; it is that **nothing about a CSV tells a reader whether it was measured or imagined**, and a table of three thousand rows carries an authority no prose claim would. **A probe must call the shipping function**, through the same header a caller uses, so that a wrong answer in the table is a wrong answer in the game. Where that is genuinely impossible - the function is not `constexpr`, or lives behind a TU nothing else can link - **the table must say so in its first line**, naming what it reconstructs and against what. A probe that links the real code is also usually easier to write than the model of it, which is the part that makes this an easy habit rather than a discipline.

⛔ **Compare against the authority, never against a sibling.** An agent filed a defect against `FallingBlock.cpp` on the strength of it disagreeing with `ItemEntity.cpp` - and retracted it once it compared both against the mesher, because `ItemEntity.cpp` was the buggy one. Two implementations disagreeing tells you exactly one thing: at least one is wrong. It does not tell you which, and the older or more familiar of the two is not evidence. **This is `CLAUDE.md`'s bug shape #11 applied to reviewing rather than to asserts** - an assert that compares one side of a derivation against itself proves nothing, and a review that compares one consumer against another proves nothing either. Find the thing that *owns* the answer - the table, the model JSON, the wiki, the mesher - and measure both against that. The cost of getting this wrong is not just a wasted review: a retracted finding is cheap, but the same reasoning **applied as a fix** would have propagated the bug into the correct file.

⛔⛔ **Every serious bug this round was found by a probe, and not one was found by reading.** The list is uniform: the furnace **duplicating every input stack** (`slots::merge` empties its source, so a `moved == wanted.count` test compared 1 against 0), **612 blocks silently removed from the fuel table** by a predicate narrowing under a consumer that meant something else by it, **every shaped block's drop mirrored** for four milestones, **8 beehives demoted from wood to rock** in a table rewrite. All four sat in code that had been read - in several cases read by more than one agent, in a round convened to read it - and all four fell out of a probe in minutes. Reading is good at the bugs that look wrong; **it is worthless against a table that is merely wrong**, because a wrong row looks exactly like a right one and there are three thousand of them. **On any table-shaped question, write the probe first, not last.** It is thirty lines, it compiles against the real headers with `cl` in about a minute, it needs no window and no GPU, and its output is a number you can put in a commit message instead of a claim you cannot check. The probe is also what makes a change *safe* rather than merely correct: dumping every id before and after an edit turns "I think this only affects carpets" into "16 removed, 0 added, 0 retimed", which is the sentence that has justified every table change in this project that did not later come back.

⛔⛔ **Twelve instruments lied in a single day, five caught only by distrusting a number that looked clean.** `Get-ChildItem <game.dir> -Filter *.obj` without `-Recurse` returned **0** to two agents independently, because CMake nests objects one level down under `game.dir\src\` - and **the agreement of those two zeros proved nothing, since both instruments shared one blind spot**; it came within a message of retiring a working verification method fleet-wide. `.ninja_deps` answered 1 for six of seven headers because its paths are interned by index. `check-ascii.ps1` printed a green line for files that do not exist, the only tell being a byte count of 0. A `static_assert` sweep counted one **quoted inside a `///` doc comment** as real. **The recurring tells: a uniform result is the instrument rather than the world; agreement of two zeros is not agreement; and a detector reporting absence must print how many probes landed** - that one line would have caught four of the twelve. **The control that settles any of them in one step is to point the same query at a case you know is positive: a detector that reports absence everywhere is reporting nothing.**

⛔⛔ **I ran a control on the wrong symbol while the right one sat in my own output, and every digit of the wrong control was true.** A claim about `kLootChestTables` was "confirmed refuted" by a sweep of `BlockId` - 16 hits, 0 inside asserts - and the probe that produced it had printed `kLootChestTables = 2` on the same screen. **A true measurement of a neighbouring symbol is the most persuasive way to be wrong, because every digit survives checking.** It arrived with a second error that made it look corroborated: the same sweep counted six `static_assert`s where there are five, having swallowed one quoted in a doc comment, and that off-by-one shifted every index so that "assert 5" named a genuinely self-contained assert. **The wrong count and the wrong verdict had a single cause and each made the other look confirmed.** Cost: a correct finding was withdrawn for an hour. **When the candidate set is small, stop counting and list it, flagging each hit as code or comment** - one flag would have caught both errors at once. **The fix for this took three passes in one evening and each pass was falsified by a case nobody went looking for**, which is the real lesson: *"a control must use the same symbol"* was refuted by a `professionName` probe that is same-symbol *and* an instrument control; *"an instrument control must use a different symbol"* was refuted the same way, the defining property being a **known expected answer**; and *"print how many probes landed"* was refuted by an agent that printed `Probes landed: 3 of 4` **as a hardcoded string inside its own demonstration of that rule**, the true value being 2 of 4 - **a narrated count is indistinguishable from a real one.** **A rule generalised from a single instance is the same defect as a claim derived from a single grep.** And the class none of them can see: a `BeeNest` absence claim with **two flawless controls** - 45 in-file, 39 same-symbol elsewhere, non-uniform, invented name 0 - **was still false**, because the id is built by `beeNestAtLevel` and `beeNest` case-insensitively returns 11 in that same file. Four more died identically: `ChainmailHelmet + piece`, sixteen `CreatureKind` by index cast, seventeen `BiomeId` by `static_cast`, `familyRoughness` through a packed row. **Before any control, ask whether the token is the only way the concept could be spelled** - and for a behavioural absence, follow the gate rather than the name, because `inFire = inFire || block == BlockId::Fire` proves structurally that no campfire id can satisfy it, which no name count can.

⛔ **A well-formed argument is self-reinforcing, and nobody re-checks the premise once the reasoning has started.** Recorded as *"the shape of the debate was right and its premise was invented"*. Every step after the first was sound, which is precisely why no reader went back. **Check the premise before admiring the argument**, and be most suspicious when a chain of reasoning is unusually tidy.

⛔ **Documentation is the one job where transcribing a stale input does not merely fail to help - it manufactures the exact defect the job exists to remove, and launders it into the most authoritative document in the tree.** Eight persistence facts arrived in a briefing; **five were stale, and every one said "absent" about something already present.** Had they been transcribed, five finished features would have been re-queued as gaps in the document agents read to choose work. The root cause was one blockquote in `WorldStore.hpp` reading *"Not yet written by anyone"* about call sites that had landed hours earlier: **one stale comment, two hops, five false entries** - and worse, acting on that comment *is* the breaking edit, because a diligent reader writes the call sites and creates two writers for one field. **Check every claim in source before writing it down, including claims from whoever assigned the work.**

⛔ **An unrecorded gap is worse than a stale one: a stale entry gets refuted, a missing entry is never evaluated.** The worst data-loss bug in the tree - dropped items were not saved, so everything lying on the ground at quit was gone - **had no entry at all** in the document agents read to pick work, so it could not be chosen. **The bias is structural: a document organised by what exists has no natural place to record what was never built, and the more completely absent something is, the less likely it is to appear.** The test that finds them: **ask it of a *property*, not a *feature*** - persistence, failure reporting, torn-file behaviour - because a feature always has a section and a property usually does not.

> ✅ **Written up at 10:45 on 2026-08-19 and CLOSED the same afternoon** - `Main.cpp` now carries both call sites, and `GAPS.md` G5.24 opens with a STOP box because the entry's own falsifier fired while the entry still read "caller missing". **That round trip is the second half of this lesson and it points the other way**: recording the gap is what got it built, and *not* re-reading the record is what nearly got it built twice. **A negative entry earns its keep only if somebody re-runs its falsifier before acting on it.** *Measured: `saveDrops|loadDrops` in `Main.cpp` = 0 at 11:13, 0 at 11:50, 8 at 15:36.*

⛔ **A control that shares no character classes with the claim is vacuous, and the failure is invisible because the control returns exactly the expected answer.** Verifying that a `GAPS.md` rewrite had landed, five probes for phrases from the new text — `3230 blocks drawn`, `It runs three checks, not one`, `271 with no model`, `0 unfound`, `ceasing to be blind` — every one returned **0**, while the stale figures the edit was meant to remove still appeared to be present. The reading was unambiguous: *the edit did not land, and the tool's success report lied.* It was one step from reverting a correct edit, and the only reason it did not happen is that the file was opened and read instead. **The edit was perfect. The probe was broken.**

The mechanism: `$h = Select-String -Path GAPS.md -Pattern ([regex]::Escape($s)) -SimpleMatch`. **`[regex]::Escape` and `-SimpleMatch` are both escapes, and applying both escapes twice.** `[regex]::Escape('3230 blocks drawn')` yields `3230\ blocks\ drawn`, because it escapes whitespace; `-SimpleMatch` then searches for that backslash **literally**, and no such text exists. Every claim string contained a space, so every claim probe was structurally incapable of matching.

**And the control passed.** The invented name was `ZZ-not-in-file`, which must return 0 — and it did. But `[regex]::Escape('ZZ-not-in-file')` is `ZZ-not-in-file` **unchanged**, because a hyphen is not escaped and there is no space in it: the control never travelled the code path that broke the claim. It confirmed the detector could return 0, which was never in doubt. **A zero from a probe that cannot match anything is indistinguishable from a zero from a probe that matched nothing.** In the same run, an earlier probe for `1,792` had worked correctly — a comma is not escaped either — so the instrument was *selectively* blind, passing on every pattern without whitespace and failing on every pattern with it, which is the worst possible mixture because half its output was right.

**The rule:** give a control the same shape as its subject — same spaces, same punctuation, the same trip through whatever escaping, quoting or normalising the probe applies. An invented single token is the weakest form available and should be the *addition* to a graded control, never the whole of one; here a graded control of three real phrases with known counts (`bundle capacity 64 weight` 1, `Model coverage holes` 1, `a phrase never written here` 0) exercised the space-bearing path and would have exposed the defect on its first row. **The generalisation of the standing `$LF`/`$lf` lesson is that a control must share the claim's *mechanism*, not merely its expected answer** — 2026-08-19.
---

## Twenty-seven agents, and nothing could be compiled (2026-08-19)

**The round's defining constraint was a hardware throttle: no `cmake`, no `cl`, no `ninja`, no `glslc`, no `game.exe`, for five hours.** That removed the safety net every other round leaned on, and what it exposed was that *the instruments were never as good as the green builds had made them look*. Fourteen instrument failures were documented in one session, nearly all self-caught. These are the ones that generalise.

### Measuring a codebase built out of id runs

- **⛔ `Enum::Name` is a PREFIX of `Enum::NameFirst` and `Enum::NameLast`, and this codebase is built out of run families.** `BlockId::Vine` measured **8 unanchored and 4 word-bounded** — and it **does not exist**. Four `static_assert`s used it, which is a guaranteed compile error sitting in the id authority, invisible because nothing in the tree had compiled. The coordinator's own brief circulated "`BlockId::Vine` = 15" as proof the id existed. **Word-bound every enum probe** (`BlockId::Vine\b`), **and say which enum you measured** — `Vine` is declared in `BlockShape`, not `BlockId`.
- **The controls were live, correct, and could not save it.** `CopperDoor` = 0 and an invented token = 0 both behaved perfectly. **"Is this token a prefix of a different symbol?" is not a control question** — it belongs to ⓪, the question no control can answer.
- **`BlockShape` ∩ `BlockId` = ∅ — 22 names against 709, zero overlap — so any `BlockId::<shape name>` is a guaranteed error, and there are 21 more live rounds.** `Ladder`, `Door`, `Fence`, `Slab`, `Stairs`, `Wall`, `Sign`, `Bed`, `Button`… **What makes the mistake feel safe is that several of them DO exist as ids under run spellings** (`SlabRunFirst`, `StairsRunFirst`), so a reader half-remembering "slabs are in `BlockId`" is right about the concept and wrong about the token. That is exactly how `BlockId::Vine` came to be written next to `VineFirst`/`VineLast`.
- **The strongest fix is to make the prefix question unaskable: extract the enumerator list from the enum body and index into it, rather than grepping for a name.** One agent did this and `CreatureVoice::Zombie` vs `ZombieVillager` never got the chance to bite.

### Instruments that lie quietly

- **⛔ A failed regex anchor does not return zero — it returns THE WHOLE FILE.** A probe anchored on `kStairFamilies\s*=\s*\{` missed, because the real spelling is brace-init `kStairFamilies{{` with no `=` at all; a failed `Match` leaves `Index = 0`, so the brace-matcher then spanned **673,449 characters** and reported a plausible number that was a whole-file count wearing a table's name. **It was caught only because the probe printed `$m.Success` next to the count. Print whether your anchor bound anything.**
- **⛔ Print the CORPUS size, not just the subject count.** `Get-ChildItem -Path DIR -Include *.hpp,*.cpp` returns **zero files** without `-Recurse` — it filters the path. Two probes used it and one was a step from filing "world/ includes nothing from other layers". **A count of the subject can look plausible; a count of the corpus cannot lie quietly.**
- **⛔ `$hash.Count` in PowerShell returns the KEY NAMED `Count`** when one exists — and `Count` is an enumerator in most of these id families. A probe printed `Effect enumerators declared: True`. Use `$hash.Keys.Count` and `ContainsKey()`.
- **`Select-String` is case-INSENSITIVE, so a `k`-prefixed constant matches ordinary English.** `kEye` matched **"keyed"** in three prose comments — 6 hits where the truth was 3. `\b` on the right end cannot help; the collision is on the left. **Use `-CaseSensitive` for any camelCase or `k`-prefixed identifier**, which also aligns `Select-String` with the `grep` tool's default and removes the two-tools-two-behaviours discrepancy.
- **A field name can belong to two structs, which is the `Vine` trap one level down.** `CreatureSpecies::halfWidth` measures 72 hits in `Creature.cpp`; `ProjectileSpecies::halfWidth` measures 6. An unscoped `\bhalfWidth\b` returns ~78 and **buries a true zero-readers claim under another type's traffic**. A bare identifier count is meaningless until you have named which type owns it.

### The absence standard, corrected three times in one night

- **⛔ A known-truth ABSENT case cannot validate an instrument whose failure mode is OVER-REPORTING absence.** A probe was validated on the `BlockId::Vine` case, passed cleanly, and then reported `ItemId::Shears` and `ItemId::Bow` as absent — *"four compile errors in the item authority"*. Both are declared. **`Vine → ABSENT` passes just as cleanly on a parser that says ABSENT about everything.** Cause: the enumerator regex lacked multiline, so **every enumerator preceded by a doc comment was dropped**, and this codebase documents most of them.
- **The remedy is a positive control from the same syntactic neighbourhood — and the first version of that rule was itself wrong.** "Use a *documented* enumerator" is **necessary but not sufficient**: `ItemId::TurtleHelmet` is documented, and it passed. **The discriminator is doc-block LENGTH, not presence** — a look-back-one-line extractor handles a 1-line block and fails on a 3-4 line one. In `ItemId` only 2 of 26 documented enumerators are 1-line, so a randomly drawn control catches the bug ~92% of the time, and the failing draw is the one that ships a false STOP.
- **The control that actually works is drawn from the KNOWN-FAILURE SET, not designed.** Require `ItemId::Shears` and `ItemId::Bow` to resolve DECLARED, plus one heavily-documented run anchor. **You cannot know in advance which region your instrument is weak in, so a control you invented yourself has no power over it.**
- **⛔ Four enums here are declared on a SINGLE LINE — `ArmourSlot`, `FluidKind`, `Placement`, `DragButton` — and a per-line extractor drops 8 of their 12 enumerators.** That was the prescribed remedy, and it has this hole. **Require two structurally different extractors to agree before believing any absence.**
- **In a use-vs-declaration diff, the DECLARATION side is almost always the weaker instrument** — uses are uniform (`Name::X`), declarations carry doc comments, array declarators, `= init`, multi-token types and nested scopes. **A vacuous control on the use side costs nothing; a vacuous control on the declaration side is what produces a STOP.** A regex `^\s*\w+\s+(\w+)\s*;` cannot match `mat4 shadowMatrices[4];`, so a legal `frame.shadowMatrices[cascade]` came back UNDECLARED — against the one member the file's own comment flags as uninstrumented.

### A measurement is a fact with a timestamp

- **⛔ Record "already fixed", never "never broken".** An agent measured `Block.hpp`, found `BlockId::Vine` absent, and concluded the reported bug had never existed — asking to halt the fleet. **Committed HEAD settled it: 4 word-bounded uses in the committed file, fixed 12 minutes before it looked.** Another agent hit the identical setup and did not take the bait, because `BlockId::VineFirst` went **0→4** in the same file: a fix leaves positive evidence, not just an absence.
- **The remedy is one step: when your measurement contradicts a report, check the file's mtime against the report's timestamp BEFORE concluding the report was wrong.** `git show HEAD:<path>` settles it outright and is read-only.
- **The opposite direction bit six documents in the same session** — a claim of absence going stale as things get built. **The asymmetry is measured: a claim that something is ABSENT was wrong five times in eight, then six in six, and never once in the other direction.**
- **A prior "not yet built" note is the most dangerous kind, because it is often phrased as an IMPERATIVE and no negative-word search can reach it.** `Copper.hpp` carried a precise work order for eight table rows **that were already done**; honouring it would have added 40 ids and **renumbered every saved world**, because stair/slab family identity is allocated purely by position. Its own falsifier — *"measured 11:52: zero"* — was a four-hour-old photograph.

### Prose, comments and the searches that miss them

- **A comment that records what it REPLACED will always trip a sweep for the phrase it replaced.** After correcting two stale claims, a sweep for `"until that lands"` and `"do not exist here yet"` still returned 1 each — both were the agent's own quotations of the old wording, each refuted in the next clause. **List the hits and read them; do not trust the count.**
- **Prose search in C++ is biased toward FALSE ABSENCE, which is the direction this project already gets wrong most.** A phrase search for a sentence returned 0 while three phrases from the same comment returned 1 — **the sentence wraps, and no contiguous search crosses a newline plus a `// ` prefix**. Settle it by normalising (strip `//`, collapse whitespace) or by `git diff`, never by the phrase.
- **A whole-file regex counts comments, and this codebase writes falsifiers INTO comments.** `BlockId::Waxed` was flagged as undeclared twice — both hits were inside `///` blocks telling a reader to search for exactly that token. **Flag every hit CODE or COMMENT; a `static_assert` count that swallowed one quoted inside a doc block gave six where there are five.**

### Asserts, and where they come from

- **⛔ An assert written from the same sentence as the code proves nothing, and this is sharper than "an assert comparing one side of a derivation against itself".** Copper oxidised **four times too slowly**, and *all three of its asserts passed* — because the asserts and the code were both transcribed from the same wrong reading of the reference. **Ask where an assert's expected value CAME FROM. If the answer is "the same place as the implementation", it is decoration.** The replacement derives its five values from the reference's own worked example, with a negative twin.
- **A tautology can be cited as proof by files that do not contain it.** `bothTurnDerivationsAgree()` reduced to the same expression on both sides with a float cast applied to each — true for every possible content of every table it names. **`FaceGeometry.hpp` listed it first in its dependants block and `ItemEntity.cpp` said "`FallingBlock.cpp` owns the proof".** Both citations were hollow, and neither file could see that by reading itself.
- **⚠️ But check the citation before striking it:** the `ItemEntity.cpp` claim turned out to describe the *real* proof (4 walls × 3 turns × 2 mirrors = 24), not the tautology. **Striking it would have deleted an accurate pointer to the file's only safety.**
- **`std::array` silently ZERO-FILLS a short initialiser — and the table is only protected if its all-defaults row is INVALID.** `kTables` escapes because `poolCount == 0` is independently rejected. `kEntries` does not, because the NSDMI weight `1` is also the commonest real weight, so a lost row passes **all four** of its asserts and an emerald becomes a weighted-nothing forever. **Derive the check from intent** — count the deliberate empty rows and assert the count — not from the size.

### Process

- **⛔ A retraction is worth more than a defended number, and this round produced several.** An agent demoted its own BLOCKING finding to a duplicate on discovering the file already documented the issue *with its own counter-argument and falsifier*. Another rewrote a row to LEAD with its retraction rather than leaving a stale prio-0 alarm open. A third named its own controls worthless — *"I reached the right answer structurally, not because my controls demonstrated it; I reported the confidence without having earned it."*
- **Controls verify the INSTRUMENT; they cannot verify the POPULATION.** An agent's claim of "20 pieces, not 24" was wrong at **25/25**, because the sixth material is `Emberite` — a coined name no Minecraft-derived list could contain. Every control passed throughout. **Applied as written, it would have removed one stale claim from a document and added two.**
- **The coordinator is not exempt, and made the exact error it had been warning 27 agents about.** Inserting a section into `TIMELINE.md`, the replacement text did not re-add the `# Phase 7 — Frontier` heading it had matched — **deleting a top-level heading while adding a section**. Caught one minute later by counting `^# Phase` headings and finding 8 where there are 9. **Verify the anchor you consumed still exists after the edit; a heading is the easiest thing in a document to delete by accident and the hardest to notice.**
- **An absence of readers is not an absence of awareness.** A BLOCKING finding about a dead field was demoted after a same-symbol/different-file probe found the issue **already documented, dated the same day, with the agent's own counter-hypothesis and falsifier almost verbatim**. **Run the same-symbol/different-file probe BEFORE filing, not as later hardening.**
